# 校园互助系统 - 功能开发需求文档

**文档版本：** v2.1  
**创建日期：** 2024-01-01  
**最近更新：** 2024-01-02  
**文档维护：** 开发团队

---

## 📋 版本说明

### v2.1 (2024-01-02) - 数据库完善版
- ✅ 补充完整的数据库表设计（14张核心表）
- ✅ 新增统计字段：view_count、favorite_count、comment_count
- ✅ 完善索引设计，提升查询性能
- ✅ 验证 MainLayout.vue 数据支持完整性
- ✅ 所有功能模块数据库字段完全覆盖

### v2.0 (2024-01-02) - 简化版
- ✅ 简化功能，聚焦核心需求
- ✅ 移除信用分系统（简化评价体系）
- ✅ 保留智能审核机制（自动审核 + 人工审核）
- ✅ 保留管理员内容管理和账号管理功能
- ✅ 明确实名认证权限控制
- ✅ 补充详细的功能实现说明
- ✅ 整合所有文档内容到一个文件

**简化原则：**
- 聚焦失物招领、闲置交易、跑腿服务三大核心功能
- 保留必要的审核和管理功能
- 简化用户权限和评价体系
- 确保系统简洁易用，便于开发和维护

---

## 一、项目概述

### 1.1 项目背景

**国外研究现状：**

在国外，校园互助类数字化服务发展较早，呈现多元化特点。例如：
- **Facebook Campus**：学生可组建学习小组、发布二手物品交易、寻找失物招领线索，依托社交平台快速传播，但功能较为分散，缺乏专门的整合性互助系统设计
- **Nextdoor**：虽以社区为服务范围，但其中针对校园区域的子板块，允许学生发布互助需求（如学习辅导、物品借用），其基于地理位置的信息推送模式，为校园互助系统在精准触达用户方面提供了参考

**国内研究现状：**

在国内，校园互助类系统的研究与应用处于快速发展阶段：
- **校园二手交易平台**：部分高校自主开发了校园二手交易平台，专注于闲置物品的流转，功能涵盖物品发布、在线议价等，解决了校园内闲置资源浪费的问题，但仅聚焦单一场景，未整合失物招领等需求
- **校园失物招领小程序**（如"校园拾物"类工具）：在多个高校推广，用户可快速发布和查询失物信息，部分小程序还支持物品特征拍照识别，但与其他校园服务的联动性不足
- **课程答疑系统**：一些高校平台为学生提供了课程资源共享、学习组队功能，助力学习协作，但覆盖的校园互助场景有限，尚未形成综合化的校园互助生态

**存在问题：**
- 信息分散，缺乏统一平台
- 功能单一，未整合多种互助场景
- 用户体验有待提升

### 1.2 项目目标

本项目旨在设计并实现一个基于 **Spring Boot + Vue** 的校园帮系统，通过该系统实现对**失物招领、闲置交易、跑腿服务**等校园互助场景的统一管理。系统将服务于高校师生，提供一个全面、便捷、安全的校园互助平台，解决传统互助渠道信息分散、效率低的问题。

**核心目标：**
1. 整合校园内各类互助服务，提供统一的信息发布和管理平台
2. 提高校园互助服务的效率和用户体验
3. 实现智能审核机制，保障信息安全
4. 实现实时消息通知，提升用户参与度
5. 提供简洁的管理后台，支持内容和用户管理

### 1.3 技术架构

**后端技术：**
- **Spring Boot 2.7.18**：采用 Spring Boot 搭建项目基础框架，利用其自动化配置、一键启动特性，简化开发流程，缩短项目周期
- **MyBatis-Plus 3.5.3**：数据库操作框架，简化 CRUD 操作
- **Spring Security + JWT**：用户认证和授权
- **Redis**：缓存和会话管理，提升系统性能
- **MySQL 8.0**：选用 MySQL 作为数据库，依托其成熟稳定的特性，保障用户信息、需求数据的安全存储，同时通过索引优化提升数据查询性能
- **WebSocket**：实时消息推送
- **Spring Mail**：邮件通知功能

**前端技术：**
- **Vue 3.x**：Vue能够构建一个响应式的用户界面，其渐进式特性允许按需选择所需功能。轻量级与高效，易于维护
- **Vite**：快速的前端构建工具
- **Element Plus**：UI 组件库，提供丰富的组件
- **Pinia**：状态管理库
- **Vue Router**：路由管理，实现单页应用
- **Axios**：HTTP 请求库
- **WebSocket 客户端**：实时消息接收

**技术可行性分析：**

本系统采用 Spring Boot 作为后端开发框架，搭配 Vue 构建前端界面，以 MySQL 作为数据存储方案。三者均为成熟且广泛应用的技术，学习资料丰富，适配大学生的技术掌握范围。

- **后端**：通过 Spring Boot 可快速搭建项目框架，简化配置流程，高效处理用户管理、需求发布等核心业务逻辑
- **前端**：借助 Vue 的轻量特性，能高效构建符合校园场景的简洁界面，适配 PC 端操作需求
- **数据库**：MySQL 的稳定性可满足系统数据存储需求，支持数据的安全存储与高效查询

整体技术栈无需复杂的技术突破，开发难度可控，适合独立或小团队完成。

---

## 二、功能模块详细说明

### 2.1 用户管理模块

#### 2.1.1 用户注册与登录

**注册功能详细实现：**

1. **注册页面布局**
   - 邮箱输入框（必填）
   - 邮箱验证码输入框（必填）
   - 获取验证码按钮（60秒倒计时）
   - 密码输入框（必填，密码强度提示）
   - 确认密码输入框（必填）
   - 用户协议复选框（必填）
   - 注册按钮

2. **注册流程**
   ```
   用户输入邮箱
       ↓
   点击"获取验证码"
       ↓
   系统验证邮箱格式
       ↓
   系统检查邮箱是否已注册
       ↓
   生成6位数字验证码
       ↓
   发送邮件（使用 Spring Mail）
       ↓
   Redis 存储验证码（有效期5分钟）
       ↓
   用户输入验证码和密码
       ↓
   前端验证：
       - 验证码不能为空
       - 密码长度至少8位
       - 密码必须包含字母和数字
       - 两次密码输入必须一致
       - 必须同意用户协议
       ↓
   提交注册请求
       ↓
   后端验证：
       - 验证码是否正确（从 Redis 获取）
       - 验证码是否过期
       - 邮箱是否已被注册
       - 密码强度是否符合要求
       ↓
   密码加密（BCrypt）
       ↓
   保存用户信息到数据库
       ↓
   返回注册成功
       ↓
   自动跳转到登录页面
   ```

3. **后端实现示例**
   ```java
   @Service
   public class UserService {
       
       @Autowired
       private RedisTemplate<String, String> redisTemplate;
       
       @Autowired
       private JavaMailSender mailSender;
       
       @Autowired
       private UserMapper userMapper;
       
       /**
        * 发送邮箱验证码
        */
       public void sendEmailCode(String email) {
           // 1. 验证邮箱格式
           if (!email.matches("^[A-Za-z0-9+_.-]+@(.+)$")) {
               throw new BusinessException("邮箱格式不正确");
           }
           
           // 2. 检查邮箱是否已注册
           User existUser = userMapper.selectByEmail(email);
           if (existUser != null) {
               throw new BusinessException("该邮箱已被注册");
           }
           
           // 3. 生成6位数字验证码
           String code = String.format("%06d", new Random().nextInt(999999));
           
           // 4. 存储到 Redis（有效期5分钟）
           String key = "email:code:" + email;
           redisTemplate.opsForValue().set(key, code, 5, TimeUnit.MINUTES);
           
           // 5. 发送邮件
           SimpleMailMessage message = new SimpleMailMessage();
           message.setTo(email);
           message.setSubject("校园互助系统 - 注册验证码");
           message.setText("您的验证码是：" + code + "，有效期5分钟，请勿泄露给他人。");
           mailSender.send(message);
       }
       
       /**
        * 用户注册
        */
       @Transactional
       public void register(RegisterDTO dto) {
           // 1. 验证验证码
           String key = "email:code:" + dto.getEmail();
           String cachedCode = redisTemplate.opsForValue().get(key);
           if (cachedCode == null) {
               throw new BusinessException("验证码已过期，请重新获取");
           }
           if (!cachedCode.equals(dto.getCode())) {
               throw new BusinessException("验证码错误");
           }
           
           // 2. 检查邮箱是否已注册
           User existUser = userMapper.selectByEmail(dto.getEmail());
           if (existUser != null) {
               throw new BusinessException("该邮箱已被注册");
           }
           
           // 3. 验证密码强度
           if (!dto.getPassword().matches("^(?=.*[A-Za-z])(?=.*\\d)[A-Za-z\\d]{8,}$")) {
               throw new BusinessException("密码必须至少8位，包含字母和数字");
           }
           
           // 4. 创建用户
           User user = new User();
           user.setEmail(dto.getEmail());
           user.setPassword(BCrypt.hashpw(dto.getPassword(), BCrypt.gensalt()));
           user.setNickname("用户" + System.currentTimeMillis());
           user.setRole("USER");
           user.setStatus(1);
           user.setCreateTime(new Date());
           
           // 5. 保存到数据库
           userMapper.insert(user);
           
           // 6. 删除验证码
           redisTemplate.delete(key);
       }
   }
   ```

**登录功能详细实现：**

1. **登录页面布局**
   - Tab 切换：密码登录 / 验证码登录
   - 邮箱输入框
   - 密码输入框 / 验证码输入框
   - 记住我复选框
   - 登录按钮
   - 忘记密码链接
   - 注册链接

2. **登录流程（密码登录）**
   ```
   用户输入邮箱和密码
       ↓
   前端验证：
       - 邮箱不能为空
       - 密码不能为空
       ↓
   提交登录请求
       ↓
   后端验证：
       - 查询用户是否存在
       - 验证密码是否正确
       - 检查账号状态（是否被封禁）
       ↓
   生成 JWT Token
       ↓
   Token 存储到 Redis（有效期7天）
       ↓
   返回 Token 和用户信息
       ↓
   前端保存 Token 到 localStorage
       ↓
   跳转到首页
   ```

3. **后端实现示例**
   ```java
   @Service
   public class AuthService {
       
       @Autowired
       private UserMapper userMapper;
       
       @Autowired
       private RedisTemplate<String, String> redisTemplate;
       
       @Autowired
       private JwtUtil jwtUtil;
       
       /**
        * 密码登录
        */
       public LoginVO login(LoginDTO dto) {
           // 1. 查询用户
           User user = userMapper.selectByEmail(dto.getEmail());
           if (user == null) {
               throw new BusinessException("用户不存在");
           }
           
           // 2. 验证密码
           if (!BCrypt.checkpw(dto.getPassword(), user.getPassword())) {
               throw new BusinessException("密码错误");
           }
           
           // 3. 检查账号状态
           if (user.getStatus() == 2) {
               throw new BusinessException("账号已被封禁");
           }
           
           // 4. 生成 JWT Token
           String token = jwtUtil.generateToken(user.getId(), user.getEmail(), user.getRole());
           
           // 5. 存储 Token 到 Redis（7天）
           String key = "token:" + user.getId();
           redisTemplate.opsForValue().set(key, token, 7, TimeUnit.DAYS);
           
           // 6. 返回登录信息
           LoginVO vo = new LoginVO();
           vo.setToken(token);
           vo.setUserId(user.getId());
           vo.setEmail(user.getEmail());
           vo.setNickname(user.getNickname());
           vo.setAvatar(user.getAvatar());
           vo.setIsVerified(user.getIsVerified());
           vo.setRole(user.getRole());
           
           return vo;
       }
   }
   ```

#### 2.1.2 用户信息管理

**基本信息编辑：**
- 昵称、头像、性别、年级、专业、手机号
- 实时保存，无需点击保存按钮
- 头像上传支持裁剪功能

#### 2.1.3 实名认证（可选，但跑腿和交易必须认证）

**实名认证的重要性：**
- ⚠️ **未认证用户只能使用失物招领功能**
- ✅ **实名认证后才能发布和参与闲置交易**
- ✅ **实名认证后才能发布和接单跑腿任务**

**认证页面设计：**

1. **未认证状态（显示认证表单）**
   
   **页面提示：**
   ```
   📌 实名认证说明
   - 实名认证后可以发布闲置商品和跑腿任务
   - 实名认证后可以参与商品交易和接单跑腿
   - 未认证用户只能使用失物招领功能
   - 认证信息仅用于平台安全管理，不会泄露给其他用户
   - 提交后，管理员将在1-3个工作日内完成审核
   ```
   
   **认证表单：**
   - 真实姓名（必填，2-10个中文字符）
   - 身份证号（必填，18位）
   - 学号/工号（必填，6-20个字符）
   - 用户类型（必填，单选：学生/教师）
   - 证明文件上传（必填）：
     - 学生：学生证照片、校园卡照片、教务系统截图
     - 教师：教师证照片、工作证照片、校园系统截图
     - 要求：至少1张，最多3张，jpg/jpeg/png，单张不超过5MB

2. **认证中状态（显示审核进度）**
   ```
   ⏳ 您的实名认证正在审核中
   - 提交时间：2024-01-02 14:30:25
   - 预计审核时间：1-3个工作日
   - 请耐心等待管理员审核
   - 审核结果将通过站内消息和邮件通知您
   ```

3. **已认证状态（显示认证信息）**
   ```
   ✅ 恭喜您，实名认证已通过！
   - 认证时间：2024-01-02 16:30:25
   - 您现在可以发布闲置商品和跑腿任务
   - 您现在可以参与商品交易和接单跑腿
   ```

4. **已拒绝状态（显示拒绝原因）**
   ```
   ❌ 您的实名认证未通过审核
   - 审核时间：2024-01-02 16:30:25
   - 拒绝原因：上传的证明文件不清晰，请重新上传高清照片
   - 您可以修改信息后重新提交认证
   ```

**认证实现示例：**
```java
@Service
public class VerificationService {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 提交实名认证
     */
    @Transactional
    public void submitVerification(VerificationDTO dto, Long userId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 检查是否已认证
        if (user.getIsVerified() == 1) {
            throw new BusinessException("您已完成实名认证");
        }
        
        // 3. 检查是否有待审核的认证
        if ("PENDING".equals(user.getVerificationStatus())) {
            throw new BusinessException("您的认证正在审核中，请勿重复提交");
        }
        
        // 4. 更新用户信息
        user.setRealName(dto.getRealName());
        user.setIdCard(dto.getIdCard());
        user.setStudentId(dto.getStudentId());
        user.setUserType(dto.getUserType());
        user.setVerificationProof(JSON.toJSONString(dto.getProofImages()));
        user.setVerificationStatus("PENDING");
        user.setVerificationSubmitTime(new Date());
        
        // 5. 保存到数据库
        userMapper.updateById(user);
        
        // 6. 发送通知给管理员（WebSocket）
        // ...
    }
    
    /**
     * 管理员审核实名认证
     */
    @Transactional
    public void auditVerification(Long userId, Boolean approved, String reason, Long adminId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 检查认证状态
        if (!"PENDING".equals(user.getVerificationStatus())) {
            throw new BusinessException("该认证不是待审核状态");
        }
        
        if (approved) {
            // 审核通过
            user.setIsVerified(1);
            user.setVerificationStatus("VERIFIED");
            user.setVerificationAuditTime(new Date());
            user.setVerificationAuditAdminId(adminId);
            
            // 发送通知给用户
            sendNotification(userId, "实名认证已通过", "恭喜您，实名认证已通过！您现在可以发布闲置商品和跑腿任务了。");
        } else {
            // 审核拒绝
            user.setVerificationStatus("REJECTED");
            user.setVerificationAuditTime(new Date());
            user.setVerificationAuditReason(reason);
            user.setVerificationAuditAdminId(adminId);
            
            // 发送通知给用户
            sendNotification(userId, "实名认证未通过", "很抱歉，您的实名认证未通过审核。拒绝原因：" + reason);
        }
        
        // 3. 保存到数据库
        userMapper.updateById(user);
    }
}
```

#### 2.1.4 用户权限说明

**权限矩阵：**

| 功能 | 未认证用户 | 已认证用户 | 管理员 |
|------|-----------|-----------|--------|
| 注册登录 | ✅ | ✅ | ✅ |
| **失物招领** |  |  |  |
| - 浏览失物信息 | ✅ | ✅ | ✅ |
| - 发布失物信息 | ✅ | ✅ | ✅ |
| - 认领失物 | ✅ | ✅ | ✅ |
| **闲置交易** |  |  |  |
| - 浏览商品 | ✅ | ✅ | ✅ |
| - 发布商品 | ❌ **需要实名认证** | ✅ | ✅ |
| - 购买商品 | ❌ **需要实名认证** | ✅ | ✅ |
| **学习互助** |  |  |  |
| - 浏览问题 | ✅ | ✅ | ✅ |
| - 发布问题 | ✅（需登录） | ✅ | ✅ |
| - 回答问题 | ✅（需登录） | ✅ | ✅ |
| - 采纳答案 | ✅（需登录，仅发布者） | ✅（仅发布者） | ✅ |
| **其他功能** |  |  |  |
| - 评论收藏 | ✅ | ✅ | ✅ |
| - 私信沟通 | ✅ | ✅ | ✅ |
| - 内容审核 | ❌ | ❌ | ✅ |
| - 用户管理 | ❌ | ❌ | ✅ |

**权限验证实现：**
```java
@Aspect
@Component
public class VerificationAspect {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 需要实名认证的接口
     */
    @Around("@annotation(requireVerification)")
    public Object checkVerification(ProceedingJoinPoint joinPoint, RequireVerification requireVerification) throws Throwable {
        // 1. 获取当前用户
        Long userId = SecurityUtils.getCurrentUserId();
        User user = userMapper.selectById(userId);
        
        // 2. 检查是否已认证
        if (user.getIsVerified() != 1) {
            throw new BusinessException("该功能需要实名认证，请先完成实名认证");
        }
        
        // 3. 执行原方法
        return joinPoint.proceed();
    }
}

// 使用注解
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 发布商品（需要实名认证）
     */
    @PostMapping("/publish")
    @RequireVerification
    public Result publishGoods(@RequestBody GoodsDTO dto) {
        // ...
    }
}
```

---

### 2.2 失物招领模块

#### 2.2.1 失物发布（详细实现）

**发布页面：** `/lost-found/publish`

**表单设计：**
1. 物品名称（必填，单行输入框，最多50字）
2. 物品分类（必填，下拉选择）
   - 证件类、电子产品、生活用品、书籍、其他
3. 物品描述（必填，多行文本框，最少10字，最多500字）
4. 丢失时间（必填，日期时间选择器）
5. 丢失地点（必填，单行输入框，最多100字）
6. 联系方式（可选，单行输入框，默认使用私信）
7. 物品图片（可选，图片上传组件）
   - 最多上传9张
   - 支持拖拽上传
   - 支持图片预览和删除
   - 图片压缩（前端）
8. 悬赏金额（可选，数字输入框，0-10000元）

**发布流程：**
```
用户填写失物信息
    ↓
前端验证：
    - 必填项不能为空
    - 物品描述至少10字
    - 图片格式和大小检查
    - 悬赏金额不能为负数
    ↓
上传图片到服务器
    ↓
提交发布请求
    ↓
后端验证：
    - 数据格式验证（@Validated）
    - 敏感词检测
    - 发布频率检测
    - 用户信用检测
    ↓
判断审核结果：
    ├─ 自动审核通过 → 状态设置为"待认领"，立即发布
    └─ 转人工审核 → 状态设置为"待审核"，进入审核队列
    ↓
保存到数据库
    ↓
返回发布成功
    ↓
跳转到详情页 / 我的发布页
```

**后端实现：**
```java
@Service
public class LostFoundService {
    
    @Autowired
    private LostFoundMapper lostFoundMapper;
    
    @Autowired
    private SensitiveWordService sensitiveWordService;
    
    @Autowired
    private PublishFrequencyService publishFrequencyService;
    
    /**
     * 发布失物
     */
    @Transactional
    public Long publishLostFound(LostFoundDTO dto, Long userId) {
        // 1. 敏感词检测
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(dto.getTitle() + " " + dto.getDescription());
        
        // 2. 发布频率检测
        boolean frequencyOk = publishFrequencyService.checkFrequency(userId, "LOST_FOUND");
        
        // 3. 用户信用检测（新注册用户7天内）
        User user = userMapper.selectById(userId);
        boolean newUser = Duration.between(user.getCreateTime().toInstant(), Instant.now()).toDays() < 7;
        
        // 4. 创建失物信息
        LostFound lostFound = new LostFound();
        BeanUtils.copyProperties(dto, lostFound);
        lostFound.setUserId(userId);
        lostFound.setImages(JSON.toJSONString(dto.getImages()));
        
        // 5. 判断审核状态
        if (checkResult.isPass() && frequencyOk && !newUser) {
            // 自动审核通过
            lostFound.setStatus("PENDING_CLAIM");
            lostFound.setAuditStatus("APPROVED");
            lostFound.setAuditTime(new Date());
        } else {
            // 转人工审核
            lostFound.setStatus("PENDING_REVIEW");
            lostFound.setAuditStatus("PENDING");
            
            // 记录触发原因
            String reason = "";
            if (!checkResult.isPass()) reason += "包含敏感词；";
            if (!frequencyOk) reason += "发布频繁；";
            if (newUser) reason += "新注册用户；";
            // 可以保存到日志表
        }
        
        lostFound.setCreateTime(new Date());
        
        // 6. 保存到数据库
        lostFoundMapper.insert(lostFound);
        
        // 7. 如果需要人工审核，发送通知给管理员
        if ("PENDING_REVIEW".equals(lostFound.getStatus())) {
            // 通知管理员审核
            notifyAdminForReview("LOST_FOUND", lostFound.getId());
        }
        
        return lostFound.getId();
    }
}
```

#### 2.2.2 失物浏览与搜索（详细实现）

**列表页：** `/lost-found/list`

**页面布局：**
```
┌─────────────────────────────────────────────────────────┐
│  [搜索框]  [搜索按钮]                    [发布失物按钮]  │
├─────────────────────────────────────────────────────────┤
│  分类筛选: [全部] [证件类] [电子产品] [生活用品] ...    │
│  状态筛选: [全部] [待认领] [认领中] [已认领] [已关闭]   │
│  地点筛选: [全部] [教学楼] [宿舍] [食堂] ...           │
│  排序方式: [最新] [浏览最多] [悬赏最高]                 │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [图片]  物品名称                               │   │
│  │          分类 | 丢失地点 | 发布时间             │   │
│  │          物品描述...                            │   │
│  │          悬赏: ¥50  浏览: 128次                │   │
│  │          [查看详情]                             │   │
│  └─────────────────────────────────────────────────┘   │
│  ...                                                    │
│  [分页器]                                              │
└─────────────────────────────────────────────────────────┘
```

**搜索实现：**
```java
@Service
public class LostFoundService {
    
    /**
     * 搜索失物列表
     */
    public PageResult<LostFoundVO> searchLostFound(LostFoundSearchDTO dto) {
        // 1. 构建查询条件
        Page<LostFound> page = new Page<>(dto.getPageNum(), dto.getPageSize());
        QueryWrapper<LostFound> wrapper = new QueryWrapper<>();
        
        // 关键词搜索（标题或描述）
        if (StringUtils.isNotBlank(dto.getKeyword())) {
            wrapper.and(w -> w.like("title", dto.getKeyword())
                              .or()
                              .like("description", dto.getKeyword()));
        }
        
        // 分类筛选
        if (StringUtils.isNotBlank(dto.getCategory())) {
            wrapper.eq("category", dto.getCategory());
        }
        
        // 状态筛选
        if (StringUtils.isNotBlank(dto.getStatus())) {
            wrapper.eq("status", dto.getStatus());
        } else {
            // 默认只显示待认领、认领中、已认领的
            wrapper.in("status", "PENDING_CLAIM", "CLAIMING", "CLAIMED");
        }
        
        // 地点筛选
        if (StringUtils.isNotBlank(dto.getLocation())) {
            wrapper.like("lost_location", dto.getLocation());
        }
        
        // 悬赏筛选
        if (dto.getHasReward() != null && dto.getHasReward()) {
            wrapper.gt("reward", 0);
        }
        
        // 不显示已删除的
        wrapper.eq("delete_flag", 0);
        
        // 排序
        if ("view".equals(dto.getSortBy())) {
            wrapper.orderByDesc("view_count");
        } else if ("reward".equals(dto.getSortBy())) {
            wrapper.orderByDesc("reward");
        } else {
            wrapper.orderByDesc("create_time");
        }
        
        // 2. 查询
        Page<LostFound> result = lostFoundMapper.selectPage(page, wrapper);
        
        // 3. 转换为 VO
        List<LostFoundVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 转换为 VO
     */
    private LostFoundVO convertToVO(LostFound lostFound) {
        LostFoundVO vo = new LostFoundVO();
        BeanUtils.copyProperties(lostFound, vo);
        
        // 解析图片
        if (StringUtils.isNotBlank(lostFound.getImages())) {
            vo.setImageList(JSON.parseArray(lostFound.getImages(), String.class));
        }
        
        // 获取发布者信息
        User user = userMapper.selectById(lostFound.getUserId());
        vo.setPublisherNickname(user.getNickname());
        vo.setPublisherAvatar(user.getAvatar());
        vo.setPublisherIsVerified(user.getIsVerified());
        
        // 格式化时间
        vo.setCreateTimeStr(formatTime(lostFound.getCreateTime()));
        
        return vo;
    }
}
```

#### 2.2.3 认领功能（详细实现）

**认领流程完整实现：**

1. **用户查看详情，点击"申请认领"**
   - 弹出认领申请对话框

2. **填写认领信息**
   ```vue
   <el-dialog title="申请认领" v-model="claimDialogVisible">
     <el-form :model="claimForm" :rules="claimRules" ref="claimFormRef">
       <el-form-item label="物品特征描述" prop="description">
         <el-input
           type="textarea"
           v-model="claimForm.description"
           placeholder="请详细描述物品的特征，以便发布者确认"
           :rows="4"
           maxlength="200"
           show-word-limit
         />
       </el-form-item>
       
       <el-form-item label="丢失时间" prop="lostTime">
         <el-date-picker
           v-model="claimForm.lostTime"
           type="datetime"
           placeholder="您记得的丢失时间"
         />
       </el-form-item>
       
       <el-form-item label="其他信息" prop="otherInfo">
         <el-input
           type="textarea"
           v-model="claimForm.otherInfo"
           placeholder="其他能证明物品归属的信息（可选）"
           :rows="2"
         />
       </el-form-item>
       
       <el-form-item label="证明文件" prop="proofImages">
         <el-upload
           :action="uploadUrl"
           :headers="uploadHeaders"
           :file-list="claimForm.proofImages"
           :on-success="handleUploadSuccess"
           :before-upload="beforeUpload"
           :limit="3"
           list-type="picture-card"
         >
           <el-icon><Plus /></el-icon>
         </el-upload>
         <div class="upload-tip">最多上传3张证明文件（可选）</div>
       </el-form-item>
     </el-form>
     
     <template #footer>
       <el-button @click="claimDialogVisible = false">取消</el-button>
       <el-button type="primary" @click="submitClaim" :loading="submitting">
         提交认领
       </el-button>
     </template>
   </el-dialog>
   ```

3. **后端处理认领申请**
   ```java
   @Service
   public class ClaimService {
       
       @Autowired
       private LostFoundMapper lostFoundMapper;
       
       @Autowired
       private ClaimRecordMapper claimRecordMapper;
       
       @Autowired
       private ChatSessionService chatSessionService;
       
       /**
        * 申请认领
        */
       @Transactional
       public void applyClaim(ClaimDTO dto, Long userId) {
           // 1. 查询失物信息
           LostFound lostFound = lostFoundMapper.selectById(dto.getLostFoundId());
           if (lostFound == null) {
               throw new BusinessException("失物信息不存在");
           }
           
           // 2. 验证状态
           if (!"PENDING_CLAIM".equals(lostFound.getStatus()) && !"CLAIMING".equals(lostFound.getStatus())) {
               throw new BusinessException("该失物当前不可认领");
           }
           
           // 3. 不能认领自己发布的
           if (lostFound.getUserId().equals(userId)) {
               throw new BusinessException("不能认领自己发布的失物");
           }
           
           // 4. 检查是否已经申请过
           QueryWrapper<ClaimRecord> wrapper = new QueryWrapper<>();
           wrapper.eq("lost_found_id", dto.getLostFoundId())
                  .eq("claimer_id", userId);
           ClaimRecord existRecord = claimRecordMapper.selectOne(wrapper);
           if (existRecord != null) {
               throw new BusinessException("您已申请认领过该失物");
           }
           
           // 5. 使用乐观锁更新失物状态
           int rows = lostFoundMapper.updateStatusWithVersion(
               dto.getLostFoundId(),
               "PENDING_CLAIM",
               "CLAIMING",
               lostFound.getVersion()
           );
           
           if (rows == 0) {
               throw new BusinessException("认领失败，请重试");
           }
           
           // 6. 创建认领记录
           ClaimRecord record = new ClaimRecord();
           record.setLostFoundId(dto.getLostFoundId());
           record.setClaimerId(userId);
           record.setDescription(dto.getDescription());
           record.setLostTime(dto.getLostTime());
           record.setOtherInfo(dto.getOtherInfo());
           record.setProofImages(JSON.toJSONString(dto.getProofImages()));
           record.setStatus("PENDING"); // 待确认
           record.setCreateTime(new Date());
           claimRecordMapper.insert(record);
           
           // 7. 创建私信会话
           chatSessionService.createSession(userId, lostFound.getUserId(), "LOST_FOUND", lostFound.getId());
           
           // 8. 发送通知给发布者
           notifyPublisher(lostFound.getUserId(), "收到认领申请", 
               "您发布的失物《" + lostFound.getTitle() + "》收到了认领申请，请及时查看");
       }
       
       /**
        * 确认认领
        */
       @Transactional
       public void confirmClaim(Long claimRecordId, Long userId) {
           // 1. 查询认领记录
           ClaimRecord record = claimRecordMapper.selectById(claimRecordId);
           if (record == null) {
               throw new BusinessException("认领记录不存在");
           }
           
           // 2. 查询失物信息
           LostFound lostFound = lostFoundMapper.selectById(record.getLostFoundId());
           if (lostFound == null) {
               throw new BusinessException("失物信息不存在");
           }
           
           // 3. 验证权限（只有发布者可以确认）
           if (!lostFound.getUserId().equals(userId)) {
               throw new BusinessException("无权操作");
           }
           
           // 4. 验证状态
           if (!"PENDING".equals(record.getStatus())) {
               throw new BusinessException("该认领已处理");
           }
           
           // 5. 更新认领记录
           record.setStatus("CONFIRMED");
           record.setConfirmTime(new Date());
           claimRecordMapper.updateById(record);
           
           // 6. 更新失物状态
           lostFound.setStatus("CLAIMED");
           lostFoundMapper.updateById(lostFound);
           
           // 7. 发送通知给认领者
           notifyUser(record.getClaimerId(), "认领成功", 
               "您认领的失物《" + lostFound.getTitle() + "》已被确认，请联系发布者取回物品");
       }
       
       /**
        * 拒绝认领
        */
       @Transactional
       public void rejectClaim(Long claimRecordId, String reason, Long userId) {
           // 类似确认认领的流程
           // ...
           
           // 更新认领记录为拒绝
           record.setStatus("REJECTED");
           record.setRejectReason(reason);
           record.setRejectTime(new Date());
           claimRecordMapper.updateById(record);
           
           // 失物状态恢复为待认领
           lostFound.setStatus("PENDING_CLAIM");
           lostFoundMapper.updateById(lostFound);
           
           // 发送通知
           notifyUser(record.getClaimerId(), "认领被拒绝", 
               "很抱歉，您认领的失物《" + lostFound.getTitle() + "》被拒绝。原因：" + reason);
       }
   }
   ```

---

### 2.3 闲置交易模块

**⚠️ 重要：实名认证要求**
- **发布商品**：必须完成实名认证，未认证用户无法发布商品
- **购买商品**：必须完成实名认证，未认证用户无法购买商品
- **浏览和搜索**：所有用户都可以浏览和搜索商品（无需实名认证）
- 未认证用户点击"发布商品"或"立即购买"时，系统提示："该功能需要实名认证，请先完成实名认证"

#### 2.3.1 商品发布（需要实名认证）

**权限验证：**
```java
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 发布商品（需要实名认证）
     */
    @PostMapping("/publish")
    @RequireVerification // 自定义注解，验证实名认证
    public Result publishGoods(@RequestBody @Validated GoodsDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long goodsId = goodsService.publishGoods(dto, userId);
        return Result.success(goodsId);
    }
}
```

**商品分类：**
- 数码产品（手机、电脑、平板、耳机等）
- 图书教材（教材、参考书、小说等）
- 服装鞋包（衣服、鞋子、包包等）
- 生活用品（日用品、小家电等）
- 运动健身（运动器材、健身用品等）
- 乐器（吉他、钢琴、小提琴等）
- 文创用品（文具、手账、周边等）
- 其他

**商品成色：**
- 全新（未使用）
- 几乎全新（使用次数很少，几乎无痕迹）
- 轻微使用痕迹（有轻微使用痕迹，功能正常）
- 明显使用痕迹（有明显使用痕迹，功能正常）

**发布流程同失物招领，额外增加：**
- **需要实名认证**（未认证用户无法发布商品）
- 商品成色必选
- 至少上传1张图片，最多9张
- **库存数量必填**（最少1件，支持多件商品）
- **交易方式选择**（单选：邮寄 或 自提）
  - 选择邮寄：需填写邮寄费用（可选，默认0）
  - 选择自提：需填写自提地点（必填，最多100字）

**商品发布表单字段：**
1. 商品标题（必填，最多50字）
2. 商品分类（必填，下拉选择）
3. 商品描述（必填，最少10字，最多500字）
4. **售价（必填，数字，大于0）**
5. 商品成色（必填，单选）
6. **库存数量（必填，整数，最少1件）**
7. 商品图片（必填，1-9张）
8. **交易方式（必填，单选：邮寄 或 自提）**
   - 邮寄：需填写邮寄费用（可选，默认0）
   - 自提：需填写自提地点（必填，最多100字）

**实名认证要求：**
- ⚠️ **发布商品必须完成实名认证**
- ⚠️ **购买商品必须完成实名认证**
- 未认证用户只能浏览商品，不能发布和购买
- 点击"发布商品"或"立即购买"时，如果未认证，提示"该功能需要实名认证，请先完成实名认证"

**发布流程：**
```
用户填写商品信息
    ↓
前端验证：
    - 必填项不能为空
    - 商品描述至少10字
    - 现价不能高于原价
    - 库存数量至少1件
    - 图片格式和大小检查
    ↓
上传图片到服务器
    ↓
提交发布请求
    ↓
后端验证：
    - 数据格式验证（@Validated）
    - 敏感词检测
    - 发布频率检测
    - 用户信用检测
    ↓
判断审核结果：
    ├─ 自动审核通过 → 状态设置为"在售"，立即发布
    └─ 转人工审核 → 状态设置为"待审核"，进入审核队列
    ↓
保存到数据库
    ↓
返回发布成功
    ↓
跳转到详情页 / 我的发布页
```

**后端实现：**
```java
@Service
public class GoodsService {
    
    @Autowired
    private GoodsMapper goodsMapper;
    
    @Autowired
    private SensitiveWordService sensitiveWordService;
    
    @Autowired
    private PublishFrequencyService publishFrequencyService;
    
    @Autowired
    private SystemMessageService systemMessageService;
    
    /**
     * 发布商品
     */
    @Transactional(rollbackFor = Exception.class)
    public Long publishGoods(GoodsDTO dto, Long userId) {
        // 1. 验证实名认证（@RequireVerification注解已处理，这里再次确认）
        User user = userMapper.selectById(userId);
        if (user.getIsVerified() != 1) {
            throw new BusinessException("发布商品需要实名认证，请先完成实名认证");
        }
        
        // 2. 验证价格（售价必须大于0）
        if (dto.getPrice() == null || dto.getPrice().compareTo(BigDecimal.ZERO) <= 0) {
            throw new BusinessException("售价必须大于0");
        }
        
        // 3. 验证库存
        if (dto.getStock() == null || dto.getStock() <= 0) {
            throw new BusinessException("库存数量必须大于0");
        }
        
        // 3. 敏感词检测
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(
            dto.getTitle() + " " + dto.getDescription()
        );
        
        // 4. 发布频率检测
        boolean frequencyOk = publishFrequencyService.checkFrequency(userId, "GOODS");
        
        // 5. 用户信用检测（新注册用户7天内）
        boolean newUser = Duration.between(
            user.getCreateTime().toInstant(), 
            Instant.now()
        ).toDays() < 7;
        
        // 6. 创建商品信息
        Goods goods = new Goods();
        BeanUtils.copyProperties(dto, goods);
        goods.setUserId(userId);
        goods.setImages(JSON.toJSONString(dto.getImages()));
        goods.setStock(dto.getStock());
        goods.setCurrentPrice(dto.getPrice()); // 售价
        goods.setVersion(0); // 乐观锁版本号初始化为0
        
        // 7. 判断审核状态
        if (checkResult.isPass() && frequencyOk && !newUser) {
            // 自动审核通过
            goods.setStatus("ON_SALE");
            goods.setAuditStatus("APPROVED");
            goods.setAuditTime(new Date());
        } else {
            // 转人工审核
            goods.setStatus("PENDING_REVIEW");
            goods.setAuditStatus("PENDING");
            
            // 记录触发原因
            StringBuilder reason = new StringBuilder();
            if (!checkResult.isPass()) {
                reason.append("包含敏感词：").append(String.join("、", checkResult.getMatchedWords())).append("；");
            }
            if (!frequencyOk) {
                reason.append("发布频繁；");
            }
            if (newUser) {
                reason.append("新注册用户；");
            }
            goods.setAuditTriggerReason(reason.toString());
        }
        
        goods.setCreateTime(new Date());
        goods.setUpdateTime(new Date());
        
        // 8. 保存到数据库
        goodsMapper.insert(goods);
        
        // 9. 如果需要人工审核，发送通知给所有管理员
        if ("PENDING_REVIEW".equals(goods.getStatus())) {
            String typeText = "商品";
            systemMessageService.sendMessageToAllAdmins(
                "ADMIN_AUDIT_REQUIRED",
                "新的" + typeText + "待审核",
                "有一条新的" + typeText + "《" + goods.getTitle() + "》需要审核，触发原因：" + goods.getAuditTriggerReason(),
                "GOODS",
                goods.getId()
            );
        }
        
        return goods.getId();
    }
}
```

**商品状态说明：**
- `PENDING_REVIEW`：待审核（新发布或编辑后需要审核）
- `ON_SALE`：在售（审核通过，可以购买）
- `SOLD_OUT`：已售完（库存为0）
- `CLOSED`：已关闭（卖家主动关闭）
- `REJECTED`：已拒绝（审核被拒绝）
- `ADMIN_OFFSHELF`：已下架（管理员下架）

**状态流转：**
```
PENDING_REVIEW（待审核）
    ↓
    ├─ 审核通过 → ON_SALE（在售）
    └─ 审核拒绝 → REJECTED（已拒绝）
        ↓
        重新发布 → PENDING_REVIEW（待审核）

ON_SALE（在售）
    ↓
    ├─ 库存为0 → SOLD_OUT（已售完）
    ├─ 卖家关闭 → CLOSED（已关闭）
    └─ 管理员下架 → ADMIN_OFFSHELF（已下架）
```

#### 2.3.2 商品浏览与搜索

**列表页：** `/goods/list`

**权限说明：**
- ✅ 所有用户都可以浏览和搜索商品（无需实名认证）
- ❌ 发布商品需要实名认证（点击"发布商品"按钮时验证）
- ❌ 购买商品需要实名认证（点击"立即购买"按钮时验证）

**页面布局：**
```
┌─────────────────────────────────────────────────────────┐
│  [搜索框]  [搜索按钮]                    [发布商品按钮]  │
├─────────────────────────────────────────────────────────┤
│  分类筛选: [全部] [数码产品] [图书教材] [服装鞋包] ...  │
│  价格筛选: [全部] [0-50] [50-100] [100-200] [200+]     │
│  成色筛选: [全部] [全新] [几乎全新] [轻微使用] [明显使用]│
│  状态筛选: [全部] [在售] [已售出]                        │
│  排序方式: [最新] [价格从低到高] [价格从高到低] [浏览量] │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [图片]  商品标题                               │   │
│  │          分类 | 成色 | 库存 | 发布时间           │   │
│  │          售价: ¥80                            │   │
│  │          浏览: 128次                           │   │
│  │          [查看详情] [立即购买]                  │   │
│  └─────────────────────────────────────────────────┘   │
│  ...                                                    │
│  [分页器]                                              │
└─────────────────────────────────────────────────────────┘
```

**搜索实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 搜索商品列表
     */
    public Page<Goods> searchGoods(GoodsSearchDTO dto) {
        // 1. 构建查询条件
        Page<Goods> page = new Page<>(dto.getPageNum(), dto.getPageSize());
        LambdaQueryWrapper<Goods> wrapper = new LambdaQueryWrapper<>();
        
        // 关键词搜索（标题或描述）
        if (StringUtils.hasText(dto.getKeyword())) {
            wrapper.and(w -> w.like(Goods::getTitle, dto.getKeyword())
                              .or()
                              .like(Goods::getDescription, dto.getKeyword()));
            
            // 保存搜索历史
            try {
                Long userId = SecurityUtils.getCurrentUserId();
                if (userId != null) {
                    searchHistoryService.saveSearchHistory(userId, dto.getKeyword(), "GOODS");
                }
            } catch (Exception e) {
                // 保存搜索历史失败不影响搜索功能
            }
        }
        
        // 分类筛选
        if (StringUtils.hasText(dto.getCategory())) {
            wrapper.eq(Goods::getCategory, dto.getCategory());
        }
        
        // 价格区间筛选
        if (dto.getMinPrice() != null) {
            wrapper.ge(Goods::getCurrentPrice, dto.getMinPrice());
        }
        if (dto.getMaxPrice() != null) {
            wrapper.le(Goods::getCurrentPrice, dto.getMaxPrice());
        }
        
        // 成色筛选
        if (StringUtils.hasText(dto.getCondition())) {
            wrapper.eq(Goods::getCondition, dto.getCondition());
        }
        
        // 状态筛选（默认只显示在售商品）
        if (StringUtils.hasText(dto.getStatus())) {
            wrapper.eq(Goods::getStatus, dto.getStatus());
        } else {
            wrapper.eq(Goods::getStatus, "ON_SALE");
        }
        
        // 不显示已删除的
        wrapper.eq(Goods::getDeleteFlag, 0);
        
        // 不显示待审核和已拒绝的（普通用户看不到）
        wrapper.notIn(Goods::getStatus, "PENDING_REVIEW", "REJECTED");
        
        // 排序
        if ("price_asc".equals(dto.getSortBy())) {
            wrapper.orderByAsc(Goods::getCurrentPrice);
        } else if ("price_desc".equals(dto.getSortBy())) {
            wrapper.orderByDesc(Goods::getCurrentPrice);
        } else if ("view".equals(dto.getSortBy())) {
            wrapper.orderByDesc(Goods::getViewCount);
        } else {
            wrapper.orderByDesc(Goods::getCreateTime);
        }
        
        // 2. 查询
        return goodsMapper.selectPage(page, wrapper);
    }
}
```

**搜索历史功能：**
- 在搜索框下方显示搜索历史（最多10条）
- 点击历史记录快速搜索
- 支持删除单条历史记录
- 支持清空所有历史记录

#### 2.3.3 商品详情展示

**详情页：** `/goods/detail/:id`

**权限说明：**
- ✅ 所有用户都可以查看商品详情（无需实名认证）
- ❌ 购买商品需要实名认证（点击"立即购买"按钮时验证）
- ❌ 联系卖家需要实名认证（点击"联系卖家"按钮时验证）

**页面内容：**
1. **商品图片区域**
   - 图片轮播（1-9张）
   - 支持放大查看
   - 支持左右切换

2. **商品基本信息**
   - 商品标题
   - 商品分类
   - 商品成色
   - 售价
   - 库存数量
   - 浏览量（只有审核通过的商品才显示）

3. **商品描述**
   - 多行文本展示
   - 支持换行和格式化

4. **交易信息**
   - 交易方式（邮寄/自提）
   - 邮寄费用（邮寄时显示）
   - 自提地点（自提时显示）

5. **卖家信息**
   - 卖家头像
   - 卖家昵称
   - 实名认证标识
   - 联系卖家按钮（跳转到聊天页面）

6. **操作按钮**
   - 立即购买（**需要实名认证**，未认证时提示"该功能需要实名认证，请先完成实名认证"）
   - 联系卖家（**需要实名认证**，跳转到聊天页面）

**权限控制：**
- 未审核通过的商品：只有发布者和管理员可以查看
- 已审核通过的商品：所有用户都可以查看
- 查看详情时增加浏览量（只有审核通过的商品才增加）

**后端实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 获取商品详情
     */
    public GoodsDetailVO getGoodsDetail(Long goodsId, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        // 权限检查
        boolean canView = false;
        if (goods.getUserId().equals(userId)) {
            // 发布者可以查看
            canView = true;
        } else if (userService.isAdmin(userId)) {
            // 管理员可以查看
            canView = true;
        } else if (!"PENDING_REVIEW".equals(goods.getStatus()) 
                && !"REJECTED".equals(goods.getStatus())) {
            // 审核通过的商品，所有用户都可以查看
            canView = true;
        }
        
        if (!canView) {
            throw new BusinessException("无权查看此商品");
        }
        
        // 只有审核通过的商品才增加浏览量（状态不是待审核和已拒绝）
        if (!"PENDING_REVIEW".equals(goods.getStatus()) 
            && !"REJECTED".equals(goods.getStatus())) {
            goodsMapper.incrementViewCount(goodsId);
        }
        
        // 查询卖家信息
        User seller = userMapper.selectById(goods.getUserId());
        
        // 构建详情VO
        GoodsDetailVO vo = new GoodsDetailVO();
        BeanUtils.copyProperties(goods, vo);
        vo.setSeller(seller);
        
        // 解析图片
        if (StringUtils.hasText(goods.getImages())) {
            vo.setImageList(JSON.parseArray(goods.getImages(), String.class));
        }
        
        return vo;
    }
}
```

#### 2.3.4 商品编辑功能

**编辑页面：** `/goods/edit/:id`

**可编辑内容：**
- 商品标题
- 商品分类
- 商品描述
- 售价
- 商品成色
- 库存数量
- 商品图片
- 交易方式（邮寄/自提）
- 邮寄费用（邮寄时）
- 自提地点（自提时）

**编辑规则：**
- 只有发布者可以编辑
- 如果商品已有订单，某些字段不能修改（如价格、库存）或需要提示影响
- 如果修改了内容，需要重新审核（如果包含敏感词或之前被拒绝）
- 编辑后，已存在的订单不受影响

**后端实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 更新商品
     */
    @Transactional(rollbackFor = Exception.class)
    public void updateGoods(Long goodsId, GoodsDTO dto, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!goods.getUserId().equals(userId)) {
            throw new BusinessException("无权修改此商品");
        }
        
        // 检查是否有订单
        long orderCount = orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getGoodsId, goodsId)
                .in(Order::getStatus, Arrays.asList("PENDING_PAYMENT", "PAID", "SHIPPED", "PENDING_PICKUP"))
                .eq(Order::getDeleteFlag, 0)
        );
        
        if (orderCount > 0) {
            // 有订单时，价格和库存不能修改
            if (!dto.getPrice().equals(goods.getCurrentPrice()) 
                || !dto.getStock().equals(goods.getStock())) {
                throw new BusinessException("商品已有订单，不能修改价格和库存");
            }
        }
        
        // 验证价格（售价必须大于0）
        if (dto.getPrice() == null || dto.getPrice().compareTo(BigDecimal.ZERO) <= 0) {
            throw new BusinessException("售价必须大于0");
        }
        
        // 记录原始状态
        String originalStatus = goods.getStatus();
        String originalAuditStatus = goods.getAuditStatus();
        
        // 更新商品信息
        BeanUtils.copyProperties(dto, goods, "id", "userId", "createTime", "viewCount", "version");
        goods.setImages(JSON.toJSONString(dto.getImages()));
        goods.setUpdateTime(new Date());
        
        // 判断是否需要重新审核
        boolean needReaudit = false;
        
        // 1. 如果之前被拒绝，编辑后需要重新审核
        if ("REJECTED".equals(originalStatus)) {
            needReaudit = true;
        }
        
        // 2. 检测敏感词
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(
            dto.getTitle() + " " + dto.getDescription()
        );
        if (!checkResult.isPass()) {
            needReaudit = true;
        }
        
        // 3. 如果内容有变化，需要重新审核
        if (needReaudit) {
            goods.setStatus("PENDING_REVIEW");
            goods.setAuditStatus("PENDING");
            goods.setAuditTriggerReason(
                "REJECTED".equals(originalStatus) ? "重新发布被拒绝的商品" : 
                "包含敏感词：" + String.join("、", checkResult.getMatchedWords())
            );
        }
        
        goodsMapper.updateById(goods);
        
        // 如果需要重新审核，发送消息给所有管理员
        if (needReaudit && "PENDING_REVIEW".equals(goods.getStatus())) {
            try {
                systemMessageService.sendMessageToAllAdmins(
                    "ADMIN_AUDIT_REQUIRED",
                    "新的商品待审核",
                    "有一条新的商品《" + goods.getTitle() + "》需要审核，触发原因：" + goods.getAuditTriggerReason(),
                    "GOODS",
                    goods.getId()
                );
            } catch (Exception e) {
                // 发送通知失败不影响业务逻辑
            }
        }
    }
}
```

#### 2.3.5 商品删除和下架功能

**删除功能：**
- 只有发布者可以删除
- 使用逻辑删除（`delete_flag = 1`）
- 有订单的商品不能删除（需要先完成或取消所有订单）

**下架功能：**
- 卖家可以主动下架商品
- 下架后，新用户无法看到，但已存在的订单不受影响
- 下架的商品可以重新上架

**后端实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 删除商品
     */
    @Transactional(rollbackFor = Exception.class)
    public void deleteGoods(Long goodsId, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!goods.getUserId().equals(userId)) {
            throw new BusinessException("无权删除此商品");
        }
        
        // 检查是否有未完成的订单
        long activeOrderCount = orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getGoodsId, goodsId)
                .in(Order::getStatus, Arrays.asList("PENDING_PAYMENT", "PAID", "SHIPPED", "PENDING_PICKUP"))
                .eq(Order::getDeleteFlag, 0)
        );
        
        if (activeOrderCount > 0) {
            throw new BusinessException("商品有未完成的订单，不能删除");
        }
        
        // 逻辑删除
        goods.setDeleteFlag(1);
        goods.setUpdateTime(new Date());
        goodsMapper.updateById(goods);
    }
    
    /**
     * 下架商品
     */
    @Transactional(rollbackFor = Exception.class)
    public void offshelfGoods(Long goodsId, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!goods.getUserId().equals(userId)) {
            throw new BusinessException("无权操作此商品");
        }
        
        if (!"ON_SALE".equals(goods.getStatus())) {
            throw new BusinessException("只有上架的商品才能下架");
        }
        
        goods.setStatus("CLOSED");
        goods.setUpdateTime(new Date());
        goodsMapper.updateById(goods);
    }
    
    /**
     * 重新上架商品
     */
    @Transactional(rollbackFor = Exception.class)
    public void reshelfGoods(Long goodsId, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!goods.getUserId().equals(userId)) {
            throw new BusinessException("无权操作此商品");
        }
        
        if (!"CLOSED".equals(goods.getStatus())) {
            throw new BusinessException("只有已关闭的商品才能重新上架");
        }
        
        // 检查库存
        if (goods.getStock() <= 0) {
            throw new BusinessException("库存为0，不能上架");
        }
        
        goods.setStatus("ON_SALE");
        goods.setUpdateTime(new Date());
        goodsMapper.updateById(goods);
    }
}
```

#### 2.3.6 我的发布（商品）

**页面路径：** `/goods/my-posts` 或 `/user/goods`

**功能：**
- 查看我发布的商品列表
- 支持筛选（全部、在售、已售出、待审核、已拒绝、已下架）
- 支持搜索（关键词搜索）
- 快速操作（编辑、删除、下架、查看详情）
- 显示订单数量（该商品的订单数）
- 显示浏览量（只有审核通过的商品才显示）

**页面布局：**
```
┌─────────────────────────────────────────────────────────┐
│  我的发布 - 商品                                        │
├─────────────────────────────────────────────────────────┤
│  状态筛选: [全部] [在售] [已售出] [待审核] [已拒绝] ... │
│  搜索框: [________] [搜索]                             │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [图片]  商品标题                               │   │
│  │          状态: [在售]  库存: 5件                │   │
│  │          售价: ¥80                              │   │
│  │          订单: 2笔  浏览: 128次                 │   │
│  │          [编辑] [删除] [下架] [查看详情]        │   │
│  └─────────────────────────────────────────────────┘   │
│  ...                                                    │
│  [分页器]                                              │
└─────────────────────────────────────────────────────────┘
```

**API接口：**
```java
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 获取我的商品列表
     */
    @GetMapping("/my-posts")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Page<GoodsVO>> getMyGoodsList(
            @RequestParam(required = false) String status,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        Long userId = SecurityUtils.getCurrentUserId();
        Page<GoodsVO> page = goodsService.getMyGoodsList(userId, status, keyword, pageNum, pageSize);
        return Result.success("查询成功", page);
    }
}
```

#### 2.3.7 管理员商品审核功能

**审核页面：** `/admin/goods-audit`

**功能：**
- 查看待审核商品列表
- 支持筛选（分类、关键词、地点）
- 审核操作（通过、拒绝）
- 审核原因填写
- 商品详情查看

**审核规则：**
- 自动审核：无敏感词、发布频率正常、非新用户
- 人工审核：包含敏感词、发布频繁、新注册用户

**API接口：**
```java
@RestController
@RequestMapping("/api/admin")
public class AdminController {
    
    /**
     * 获取待审核商品列表
     */
    @GetMapping("/goods/pending-audit")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<Page<Goods>> getPendingGoodsList(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) String location,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        Page<Goods> page = goodsService.getPendingAuditList(category, keyword, location, pageNum, pageSize);
        return Result.success("查询成功", page);
    }
    
    /**
     * 审核商品
     */
    @PostMapping("/goods/{id}/audit")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<Void> auditGoods(
            @PathVariable Long id,
            @RequestBody GoodsAuditDTO dto) {
        Long adminId = SecurityUtils.getCurrentUserId();
        goodsService.auditGoods(id, dto.getApproved(), dto.getReason(), adminId);
        return Result.success(dto.getApproved() ? "审核通过" : "审核拒绝", null);
    }
}
```

**Service实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 获取待审核商品列表
     */
    public Page<Goods> getPendingAuditList(String category, String keyword, String location,
            Integer pageNum, Integer pageSize) {
        Page<Goods> page = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<Goods> wrapper = new LambdaQueryWrapper<>();
        
        // 只查询待审核的商品
        wrapper.eq(Goods::getStatus, "PENDING_REVIEW");
        wrapper.eq(Goods::getAuditStatus, "PENDING");
        wrapper.eq(Goods::getDeleteFlag, 0);
        
        // 分类筛选
        if (StringUtils.hasText(category)) {
            wrapper.eq(Goods::getCategory, category);
        }
        
        // 关键词筛选
        if (StringUtils.hasText(keyword)) {
            wrapper.and(w -> w.like(Goods::getTitle, keyword)
                             .or()
                             .like(Goods::getDescription, keyword));
        }
        
        // 地点筛选（自提地点）
        if (StringUtils.hasText(location)) {
            wrapper.like(Goods::getTradeLocation, location);
        }
        
        wrapper.orderByDesc(Goods::getCreateTime);
        
        return goodsMapper.selectPage(page, wrapper);
    }
    
    /**
     * 审核商品
     */
    @Transactional(rollbackFor = Exception.class)
    public void auditGoods(Long goodsId, Boolean approved, String reason, Long adminId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!"PENDING_REVIEW".equals(goods.getStatus())) {
            throw new BusinessException("该商品当前不需要审核");
        }
        
        if (approved) {
            // 审核通过
            goods.setStatus("ON_SALE");
            goods.setAuditStatus("APPROVED");
            goods.setAuditReason(null);
        } else {
            // 审核拒绝
            goods.setStatus("REJECTED");
            goods.setAuditStatus("REJECTED");
            goods.setAuditReason(reason);
        }
        
        goods.setAuditTime(new Date());
        goods.setAuditAdminId(adminId);
        goods.setUpdateTime(new Date());
        
        goodsMapper.updateById(goods);
        
        // 发送系统消息给发布者
        String messageType = approved ? "GOODS_APPROVED" : "GOODS_REJECTED";
        String messageTitle = approved ? "商品审核通过" : "商品审核被拒绝";
        String messageContent = approved 
            ? "您的商品《" + goods.getTitle() + "》审核通过，已上架"
            : "很抱歉，您的商品《" + goods.getTitle() + "》审核被拒绝。原因：" + reason;
        
        systemMessageService.sendMessage(
            goods.getUserId(),
            messageType,
            messageTitle,
            messageContent,
            "GOODS",
            goodsId
        );
    }
}
```

#### 2.3.8 交易流程（需要实名认证）

**买家购买流程：**
```
买家浏览商品
    ↓
点击"立即购买"
    ↓
验证实名认证（未认证提示"该功能需要实名认证"）
    ↓
选择购买数量（不能超过库存）
    ↓
选择交易方式（邮寄/自提）
    ↓
【邮寄方式】选择收货地址（从地址列表中选择，或添加新地址）
    ↓
创建订单（生成唯一订单号，状态：PENDING_PAYMENT-待付款）
    ↓
系统自动创建私信会话（聊天页面显示订单信息卡片）
    ↓
双方通过聊天协商（可选）
    ↓
【订单状态流转】
    ├─ 卖家可以改价（状态仍为PENDING_PAYMENT）
    ├─ 买家付款（状态：PAID-已付款）
    ├─ 【邮寄方式】卖家发货（填写快递单号、物流公司）
    │   └─ 状态：SHIPPED（已发货）→ 买家确认收货 → COMPLETED（已完成）
    ├─ 【自提方式】无需发货，付款后直接进入待自提状态
    │   └─ 状态：PENDING_PICKUP（待自提）→ 买家确认收货 → COMPLETED（已完成）
    └─ 双方评价（可选）
```

**订单状态流转图：**
```
PENDING_PAYMENT（待付款）
    ↓
    ├─ 卖家改价 → 仍为 PENDING_PAYMENT
    ├─ 买家取消订单 → CANCELLED（已取消）
    └─ 买家付款 → PAID（已付款）
        ↓
        ├─ 【邮寄方式】卖家发货（填写快递单号） → SHIPPED（已发货）
        │   └─ 买家确认收货 → COMPLETED（已完成）
        └─ 【自提方式】无需发货，自动进入 → PENDING_PICKUP（待自提）
            └─ 买家确认收货 → COMPLETED（已完成）
```

**自提方式说明（参考咸鱼）：**
- 自提订单**无需发货**操作
- 买家付款后，订单状态自动变为"待自提"（PENDING_PICKUP）
- 双方通过聊天约定自提地点和时间
- 买家自提后，直接确认收货即可完成订单
- 卖家无需进行"发货"或"确认自提"操作

**订单状态说明：**
- `PENDING_PAYMENT`：待付款（买家已创建订单，未付款）
- `PAID`：已付款（买家已付款）
  - 邮寄方式：等待卖家发货
  - 自提方式：自动进入待自提状态（无需发货）
- `SHIPPED`：已发货（仅邮寄方式，卖家已发货，等待买家确认收货）
- `PENDING_PICKUP`：待自提（仅自提方式，买家已付款，等待买家自提并确认收货）
- `COMPLETED`：已完成（买家已确认收货）
- `CANCELLED`：已取消（买家或卖家取消订单）
- `REFUNDED`：已退款（订单退款）

#### 2.3.9 收货地址管理功能

**功能说明：**
- 用户需要管理自己的收货地址（邮寄方式下单时使用）
- 支持添加、编辑、删除、设置默认地址
- 最多保存10个收货地址

**收货地址字段：**
1. 收货人姓名（必填，最多20字）
2. 收货人电话（必填，手机号格式验证）
3. 所在地区（必填，省市区三级联动）
4. 详细地址（必填，最多200字）
5. 邮政编码（可选，6位数字）
6. 是否默认地址（单选，只能有一个默认地址）

**API接口：**
```java
@RestController
@RequestMapping("/api/address")
public class AddressController {
    
    /**
     * 获取当前用户的收货地址列表
     */
    @GetMapping("/list")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<List<Address>> getAddressList() {
        Long userId = SecurityUtils.getCurrentUserId();
        List<Address> list = addressService.getAddressList(userId);
        return Result.success("查询成功", list);
    }
    
    /**
     * 添加收货地址
     */
    @PostMapping("/add")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Long> addAddress(@RequestBody @Validated AddressDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long addressId = addressService.addAddress(dto, userId);
        return Result.success("添加成功", addressId);
    }
    
    /**
     * 更新收货地址
     */
    @PutMapping("/{id}")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Void> updateAddress(@PathVariable Long id, @RequestBody @Validated AddressDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        addressService.updateAddress(id, dto, userId);
        return Result.success("更新成功", null);
    }
    
    /**
     * 删除收货地址
     */
    @DeleteMapping("/{id}")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Void> deleteAddress(@PathVariable Long id) {
        Long userId = SecurityUtils.getCurrentUserId();
        addressService.deleteAddress(id, userId);
        return Result.success("删除成功", null);
    }
    
    /**
     * 设置默认地址
     */
    @PutMapping("/{id}/default")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Void> setDefaultAddress(@PathVariable Long id) {
        Long userId = SecurityUtils.getCurrentUserId();
        addressService.setDefaultAddress(id, userId);
        return Result.success("设置成功", null);
    }
}
```

**Service实现：**
```java
@Service
public class AddressService {
    
    /**
     * 添加收货地址
     */
    @Transactional(rollbackFor = Exception.class)
    public Long addAddress(AddressDTO dto, Long userId) {
        // 1. 检查地址数量限制（最多10个）
        long count = addressMapper.selectCount(
            new LambdaQueryWrapper<Address>()
                .eq(Address::getUserId, userId)
                .eq(Address::getDeleteFlag, 0)
        );
        if (count >= 10) {
            throw new BusinessException("最多只能保存10个收货地址");
        }
        
        // 2. 如果设置为默认地址，取消其他默认地址
        if (dto.getIsDefault() != null && dto.getIsDefault() == 1) {
            addressMapper.update(null, 
                new LambdaUpdateWrapper<Address>()
                    .eq(Address::getUserId, userId)
                    .eq(Address::getIsDefault, 1)
                    .set(Address::getIsDefault, 0)
            );
        }
        
        // 3. 创建地址
        Address address = new Address();
        address.setUserId(userId);
        address.setReceiverName(dto.getReceiverName());
        address.setReceiverPhone(dto.getReceiverPhone());
        address.setProvince(dto.getProvince());
        address.setCity(dto.getCity());
        address.setDistrict(dto.getDistrict());
        address.setDetailAddress(dto.getDetailAddress());
        address.setPostalCode(dto.getPostalCode());
        // 拼接完整地址（省+市+区+详细地址）
        address.setFullAddress(dto.getProvince() + dto.getCity() + dto.getDistrict() + dto.getDetailAddress());
        address.setIsDefault(dto.getIsDefault() != null ? dto.getIsDefault() : 0);
        address.setDeleteFlag(0);
        address.setCreateTime(new Date());
        address.setUpdateTime(new Date());
        
        addressMapper.insert(address);
        return address.getId();
    }
    
    /**
     * 更新收货地址
     */
    @Transactional(rollbackFor = Exception.class)
    public void updateAddress(Long addressId, AddressDTO dto, Long userId) {
        Address address = addressMapper.selectById(addressId);
        if (address == null || !address.getUserId().equals(userId)) {
            throw new BusinessException("地址不存在或无权操作");
        }
        
        // 如果设置为默认地址，取消其他默认地址
        if (dto.getIsDefault() != null && dto.getIsDefault() == 1) {
            addressMapper.update(null, 
                new LambdaUpdateWrapper<Address>()
                    .eq(Address::getUserId, userId)
                    .eq(Address::getIsDefault, 1)
                    .set(Address::getIsDefault, 0)
            );
        }
        
        // 更新地址信息
        address.setReceiverName(dto.getReceiverName());
        address.setReceiverPhone(dto.getReceiverPhone());
        address.setProvince(dto.getProvince());
        address.setCity(dto.getCity());
        address.setDistrict(dto.getDistrict());
        address.setDetailAddress(dto.getDetailAddress());
        address.setPostalCode(dto.getPostalCode());
        // 更新完整地址
        address.setFullAddress(dto.getProvince() + dto.getCity() + dto.getDistrict() + dto.getDetailAddress());
        address.setIsDefault(dto.getIsDefault() != null ? dto.getIsDefault() : address.getIsDefault());
        address.setUpdateTime(new Date());
        
        addressMapper.updateById(address);
    }
    
    /**
     * 删除收货地址
     */
    @Transactional(rollbackFor = Exception.class)
    public void deleteAddress(Long addressId, Long userId) {
        Address address = addressMapper.selectById(addressId);
        if (address == null || !address.getUserId().equals(userId)) {
            throw new BusinessException("地址不存在或无权操作");
        }
        
        // 逻辑删除
        address.setDeleteFlag(1);
        address.setUpdateTime(new Date());
        addressMapper.updateById(address);
    }
    
    /**
     * 设置默认地址
     */
    @Transactional(rollbackFor = Exception.class)
    public void setDefaultAddress(Long addressId, Long userId) {
        Address address = addressMapper.selectById(addressId);
        if (address == null || !address.getUserId().equals(userId)) {
            throw new BusinessException("地址不存在或无权操作");
        }
        
        // 取消其他默认地址
        addressMapper.update(null, 
            new LambdaUpdateWrapper<Address>()
                .eq(Address::getUserId, userId)
                .eq(Address::getIsDefault, 1)
                .set(Address::getIsDefault, 0)
        );
        
        // 设置当前地址为默认
        address.setIsDefault(1);
        address.setUpdateTime(new Date());
        addressMapper.updateById(address);
    }
}
```

#### 2.3.10 修改商品交易方式功能

**功能说明：**
- 卖家可以修改商品的交易方式（邮寄/自提）
- 如果商品已有订单，修改交易方式不影响已存在的订单
- 新订单使用新的交易方式

**API接口：**
```java
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 修改商品交易方式
     */
    @PutMapping("/{id}/trade-method")
    @RequireVerification
    public Result<Void> updateTradeMethod(
            @PathVariable Long id,
            @RequestBody @Validated TradeMethodDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        goodsService.updateTradeMethod(id, dto, userId);
        return Result.success("修改成功", null);
    }
}
```

**Service实现：**
```java
@Service
public class GoodsService {
    
    /**
     * 修改商品交易方式
     */
    @Transactional(rollbackFor = Exception.class)
    public void updateTradeMethod(Long goodsId, TradeMethodDTO dto, Long userId) {
        Goods goods = goodsMapper.selectById(goodsId);
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        if (!goods.getUserId().equals(userId)) {
            throw new BusinessException("无权修改此商品");
        }
        
        // 验证交易方式
        if (!"MAIL".equals(dto.getTradeMethod()) && !"FACE_TO_FACE".equals(dto.getTradeMethod())) {
            throw new BusinessException("交易方式只能是邮寄或自提");
        }
        
        // 更新交易方式
        goods.setTradeMethod(dto.getTradeMethod());
        
        // 如果是邮寄，更新邮寄费用
        if ("MAIL".equals(dto.getTradeMethod())) {
            goods.setShippingFee(dto.getShippingFee() != null ? dto.getShippingFee() : BigDecimal.ZERO);
            goods.setTradeLocation(null); // 清空自提地点
        }
        
        // 如果是自提，更新自提地点
        if ("FACE_TO_FACE".equals(dto.getTradeMethod())) {
            if (dto.getTradeLocation() == null || dto.getTradeLocation().trim().isEmpty()) {
                throw new BusinessException("自提方式必须填写自提地点");
            }
            goods.setTradeLocation(dto.getTradeLocation().trim());
            goods.setShippingFee(BigDecimal.ZERO); // 清空邮寄费用
        }
        
        goods.setUpdateTime(new Date());
        goodsMapper.updateById(goods);
        
        // 发送系统消息给所有已下单的买家（可选，提示交易方式变更）
        // 注意：已存在的订单不受影响，只是提示新订单会使用新的交易方式
    }
}
```

#### 2.3.11 我的交易页面

**功能说明：**
- 用户查看所有交易订单的页面
- 支持查看作为买家的订单和作为卖家的订单
- 支持订单筛选、搜索、详情查看和操作

**页面路径：** `/order/list` 或 `/user/orders`

**页面功能：**

**1. 订单列表展示：**
- 使用标签页（Tabs）区分"我买到的"和"我卖出的"
- 每个订单卡片显示：
  - 订单号（可点击查看详情）
  - 商品信息（缩略图、标题、价格、数量）
  - 交易方式（邮寄/自提）
  - 订单状态（带状态标签和颜色）
  - 订单时间（创建时间、付款时间、完成时间等）
  - 订单金额（总金额、邮寄费用）
  - 对方信息（买家/卖家头像、昵称）
  - 快捷操作按钮（根据订单状态和用户角色显示）

**2. 订单筛选：**
- 状态筛选（全部、待付款、已付款、已发货、待自提、已完成、已取消）
- 时间筛选（全部、今天、近7天、近30天、自定义时间范围）
- 交易方式筛选（全部、邮寄、自提）
- 角色筛选（我买到的、我卖出的）

**3. 订单搜索：**
- 支持按订单号搜索
- 支持按商品标题搜索
- 支持按对方昵称搜索

**4. 订单详情：**
- 点击订单卡片或订单号进入订单详情页
- 显示完整的订单信息：
  - 订单基本信息（订单号、状态、创建时间等）
  - 商品详情（图片、标题、描述、价格、数量）
  - 交易信息（交易方式、邮寄信息/自提信息）
  - 物流信息（邮寄时：快递单号、物流公司、发货时间）
  - 订单金额明细（商品金额、邮寄费用、总金额）
  - 对方信息（头像、昵称、联系方式）
  - 订单操作按钮（付款、发货、确认收货、取消等）

**5. 订单操作：**
- **作为买家：**
  - 待付款：付款、取消订单
  - 已付款（邮寄）：等待卖家发货
  - 已发货：查看物流、确认收货
  - 待自提：查看自提地点、确认收货
  - 已完成：查看订单详情、评价（可选）
  - 已取消：查看取消原因
- **作为卖家：**
  - 待付款：改价、等待买家付款
  - 已付款（邮寄）：发货（填写快递单号）
  - 已发货：等待买家确认收货
  - 待自提：等待买家自提并确认收货
  - 已完成：查看订单详情、评价（可选）
  - 已取消：查看取消原因

**6. 订单统计：**
- 显示订单数量统计（各状态订单数量）
- 显示交易金额统计（总交易额、本月交易额等）

**API接口：**
```java
@RestController
@RequestMapping("/api/order")
public class OrderController {
    
    /**
     * 获取我的订单列表（分页）
     * @param role 角色：BUYER-我买到的，SELLER-我卖出的，ALL-全部
     * @param status 订单状态筛选
     * @param tradeMethod 交易方式筛选
     * @param startTime 开始时间
     * @param endTime 结束时间
     * @param keyword 关键词（订单号、商品标题、对方昵称）
     * @param pageNum 页码
     * @param pageSize 每页数量
     */
    @GetMapping("/list")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Page<Order>> getMyOrderList(
            @RequestParam(required = false, defaultValue = "ALL") String role,
            @RequestParam(required = false) String status,
            @RequestParam(required = false) String tradeMethod,
            @RequestParam(required = false) String startTime,
            @RequestParam(required = false) String endTime,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        Long userId = SecurityUtils.getCurrentUserId();
        Page<Order> page = orderService.getMyOrderList(userId, role, status, tradeMethod, 
                startTime, endTime, keyword, pageNum, pageSize);
        return Result.success("查询成功", page);
    }
    
    /**
     * 获取订单详情
     */
    @GetMapping("/{id}")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<OrderDetailVO> getOrderDetail(@PathVariable Long id) {
        Long userId = SecurityUtils.getCurrentUserId();
        OrderDetailVO detail = orderService.getOrderDetail(id, userId);
        return Result.success("查询成功", detail);
    }
    
    /**
     * 获取订单统计信息
     */
    @GetMapping("/statistics")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<OrderStatisticsVO> getOrderStatistics() {
        Long userId = SecurityUtils.getCurrentUserId();
        OrderStatisticsVO statistics = orderService.getOrderStatistics(userId);
        return Result.success("查询成功", statistics);
    }
}
```

**Service实现：**
```java
@Service
public class OrderService {
    
    /**
     * 获取我的订单列表
     */
    public Page<Order> getMyOrderList(Long userId, String role, String status, 
            String tradeMethod, String startTime, String endTime, String keyword,
            Integer pageNum, Integer pageSize) {
        
        Page<Order> page = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<Order> wrapper = new LambdaQueryWrapper<>();
        
        // 根据角色筛选
        if ("BUYER".equals(role)) {
            wrapper.eq(Order::getBuyerId, userId);
        } else if ("SELLER".equals(role)) {
            wrapper.eq(Order::getSellerId, userId);
        } else {
            // ALL：查询作为买家或卖家的订单
            wrapper.and(w -> w.eq(Order::getBuyerId, userId)
                             .or()
                             .eq(Order::getSellerId, userId));
        }
        
        // 状态筛选
        if (StringUtils.hasText(status)) {
            wrapper.eq(Order::getStatus, status);
        }
        
        // 交易方式筛选
        if (StringUtils.hasText(tradeMethod)) {
            wrapper.eq(Order::getTradeMethod, tradeMethod);
        }
        
        // 时间范围筛选
        if (StringUtils.hasText(startTime)) {
            wrapper.ge(Order::getCreateTime, LocalDateTime.parse(startTime));
        }
        if (StringUtils.hasText(endTime)) {
            wrapper.le(Order::getCreateTime, LocalDateTime.parse(endTime));
        }
        
        // 关键词搜索（订单号、商品标题）
        if (StringUtils.hasText(keyword)) {
            wrapper.and(w -> w.like(Order::getOrderNo, keyword)
                             .or()
                             .exists("SELECT 1 FROM goods g WHERE g.id = order.goods_id AND g.title LIKE {0}", 
                                     "%" + keyword + "%"));
        }
        
        // 逻辑删除过滤
        wrapper.eq(Order::getDeleteFlag, 0);
        
        // 按创建时间倒序
        wrapper.orderByDesc(Order::getCreateTime);
        
        return orderMapper.selectPage(page, wrapper);
    }
    
    /**
     * 获取订单详情（包含商品信息和对方信息）
     */
    public OrderDetailVO getOrderDetail(Long orderId, Long userId) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        // 验证权限（只有买家或卖家可以查看）
        if (!order.getBuyerId().equals(userId) && !order.getSellerId().equals(userId)) {
            throw new BusinessException("无权查看此订单");
        }
        
        // 查询商品信息
        Goods goods = goodsMapper.selectById(order.getGoodsId());
        
        // 查询对方信息
        User otherUser = null;
        String userRole = null;
        if (order.getBuyerId().equals(userId)) {
            otherUser = userMapper.selectById(order.getSellerId());
            userRole = "SELLER";
        } else {
            otherUser = userMapper.selectById(order.getBuyerId());
            userRole = "BUYER";
        }
        
        // 构建详情VO
        OrderDetailVO detail = new OrderDetailVO();
        detail.setOrder(order);
        detail.setGoods(goods);
        detail.setOtherUser(otherUser);
        detail.setUserRole(userRole);
        
        return detail;
    }
    
    /**
     * 获取订单统计信息
     */
    public OrderStatisticsVO getOrderStatistics(Long userId) {
        OrderStatisticsVO statistics = new OrderStatisticsVO();
        
        // 作为买家的订单统计
        statistics.setBuyerTotal(orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getBuyerId, userId)
                .eq(Order::getDeleteFlag, 0)
        ));
        
        statistics.setBuyerPendingPayment(orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getBuyerId, userId)
                .eq(Order::getStatus, "PENDING_PAYMENT")
                .eq(Order::getDeleteFlag, 0)
        ));
        
        statistics.setBuyerPendingReceipt(orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getBuyerId, userId)
                .in(Order::getStatus, Arrays.asList("SHIPPED", "PENDING_PICKUP"))
                .eq(Order::getDeleteFlag, 0)
        ));
        
        // 作为卖家的订单统计
        statistics.setSellerTotal(orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getSellerId, userId)
                .eq(Order::getDeleteFlag, 0)
        ));
        
        statistics.setSellerPendingShipment(orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getSellerId, userId)
                .eq(Order::getStatus, "PAID")
                .eq(Order::getTradeMethod, "MAIL")
                .eq(Order::getDeleteFlag, 0)
        ));
        
        // 交易金额统计（本月）
        LocalDateTime startOfMonth = LocalDateTime.now().withDayOfMonth(1).withHour(0).withMinute(0).withSecond(0);
        List<Order> completedOrders = orderMapper.selectList(
            new LambdaQueryWrapper<Order>()
                .and(w -> w.eq(Order::getBuyerId, userId).or().eq(Order::getSellerId, userId))
                .eq(Order::getStatus, "COMPLETED")
                .ge(Order::getCreateTime, startOfMonth)
                .eq(Order::getDeleteFlag, 0)
        );
        
        BigDecimal monthlyAmount = completedOrders.stream()
            .map(Order::getTotalAmount)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
        statistics.setMonthlyAmount(monthlyAmount);
        
        return statistics;
    }
}
```

**前端页面结构：**
```vue
<template>
  <div class="order-list-container">
    <!-- 页面标题 -->
    <h1 class="page-title">我的交易</h1>
    
    <!-- 订单统计卡片 -->
    <div class="statistics-cards">
      <el-card v-for="stat in statistics" :key="stat.label">
        <div class="stat-item">
          <div class="stat-value">{{ stat.value }}</div>
          <div class="stat-label">{{ stat.label }}</div>
        </div>
      </el-card>
    </div>
    
    <!-- 标签页：我买到的 / 我卖出的 -->
    <el-tabs v-model="activeTab" @tab-change="handleTabChange">
      <el-tab-pane label="我买到的" name="BUYER">
        <OrderList 
          :role="'BUYER'"
          :filters="filters"
          @order-click="handleOrderClick"
        />
      </el-tab-pane>
      <el-tab-pane label="我卖出的" name="SELLER">
        <OrderList 
          :role="'SELLER'"
          :filters="filters"
          @order-click="handleOrderClick"
        />
      </el-tab-pane>
    </el-tabs>
  </div>
</template>
```

#### 2.3.12 聊天页面集成订单功能

**方案选择：复用现有聊天组件**

**理由：**
1. 现有聊天组件功能完善（消息发送、图片上传、已读状态等）
2. 订单相关功能作为聊天页面的扩展模块，无需重新开发
3. 用户体验统一，订单和聊天在同一界面，方便沟通

**实现方案：**

**1. 聊天页面订单卡片显示：**
- 当会话类型为 `GOODS` 且存在关联订单时，在聊天窗口顶部显示订单信息卡片
- 订单卡片显示：
  - **订单号**（唯一标识，格式：GO+时间戳13位+用户ID后2位，示例：GO170406720000012）
  - 商品信息（缩略图、标题、价格、数量）
  - 订单状态（带状态标签）
  - 交易方式（邮寄/自提）
  - 邮寄信息（邮寄时显示：收货地址、快递单号、物流公司）
  - 自提信息（自提时显示：自提地点）
  - 订单操作按钮（根据订单状态和用户角色显示）
    - 买家：选择收货地址（邮寄时）、付款、确认收货、取消订单
    - 卖家：改价、发货（仅邮寄时，填写快递单号）
    - **注意：自提方式无需发货操作，买家付款后自动进入待自提状态**

**2. 订单操作集成：**
- 所有订单操作（改价、付款、发货、确认收货）在聊天页面的订单卡片中完成
- 操作后实时更新订单卡片状态
- 重要操作（如改价、发货）自动发送系统消息到聊天窗口

**3. 前端实现：**
```vue
<!-- Chat.vue 中增加订单卡片组件 -->
<template>
  <div class="chat-window">
    <!-- 订单信息卡片（仅在GOODS类型会话且存在订单时显示） -->
    <div v-if="currentOrder" class="order-card">
      <OrderCard 
        :order="currentOrder" 
        :is-buyer="isBuyer"
        @update-order="handleOrderUpdate"
      />
    </div>
    
    <!-- 原有的聊天消息列表 -->
    <div class="messages-container">...</div>
    
    <!-- 原有的输入框 -->
    <div class="chat-input-area">...</div>
  </div>
</template>
```

**4. 后端支持：**
- `ChatSession` 增加 `relatedId` 字段（关联商品ID）
- `Order` 表增加 `sessionId` 字段（关联会话ID）
- 创建订单时，自动创建或关联聊天会话
- 订单状态变更时，通过WebSocket推送更新到聊天页面

**订单创建实现（防止超卖）：**
```java
@Service
public class OrderService {
    
    /**
     * 创建订单（使用乐观锁+库存检查防止超卖）
     */
    @Transactional(rollbackFor = Exception.class)
    public Long createOrder(OrderDTO dto, Long userId) {
        // 1. 验证实名认证
        User user = userMapper.selectById(userId);
        if (user.getIsVerified() != 1) {
            throw new BusinessException("该功能需要实名认证，请先完成实名认证");
        }
        
        // 2. 验证购买数量
        if (dto.getQuantity() == null || dto.getQuantity() <= 0) {
            throw new BusinessException("购买数量必须大于0");
        }
        
        // 3. 查询商品（使用悲观锁，防止并发）
        Goods goods = goodsMapper.selectByIdForUpdate(dto.getGoodsId());
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        // 4. 验证商品状态
        if (!"ON_SALE".equals(goods.getStatus())) {
            throw new BusinessException("商品已下架或已售出");
        }
        
        // 5. 不能购买自己的商品
        if (goods.getUserId().equals(userId)) {
            throw new BusinessException("不能购买自己的商品");
        }
        
        // 6. 检查库存（防止超卖）
        if (goods.getStock() < dto.getQuantity()) {
            throw new BusinessException("库存不足，当前库存：" + goods.getStock() + "件");
        }
        
        // 7. 使用乐观锁扣减库存
        int rows = goodsMapper.decreaseStockWithVersion(
            dto.getGoodsId(),
            dto.getQuantity(),
            goods.getVersion()
        );
        
        if (rows == 0) {
            throw new BusinessException("库存不足或商品已被其他买家购买，请刷新后重试");
        }
        
        // 8. 如果库存为0，更新商品状态为已售完
        Goods updatedGoods = goodsMapper.selectById(dto.getGoodsId());
        if (updatedGoods.getStock() == 0) {
            goodsMapper.updateStatus(dto.getGoodsId(), "SOLD_OUT");
        }
        
        // 9. 生成唯一订单号
        String orderNo = generateOrderNo();
        
        // 10. 创建订单
        Order order = new Order();
        order.setOrderNo(orderNo); // 唯一订单号
        order.setGoodsId(dto.getGoodsId());
        order.setBuyerId(userId);
        order.setSellerId(goods.getUserId());
        order.setQuantity(dto.getQuantity());
        order.setPrice(goods.getCurrentPrice());
        order.setTotalAmount(goods.getCurrentPrice().multiply(new BigDecimal(dto.getQuantity())));
        order.setTradeMethod(dto.getTradeMethod());
        
        // 邮寄方式需要选择收货地址
        if ("MAIL".equals(dto.getTradeMethod())) {
            // 验证收货地址ID
            if (dto.getAddressId() == null) {
                throw new BusinessException("邮寄方式必须选择收货地址");
            }
            
            // 查询收货地址
            Address address = addressMapper.selectById(dto.getAddressId());
            if (address == null || !address.getUserId().equals(userId)) {
                throw new BusinessException("收货地址不存在或无权使用");
            }
            
            order.setReceiverName(address.getReceiverName());
            order.setReceiverPhone(address.getReceiverPhone());
            order.setReceiverAddress(address.getFullAddress());
            order.setShippingFee(goods.getShippingFee() != null ? goods.getShippingFee() : BigDecimal.ZERO);
        }
        
        // 自提方式使用商品的自提地点
        if ("FACE_TO_FACE".equals(dto.getTradeMethod())) {
            order.setPickupLocation(goods.getTradeLocation());
        }
        
        // 设置初始订单状态
        order.setStatus("PENDING_PAYMENT");
        order.setCreateTime(new Date());
        orderMapper.insert(order);
        
        // 11. 创建或获取私信会话
        ChatSession session = chatSessionService.getOrCreateSession(
            userId, 
            goods.getUserId(), 
            "GOODS", 
            goods.getId()
        );
        
        // 12. 关联订单和会话
        order.setSessionId(session.getId());
        orderMapper.updateById(order);
        
        // 13. 发送通知给卖家
        systemMessageService.sendMessage(
            goods.getUserId(),
            "ORDER_CREATED",
            "收到新订单",
            "您的商品《" + goods.getTitle() + "》被购买了，订单号：" + orderNo,
            "ORDER",
            order.getId()
        );
        
        return order.getId();
    }
    
    /**
     * 生成唯一订单号（使用时间戳）
     * 格式：GO + 时间戳（13位毫秒） + 用户ID后2位
     * 示例：GO170406720000012（GO + 1704067200000 + 12）
     * 
     * 优势：
     * 1. 时间戳天然唯一，不需要检查重复
     * 2. 可以反映订单创建时间
     * 3. 简洁高效，无需重试机制
     * 4. 用户ID后缀便于识别和调试
     */
    private String generateOrderNo() {
        // 1. 获取当前时间戳（13位毫秒时间戳）
        long timestamp = System.currentTimeMillis();
        
        // 2. 获取当前用户ID的后2位（如果用户ID不足2位，前面补0）
        Long userId = SecurityUtils.getCurrentUserId();
        String userIdSuffix = String.format("%02d", userId % 100);
        
        // 3. 组合订单号：GO + 时间戳 + 用户ID后缀
        String orderNo = "GO" + timestamp + userIdSuffix;
        
        return orderNo;
    }
}
```

**订单状态管理实现：**

```java
@Service
public class OrderService {
    
    /**
     * 卖家改价
     */
    @Transactional(rollbackFor = Exception.class)
    public void updateOrderPrice(Long orderId, BigDecimal newPrice, Long sellerId) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        if (!order.getSellerId().equals(sellerId)) {
            throw new BusinessException("无权修改此订单");
        }
        
        if (!"PENDING_PAYMENT".equals(order.getStatus())) {
            throw new BusinessException("只有待付款订单才能改价");
        }
        
        if (newPrice.compareTo(BigDecimal.ZERO) <= 0) {
            throw new BusinessException("价格必须大于0");
        }
        
        // 更新订单价格和总金额
        order.setPrice(newPrice);
        order.setTotalAmount(newPrice.multiply(new BigDecimal(order.getQuantity())));
        order.setUpdateTime(new Date());
        orderMapper.updateById(order);
        
        // 发送通知给买家
        systemMessageService.sendMessage(
            order.getBuyerId(),
            "ORDER_PRICE_UPDATED",
            "订单价格已修改",
            "订单号：" + order.getOrderNo() + " 的价格已修改为：" + newPrice + "元",
            "ORDER",
            orderId
        );
    }
    
    /**
     * 买家付款
     */
    @Transactional(rollbackFor = Exception.class)
    public void payOrder(Long orderId, Long buyerId) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        if (!order.getBuyerId().equals(buyerId)) {
            throw new BusinessException("无权操作此订单");
        }
        
        if (!"PENDING_PAYMENT".equals(order.getStatus())) {
            throw new BusinessException("订单状态不正确，无法付款");
        }
        
        // 根据交易方式设置订单状态
        // 邮寄方式：付款后进入"已付款"状态，等待卖家发货
        // 自提方式：付款后直接进入"待自提"状态，无需发货
        if ("MAIL".equals(order.getTradeMethod())) {
            order.setStatus("PAID");
            // 发送通知给卖家（邮寄方式需要发货）
            systemMessageService.sendMessage(
                order.getSellerId(),
                "ORDER_PAID",
                "订单已付款",
                "订单号：" + order.getOrderNo() + " 已付款，请及时发货",
                "ORDER",
                orderId
            );
        } else if ("FACE_TO_FACE".equals(order.getTradeMethod())) {
            // 自提方式：付款后直接进入待自提状态（无需发货）
            order.setStatus("PENDING_PICKUP");
            // 发送通知给卖家（自提方式无需发货）
            systemMessageService.sendMessage(
                order.getSellerId(),
                "ORDER_PAID",
                "订单已付款",
                "订单号：" + order.getOrderNo() + " 已付款（自提方式，无需发货），请等待买家自提",
                "ORDER",
                orderId
            );
        }
        
        order.setPayTime(new Date());
        order.setUpdateTime(new Date());
        orderMapper.updateById(order);
    }
    
    /**
     * 卖家发货（邮寄方式）
     */
    @Transactional(rollbackFor = Exception.class)
    public void shipOrder(Long orderId, String trackingNumber, String logisticsCompany, Long sellerId) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        if (!order.getSellerId().equals(sellerId)) {
            throw new BusinessException("无权操作此订单");
        }
        
        if (!"PAID".equals(order.getStatus())) {
            throw new BusinessException("订单状态不正确，无法发货");
        }
        
        if (!"MAIL".equals(order.getTradeMethod())) {
            throw new BusinessException("该订单不是邮寄方式，无法发货");
        }
        
        // 验证快递单号
        if (trackingNumber == null || trackingNumber.trim().isEmpty()) {
            throw new BusinessException("快递单号不能为空");
        }
        
        // 更新订单状态和物流信息
        order.setStatus("SHIPPED");
        order.setTrackingNumber(trackingNumber.trim());
        order.setLogisticsCompany(logisticsCompany != null ? logisticsCompany.trim() : null);
        order.setShipTime(new Date());
        order.setUpdateTime(new Date());
        orderMapper.updateById(order);
        
        // 发送通知给买家
        systemMessageService.sendMessage(
            order.getBuyerId(),
            "ORDER_SHIPPED",
            "订单已发货",
            "订单号：" + order.getOrderNo() + " 已发货，快递单号：" + trackingNumber,
            "ORDER",
            orderId
        );
    }
    
    /**
     * 买家确认收货
     * 支持邮寄和自提两种方式
     */
    @Transactional(rollbackFor = Exception.class)
    public void confirmReceipt(Long orderId, Long buyerId) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        if (!order.getBuyerId().equals(buyerId)) {
            throw new BusinessException("无权操作此订单");
        }
        
        // 邮寄方式：需要已发货状态
        // 自提方式：需要待自提状态（付款后自动进入）
        if ("MAIL".equals(order.getTradeMethod())) {
            if (!"SHIPPED".equals(order.getStatus())) {
                throw new BusinessException("订单状态不正确，无法确认收货（邮寄方式需要已发货）");
            }
        } else if ("FACE_TO_FACE".equals(order.getTradeMethod())) {
            if (!"PENDING_PICKUP".equals(order.getStatus())) {
                throw new BusinessException("订单状态不正确，无法确认收货（自提方式需要待自提状态）");
            }
        } else {
            throw new BusinessException("订单交易方式不正确");
        }
        
        // 更新订单状态
        order.setStatus("COMPLETED");
        order.setCompleteTime(new Date());
        order.setUpdateTime(new Date());
        orderMapper.updateById(order);
        
        // 发送通知给卖家
        systemMessageService.sendMessage(
            order.getSellerId(),
            "ORDER_COMPLETED",
            "订单已完成",
            "订单号：" + order.getOrderNo() + " 买家已确认收货，订单完成",
            "ORDER",
            orderId
        );
    }
    
    /**
     * 取消订单
     */
    @Transactional(rollbackFor = Exception.class)
    public void cancelOrder(Long orderId, Long userId, String reason) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new BusinessException("订单不存在");
        }
        
        // 只有买家或卖家可以取消订单
        if (!order.getBuyerId().equals(userId) && !order.getSellerId().equals(userId)) {
            throw new BusinessException("无权操作此订单");
        }
        
        // 只有待付款订单可以取消
        if (!"PENDING_PAYMENT".equals(order.getStatus())) {
            throw new BusinessException("只有待付款订单可以取消");
        }
        
        // 恢复库存
        Goods goods = goodsMapper.selectById(order.getGoodsId());
        goodsMapper.increaseStock(order.getGoodsId(), order.getQuantity());
        
        // 如果商品状态是已售完，恢复为在售
        if ("SOLD_OUT".equals(goods.getStatus())) {
            goodsMapper.updateStatus(order.getGoodsId(), "ON_SALE");
        }
        
        // 更新订单状态
        order.setStatus("CANCELLED");
        order.setCancelReason(reason);
        order.setCancelTime(new Date());
        order.setUpdateTime(new Date());
        orderMapper.updateById(order);
        
        // 发送通知给对方
        Long otherUserId = order.getBuyerId().equals(userId) ? order.getSellerId() : order.getBuyerId();
        systemMessageService.sendMessage(
            otherUserId,
            "ORDER_CANCELLED",
            "订单已取消",
            "订单号：" + order.getOrderNo() + " 已取消，原因：" + reason,
            "ORDER",
            orderId
        );
    }
}
```

**MyBatis Mapper 方法（防止超卖和其他操作）：**
```java
@Mapper
public interface GoodsMapper extends BaseMapper<Goods> {
    
    /**
     * 使用悲观锁查询商品（防止并发）
     */
    @Select("SELECT * FROM goods WHERE id = #{id} FOR UPDATE")
    Goods selectByIdForUpdate(Long id);
    
    /**
     * 使用乐观锁扣减库存
     */
    @Update("UPDATE goods SET stock = stock - #{quantity}, version = version + 1 " +
            "WHERE id = #{goodsId} AND stock >= #{quantity} AND version = #{version}")
    int decreaseStockWithVersion(@Param("goodsId") Long goodsId, 
                                 @Param("quantity") Integer quantity, 
                                 @Param("version") Integer version);
    
    /**
     * 增加库存（取消订单时恢复库存）
     */
    @Update("UPDATE goods SET stock = stock + #{quantity} WHERE id = #{goodsId}")
    int increaseStock(@Param("goodsId") Long goodsId, @Param("quantity") Integer quantity);
    
    /**
     * 更新商品状态
     */
    @Update("UPDATE goods SET status = #{status} WHERE id = #{id}")
    int updateStatus(@Param("id") Long id, @Param("status") String status);
    
    /**
     * 增加浏览量（查看详情时）
     */
    @Update("UPDATE goods SET view_count = view_count + 1 WHERE id = #{id}")
    int incrementViewCount(@Param("id") Long id);
}

@Mapper
public interface OrderMapper extends BaseMapper<Order> {
    
    /**
     * 根据订单号查询订单
     */
    @Select("SELECT * FROM `order` WHERE order_no = #{orderNo} AND delete_flag = 0")
    Order selectByOrderNo(String orderNo);
}
```

---

### 2.4 学习互助模块

#### 2.4.1 问题发布（无需实名认证）

**权限说明：**
- ✅ **所有登录用户都可以发布学习问题**（无需实名认证）
- ✅ **所有登录用户都可以回答问题**（无需实名认证）
- 未登录用户需要先登录

**问题分类：**
- 数学（MATH）
- 物理（PHYSICS）
- 化学（CHEMISTRY）
- 生物（BIOLOGY）
- 计算机（COMPUTER）
- 英语（ENGLISH）
- 文学（LITERATURE）
- 历史（HISTORY）
- 哲学（PHILOSOPHY）
- 经济（ECONOMICS）
- 管理（MANAGEMENT）
- 法律（LAW）
- 教育（EDUCATION）
- 艺术（ART）
- 工程（ENGINEERING）
- 医学（MEDICINE）
- 农学（AGRICULTURE）
- 其他（OTHER）

**发布流程：**
```
用户填写问题信息
    ↓
前端验证：
    - 问题分类必填
    - 问题标题必填，长度1-200字符
    - 问题描述必填，长度10-2000字符
    - 悬赏金额可选，如果填写范围0.01-9999.99元
    - 图片格式和大小检查
    ↓
上传图片到服务器
    ↓
提交发布请求
    ↓
后端验证：
    - 数据格式验证（@Validated）
    - 敏感词检测
    - 发布频率检测（1小时内最多5条）
    - 新用户检测
    ↓
判断审核结果：
    ├─ 自动审核通过 → 状态设置为"待解答"，立即发布
    └─ 转人工审核 → 状态设置为"待审核"，进入审核队列
    ↓
保存到数据库
    ↓
返回发布成功
    ↓
跳转到详情页 / 我的问题页
```

**后端实现：**
```java
@RestController
@RequestMapping("/api/question")
public class QuestionController {
    
    /**
     * 发布问题（无需实名认证）
     */
    @PostMapping("/publish")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result publishQuestion(@RequestBody @Validated QuestionDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long questionId = questionService.publishQuestion(dto, userId);
        return Result.success(questionId);
    }
}

@Service
public class QuestionService {
    
    /**
     * 发布问题
     */
    @Transactional(rollbackFor = Exception.class)
    public Long publishQuestion(QuestionDTO dto, Long userId) {
        // 1. 验证表单数据
        if (StringUtils.isBlank(dto.getTitle()) || dto.getTitle().length() > 200) {
            throw new BusinessException("问题标题长度必须在1-200字符之间");
        }
        if (StringUtils.isBlank(dto.getDescription()) || dto.getDescription().length() < 10 || dto.getDescription().length() > 2000) {
            throw new BusinessException("问题描述长度必须在10-2000字符之间");
        }
        
        // 2. 验证悬赏金额（如果填写）
        if (dto.getReward() != null && dto.getReward().compareTo(BigDecimal.ZERO) > 0) {
            if (dto.getReward().compareTo(new BigDecimal("0.01")) < 0 || dto.getReward().compareTo(new BigDecimal("9999.99")) > 0) {
                throw new BusinessException("悬赏金额必须在0.01-9999.99元之间");
            }
        } else {
            dto.setReward(BigDecimal.ZERO); // 默认为0（无酬劳）
        }
        
        // 3. 敏感词检测
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(
            dto.getTitle() + " " + dto.getDescription()
        );
        
        // 4. 发布频率检测（1小时内最多5条）
        boolean frequencyOk = publishFrequencyService.checkFrequency(userId, "QUESTION", 5, 1);
        
        // 5. 用户信用检测（新注册用户7天内）
        User user = userMapper.selectById(userId);
        boolean newUser = Duration.between(
            user.getCreateTime().toInstant(), 
            Instant.now()
        ).toDays() < 7;
        
        // 6. 创建问题信息
        StudyQuestion question = new StudyQuestion();
        BeanUtils.copyProperties(dto, question);
        question.setUserId(userId);
        question.setImages(JSON.toJSONString(dto.getImages()));
        question.setReward(dto.getReward() != null ? dto.getReward() : BigDecimal.ZERO);
        question.setAnswerCount(0);
        
        // 7. 判断审核状态
        if (checkResult.isPass() && frequencyOk && !newUser) {
            // 自动审核通过
            question.setStatus("PENDING_ANSWER");
            question.setAuditStatus("APPROVED");
            question.setAuditTime(new Date());
        } else {
            // 转人工审核
            question.setStatus("PENDING_REVIEW");
            question.setAuditStatus("PENDING");
            
            // 记录触发原因
            StringBuilder reason = new StringBuilder();
            if (!checkResult.isPass()) {
                reason.append("包含敏感词：").append(String.join("、", checkResult.getMatchedWords())).append("；");
            }
            if (!frequencyOk) {
                reason.append("发布频繁；");
            }
            if (newUser) {
                reason.append("新注册用户；");
            }
            question.setAuditTriggerReason(reason.toString());
        }
        
        question.setCreateTime(new Date());
        question.setUpdateTime(new Date());
        
        // 8. 保存到数据库
        questionMapper.insert(question);
        
        // 9. 如果需要人工审核，发送通知给所有管理员
        if ("PENDING_REVIEW".equals(question.getStatus())) {
            systemMessageService.sendMessageToAllAdmins(
                "ADMIN_AUDIT_REQUIRED",
                "新的问题待审核",
                "有一条新的问题《" + question.getTitle() + "》需要审核，触发原因：" + question.getAuditTriggerReason(),
                "QUESTION",
                question.getId()
            );
        }
        
        return question.getId();
    }
}
```

#### 2.4.2 回答问题功能

**回答流程：**
```
用户浏览问题
    ↓
点击"回答"
    ↓
填写回答内容（文字+图片，可选）
    ↓
提交回答请求
    ↓
敏感词检测
    ↓
保存回答到数据库
    ↓
更新问题状态（如果之前是待解答，更新为已回答）
    ↓
更新问题回答数量
    ↓
发送系统消息通知发布者
```

**后端实现：**
```java
@Service
public class QuestionService {
    
    /**
     * 回答问题
     */
    @Transactional(rollbackFor = Exception.class)
    public Long answerQuestion(Long questionId, AnswerDTO dto, Long userId) {
        // 1. 查询问题信息
        StudyQuestion question = questionMapper.selectById(questionId);
        if (question == null) {
            throw new BusinessException("问题不存在");
        }
        
        // 2. 验证问题状态
        if (!"PENDING_ANSWER".equals(question.getStatus()) && !"ANSWERED".equals(question.getStatus())) {
            throw new BusinessException("该问题当前不可回答");
        }
        
        // 3. 不能回答自己的问题（可选，也可以允许）
        // if (question.getUserId().equals(userId)) {
        //     throw new BusinessException("不能回答自己的问题");
        // }
        
        // 4. 敏感词检测
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(dto.getContent());
        if (!checkResult.isPass()) {
            throw new BusinessException("回答内容包含敏感词，请修改后提交");
        }
        
        // 5. 创建回答
        StudyAnswer answer = new StudyAnswer();
        answer.setQuestionId(questionId);
        answer.setUserId(userId);
        answer.setContent(dto.getContent());
        answer.setImages(JSON.toJSONString(dto.getImages()));
        answer.setIsAccepted(0);
        answer.setLikeCount(0);
        answer.setCreateTime(new Date());
        answerMapper.insert(answer);
        
        // 6. 如果问题状态是待解答，更新为已回答
        if ("PENDING_ANSWER".equals(question.getStatus())) {
            question.setStatus("ANSWERED");
            question.setUpdateTime(new Date());
            questionMapper.updateById(question);
        }
        
        // 7. 更新问题回答数量
        questionMapper.incrementAnswerCount(questionId);
        
        // 8. 发送通知给发布者
        systemMessageService.sendMessage(
            question.getUserId(),
            "QUESTION_ANSWERED",
            "问题收到回答",
            "您发布的问题《" + question.getTitle() + "》收到了新的回答",
            "QUESTION",
            questionId
        );
        
        return answer.getId();
    }
}
```

#### 2.4.3 采纳答案功能

**采纳流程：**
```
发布者查看问题详情
    ↓
查看所有回答
    ↓
选择最佳答案
    ↓
点击"采纳答案"
    ↓
确认采纳对话框
    ↓
提交采纳请求
    ↓
更新问题状态为已解决
    ↓
标记答案为已采纳
    ↓
如果有酬劳，处理酬劳支付（模拟）
    ↓
发送系统消息通知被采纳的回答者
```

**后端实现：**
```java
@Service
public class QuestionService {
    
    /**
     * 采纳答案
     */
    @Transactional(rollbackFor = Exception.class)
    public void acceptAnswer(Long questionId, Long answerId, Long userId) {
        // 1. 查询问题信息
        StudyQuestion question = questionMapper.selectById(questionId);
        if (question == null) {
            throw new BusinessException("问题不存在");
        }
        
        // 2. 验证用户是发布者
        if (!question.getUserId().equals(userId)) {
            throw new BusinessException("只有发布者可以采纳答案");
        }
        
        // 3. 验证问题状态
        if (!"ANSWERED".equals(question.getStatus())) {
            throw new BusinessException("该问题当前不可采纳答案");
        }
        
        // 4. 查询回答信息
        StudyAnswer answer = answerMapper.selectById(answerId);
        if (answer == null) {
            throw new BusinessException("回答不存在");
        }
        
        // 5. 验证回答属于该问题
        if (!answer.getQuestionId().equals(questionId)) {
            throw new BusinessException("回答不属于该问题");
        }
        
        // 6. 更新问题状态
        question.setStatus("SOLVED");
        question.setAcceptedAnswerId(answerId);
        question.setAcceptTime(new Date());
        question.setUpdateTime(new Date());
        questionMapper.updateById(question);
        
        // 7. 标记答案为已采纳
        answer.setIsAccepted(1);
        answer.setAcceptTime(new Date());
        answerMapper.updateById(answer);
        
        // 8. 如果有酬劳，处理酬劳支付（模拟）
        if (question.getReward().compareTo(BigDecimal.ZERO) > 0) {
            // 模拟支付处理（实际可对接支付系统）
            // paymentService.transferReward(question.getUserId(), answer.getUserId(), question.getReward());
        }
        
        // 9. 发送通知给被采纳的回答者
        systemMessageService.sendMessage(
            answer.getUserId(),
            "ANSWER_ACCEPTED",
            "答案被采纳",
            "您回答的问题《" + question.getTitle() + "》的答案被采纳" + 
            (question.getReward().compareTo(BigDecimal.ZERO) > 0 ? "，获得酬劳：" + question.getReward() + "元" : ""),
            "QUESTION",
            questionId
        );
    }
}
```

#### 2.4.4 问题列表和搜索功能（详细实现）

**列表页面：** `/question/list`

**前端页面布局：**
```vue
<template>
  <div class="question-list-container">
    <!-- 搜索和筛选区域 -->
    <div class="filter-section">
      <el-input
        v-model="searchForm.keyword"
        placeholder="搜索问题..."
        clearable
        @keyup.enter="handleSearch"
      >
        <template #append>
          <el-button @click="handleSearch">搜索</el-button>
        </template>
      </el-input>
      
      <div class="filters">
        <el-select v-model="searchForm.category" placeholder="选择分类" clearable>
          <el-option label="全部" value="" />
          <el-option label="数学" value="MATH" />
          <el-option label="物理" value="PHYSICS" />
          <el-option label="化学" value="CHEMISTRY" />
          <el-option label="生物" value="BIOLOGY" />
          <el-option label="计算机" value="COMPUTER" />
          <el-option label="英语" value="ENGLISH" />
          <el-option label="文学" value="LITERATURE" />
          <el-option label="历史" value="HISTORY" />
          <el-option label="哲学" value="PHILOSOPHY" />
          <el-option label="经济" value="ECONOMICS" />
          <el-option label="管理" value="MANAGEMENT" />
          <el-option label="法律" value="LAW" />
          <el-option label="教育" value="EDUCATION" />
          <el-option label="艺术" value="ART" />
          <el-option label="工程" value="ENGINEERING" />
          <el-option label="医学" value="MEDICINE" />
          <el-option label="农学" value="AGRICULTURE" />
          <el-option label="其他" value="OTHER" />
        </el-select>
        
        <el-select v-model="searchForm.status" placeholder="选择状态" clearable>
          <el-option label="全部" value="" />
          <el-option label="待解答" value="PENDING_ANSWER" />
          <el-option label="已回答" value="ANSWERED" />
          <el-option label="已解决" value="SOLVED" />
        </el-select>
        
        <el-select v-model="searchForm.hasReward" placeholder="酬劳筛选" clearable>
          <el-option label="全部" value="" />
          <el-option label="有酬劳" :value="true" />
          <el-option label="无酬劳" :value="false" />
        </el-select>
        
        <el-select v-model="searchForm.sortBy" placeholder="排序方式">
          <el-option label="最新发布" value="latest" />
          <el-option label="酬劳最高" value="reward" />
          <el-option label="回答最多" value="popular" />
          <el-option label="最新回答" value="recent_answer" />
        </el-select>
      </div>
    </div>
    
    <!-- 问题列表 -->
    <div class="question-list">
      <el-card
        v-for="question in questionList"
        :key="question.id"
        class="question-card"
        @click="goToDetail(question.id)"
      >
        <div class="question-header">
          <el-tag :type="getCategoryType(question.category)">
            {{ getCategoryName(question.category) }}
          </el-tag>
          <el-tag :type="getStatusType(question.status)" size="small">
            {{ getStatusName(question.status) }}
          </el-tag>
        </div>
        
        <h3 class="question-title">{{ question.title }}</h3>
        
        <p class="question-description">
          {{ truncateText(question.description, 100) }}
        </p>
        
        <div class="question-footer">
          <div class="question-info">
            <span class="reward" v-if="question.reward > 0">
              <i class="el-icon-coin"></i> 悬赏 ¥{{ question.reward }}
            </span>
            <span class="answer-count">
              <i class="el-icon-chat-line-round"></i> {{ question.answerCount }} 个回答
            </span>
            <span class="view-count">
              <i class="el-icon-view"></i> {{ question.viewCount }} 次浏览
            </span>
          </div>
          
          <div class="question-user">
            <el-avatar :src="question.user.avatar" :size="24" />
            <span>{{ question.user.nickname }}</span>
            <span class="time">{{ formatTime(question.createTime) }}</span>
          </div>
        </div>
      </el-card>
    </div>
    
    <!-- 分页 -->
    <el-pagination
      v-model:current-page="pagination.pageNum"
      v-model:page-size="pagination.pageSize"
      :total="pagination.total"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handlePageChange"
    />
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { questionApi } from '@/api'

const router = useRouter()

const searchForm = ref({
  keyword: '',
  category: '',
  status: '',
  hasReward: '',
  sortBy: 'latest'
})

const questionList = ref([])
const pagination = ref({
  pageNum: 1,
  pageSize: 10,
  total: 0
})

// 加载问题列表
const loadQuestionList = async () => {
  try {
    const params = {
      ...searchForm.value,
      pageNum: pagination.value.pageNum,
      pageSize: pagination.value.pageSize
    }
    const response = await questionApi.getList(params)
    if (response.code === 200) {
      questionList.value = response.data.records
      pagination.value.total = response.data.total
    }
  } catch (error) {
    console.error('加载问题列表失败:', error)
  }
}

// 搜索
const handleSearch = () => {
  pagination.value.pageNum = 1
  loadQuestionList()
}

// 分页变化
const handlePageChange = () => {
  loadQuestionList()
}

const handleSizeChange = () => {
  pagination.value.pageNum = 1
  loadQuestionList()
}

// 跳转到详情页
const goToDetail = (id) => {
  router.push(`/question/detail/${id}`)
}

onMounted(() => {
  loadQuestionList()
})
</script>
```

**后端接口实现：**
```java
@RestController
@RequestMapping("/api/question")
public class QuestionController {
    
    @Autowired
    private QuestionService questionService;
    
    /**
     * 获取问题列表
     */
    @GetMapping("/list")
    public Result<PageResult<QuestionVO>> getQuestionList(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String status,
            @RequestParam(required = false) Boolean hasReward,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) String sortBy,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        
        QuestionSearchDTO dto = new QuestionSearchDTO();
        dto.setCategory(category);
        dto.setStatus(status);
        dto.setHasReward(hasReward);
        dto.setKeyword(keyword);
        dto.setSortBy(sortBy);
        dto.setPageNum(pageNum);
        dto.setPageSize(pageSize);
        
        PageResult<QuestionVO> result = questionService.getQuestionList(dto);
        return Result.success("查询成功", result);
    }
}

@Service
public class QuestionService {
    
    /**
     * 获取问题列表
     */
    public PageResult<QuestionVO> getQuestionList(QuestionSearchDTO dto) {
        Page<StudyQuestion> page = new Page<>(dto.getPageNum(), dto.getPageSize());
        LambdaQueryWrapper<StudyQuestion> wrapper = new LambdaQueryWrapper<>();
        
        // 关键词搜索（标题或描述）
        if (StringUtils.hasText(dto.getKeyword())) {
            wrapper.and(w -> w.like(StudyQuestion::getTitle, dto.getKeyword())
                            .or()
                            .like(StudyQuestion::getDescription, dto.getKeyword()));
        }
        
        // 分类筛选
        if (StringUtils.hasText(dto.getCategory())) {
            wrapper.eq(StudyQuestion::getCategory, dto.getCategory());
        }
        
        // 状态筛选（默认只显示待解答和已回答）
        if (StringUtils.hasText(dto.getStatus())) {
            wrapper.eq(StudyQuestion::getStatus, dto.getStatus());
        } else {
            wrapper.in(StudyQuestion::getStatus, "PENDING_ANSWER", "ANSWERED");
        }
        
        // 酬劳筛选
        if (dto.getHasReward() != null) {
            if (dto.getHasReward()) {
                wrapper.gt(StudyQuestion::getReward, 0);
            } else {
                wrapper.eq(StudyQuestion::getReward, 0);
            }
        }
        
        // 不显示已删除的
        wrapper.eq(StudyQuestion::getDeleteFlag, 0);
        
        // 排序
        if ("reward".equals(dto.getSortBy())) {
            wrapper.orderByDesc(StudyQuestion::getReward);
        } else if ("popular".equals(dto.getSortBy())) {
            wrapper.orderByDesc(StudyQuestion::getAnswerCount);
        } else if ("recent_answer".equals(dto.getSortBy())) {
            // 按最新回答时间排序（需要关联回答表）
            wrapper.orderByDesc(StudyQuestion::getUpdateTime);
        } else {
            wrapper.orderByDesc(StudyQuestion::getCreateTime);
        }
        
        // 查询
        Page<StudyQuestion> result = questionMapper.selectPage(page, wrapper);
        
        // 转换为VO
        List<QuestionVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 转换为VO
     */
    private QuestionVO convertToVO(StudyQuestion question) {
        QuestionVO vo = new QuestionVO();
        BeanUtils.copyProperties(question, vo);
        
        // 解析图片
        if (StringUtils.hasText(question.getImages())) {
            vo.setImageList(JSON.parseArray(question.getImages(), String.class));
        }
        
        // 获取发布者信息
        User user = userMapper.selectById(question.getUserId());
        vo.setUser(new UserVO(user));
        
        return vo;
    }
}
```

#### 2.4.5 问题详情页面（详细实现）

**详情页面：** `/question/detail/:id`

**前端页面实现：**
```vue
<template>
  <div class="question-detail-container">
    <!-- 问题信息 -->
    <el-card class="question-card">
      <div class="question-header">
        <el-tag :type="getCategoryType(question.category)">
          {{ getCategoryName(question.category) }}
        </el-tag>
        <el-tag :type="getStatusType(question.status)" size="small">
          {{ getStatusName(question.status) }}
        </el-tag>
        <span class="reward" v-if="question.reward > 0">
          <i class="el-icon-coin"></i> 悬赏 ¥{{ question.reward }}
        </span>
      </div>
      
      <h1 class="question-title">{{ question.title }}</h1>
      
      <div class="question-meta">
        <el-avatar :src="question.user.avatar" :size="32" />
        <span class="user-name">{{ question.user.nickname }}</span>
        <span class="time">{{ formatTime(question.createTime) }}</span>
        <span class="view-count">浏览 {{ question.viewCount }} 次</span>
      </div>
      
      <div class="question-content">
        <p>{{ question.description }}</p>
        
        <!-- 问题图片 -->
        <el-image
          v-for="(img, index) in question.imageList"
          :key="index"
          :src="img"
          :preview-src-list="question.imageList"
          fit="cover"
          class="question-image"
        />
      </div>
    </el-card>
    
    <!-- 回答列表 -->
    <el-card class="answers-card">
      <template #header>
        <div class="answers-header">
          <span>回答 ({{ question.answerCount }})</span>
        </div>
      </template>
      
      <div
        v-for="answer in answerList"
        :key="answer.id"
        class="answer-item"
        :class="{ 'accepted': answer.isAccepted }"
      >
        <div class="answer-header">
          <el-avatar :src="answer.user.avatar" :size="32" />
          <span class="user-name">{{ answer.user.nickname }}</span>
          <el-tag v-if="answer.isAccepted" type="success" size="small">已采纳</el-tag>
          <span class="time">{{ formatTime(answer.createTime) }}</span>
        </div>
        
        <div class="answer-content">
          <p>{{ answer.content }}</p>
          
          <!-- 回答图片 -->
          <el-image
            v-for="(img, index) in answer.imageList"
            :key="index"
            :src="img"
            :preview-src-list="answer.imageList"
            fit="cover"
            class="answer-image"
          />
        </div>
        
        <div class="answer-actions" v-if="isPublisher && question.status === 'ANSWERED'">
          <el-button
            type="primary"
            size="small"
            @click="handleAcceptAnswer(answer.id)"
            :disabled="answer.isAccepted"
          >
            {{ answer.isAccepted ? '已采纳' : '采纳答案' }}
          </el-button>
        </div>
      </div>
      
      <el-empty v-if="answerList.length === 0" description="暂无回答" />
    </el-card>
    
    <!-- 回答输入框 -->
    <el-card class="answer-input-card" v-if="question.status !== 'SOLVED'">
      <el-form :model="answerForm" :rules="answerRules" ref="answerFormRef">
        <el-form-item prop="content">
          <el-input
            v-model="answerForm.content"
            type="textarea"
            :rows="6"
            placeholder="写下你的回答..."
            maxlength="2000"
            show-word-limit
          />
        </el-form-item>
        
        <el-form-item>
          <el-upload
            :action="uploadUrl"
            :headers="uploadHeaders"
            :file-list="answerForm.images"
            :on-success="handleUploadSuccess"
            :before-upload="beforeUpload"
            :limit="3"
            list-type="picture-card"
          >
            <el-icon><Plus /></el-icon>
          </el-upload>
        </el-form-item>
        
        <el-form-item>
          <el-button type="primary" @click="submitAnswer" :loading="submitting">
            提交回答
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup>
import { ref, onMounted, computed } from 'vue'
import { useRoute } from 'vue-router'
import { questionApi } from '@/api'
import { useUserStore } from '@/stores/user'

const route = useRoute()
const userStore = useUserStore()

const question = ref({})
const answerList = ref([])
const answerForm = ref({
  content: '',
  images: []
})
const submitting = ref(false)

// 判断是否是发布者
const isPublisher = computed(() => {
  return question.value.userId === userStore.userInfo?.id
})

// 加载问题详情
const loadQuestionDetail = async () => {
  try {
    const questionId = route.params.id
    const response = await questionApi.getDetail(questionId)
    if (response.code === 200) {
      question.value = response.data.question
      answerList.value = response.data.answers || []
    }
  } catch (error) {
    console.error('加载问题详情失败:', error)
  }
}

// 提交回答
const submitAnswer = async () => {
  try {
    submitting.value = true
    const questionId = route.params.id
    const response = await questionApi.answerQuestion(questionId, {
      content: answerForm.value.content,
      images: answerForm.value.images.map(img => img.url)
    })
    
    if (response.code === 200) {
      ElMessage.success('回答成功')
      answerForm.value = { content: '', images: [] }
      loadQuestionDetail() // 重新加载
    }
  } catch (error) {
    console.error('提交回答失败:', error)
  } finally {
    submitting.value = false
  }
}

// 采纳答案
const handleAcceptAnswer = async (answerId) => {
  try {
    await ElMessageBox.confirm('确定采纳这个答案吗？', '提示', {
      type: 'warning'
    })
    
    const questionId = route.params.id
    const response = await questionApi.acceptAnswer(questionId, answerId)
    
    if (response.code === 200) {
      ElMessage.success('采纳成功')
      loadQuestionDetail() // 重新加载
    }
  } catch (error) {
    if (error !== 'cancel') {
      console.error('采纳答案失败:', error)
    }
  }
}

onMounted(() => {
  loadQuestionDetail()
})
</script>
```

**后端接口实现：**
```java
@RestController
@RequestMapping("/api/question")
public class QuestionController {
    
    /**
     * 获取问题详情
     */
    @GetMapping("/detail/{id}")
    public Result<QuestionDetailVO> getQuestionDetail(@PathVariable Long id) {
        Long userId = SecurityUtils.getCurrentUserId();
        QuestionDetailVO detail = questionService.getQuestionDetail(id, userId);
        return Result.success("查询成功", detail);
    }
    
    /**
     * 回答问题
     */
    @PostMapping("/{id}/answer")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Long> answerQuestion(
            @PathVariable Long id,
            @RequestBody @Validated AnswerDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long answerId = questionService.answerQuestion(id, dto, userId);
        return Result.success("回答成功", answerId);
    }
    
    /**
     * 采纳答案
     */
    @PostMapping("/{id}/accept-answer")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<Void> acceptAnswer(
            @PathVariable Long id,
            @RequestBody AcceptAnswerDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        questionService.acceptAnswer(id, dto.getAnswerId(), userId);
        return Result.success("采纳成功", null);
    }
}

@Service
public class QuestionService {
    
    /**
     * 获取问题详情
     */
    public QuestionDetailVO getQuestionDetail(Long questionId, Long userId) {
        // 1. 查询问题信息
        StudyQuestion question = questionMapper.selectById(questionId);
        if (question == null) {
            throw new BusinessException("问题不存在");
        }
        
        // 2. 权限检查
        boolean canView = false;
        if (question.getUserId().equals(userId)) {
            // 发布者可以查看
            canView = true;
        } else if (userService.isAdmin(userId)) {
            // 管理员可以查看
            canView = true;
        } else if (!"PENDING_REVIEW".equals(question.getStatus()) 
                && !"REJECTED".equals(question.getStatus())) {
            // 审核通过的问题，所有用户都可以查看
            canView = true;
        }
        
        if (!canView) {
            throw new BusinessException("无权查看此问题");
        }
        
        // 3. 只有审核通过的问题才增加浏览量
        if (!"PENDING_REVIEW".equals(question.getStatus()) 
            && !"REJECTED".equals(question.getStatus())) {
            questionMapper.incrementViewCount(questionId);
        }
        
        // 4. 查询回答列表
        LambdaQueryWrapper<StudyAnswer> answerWrapper = new LambdaQueryWrapper<>();
        answerWrapper.eq(StudyAnswer::getQuestionId, questionId)
                    .eq(StudyAnswer::getDeleteFlag, 0)
                    .orderByDesc(StudyAnswer::getIsAccepted) // 已采纳的排在前面
                    .orderByDesc(StudyAnswer::getCreateTime);
        List<StudyAnswer> answers = answerMapper.selectList(answerWrapper);
        
        // 5. 构建详情VO
        QuestionDetailVO detail = new QuestionDetailVO();
        detail.setQuestion(convertToVO(question));
        detail.setAnswers(answers.stream()
            .map(this::convertAnswerToVO)
            .collect(Collectors.toList()));
        
        return detail;
    }
    
    /**
     * 增加浏览量
     */
    @Update("UPDATE study_question SET view_count = view_count + 1 WHERE id = #{id}")
    int incrementViewCount(@Param("id") Long id);
}
```

#### 2.4.6 我的问题/我的回答页面

**我的问题页面：** `/user/questions/published`

**后端接口：**
```java
@RestController
@RequestMapping("/api/question")
public class QuestionController {
    
    /**
     * 获取我发布的问题列表
     */
    @GetMapping("/my/published")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<PageResult<QuestionVO>> getMyPublishedQuestions(
            @RequestParam(required = false) String status,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        Long userId = SecurityUtils.getCurrentUserId();
        PageResult<QuestionVO> result = questionService.getMyPublishedQuestions(userId, status, pageNum, pageSize);
        return Result.success("查询成功", result);
    }
    
    /**
     * 获取我回答的问题列表
     */
    @GetMapping("/my/answered")
    @PreAuthorize("hasAuthority('ROLE_USER') or hasAuthority('ROLE_ADMIN')")
    public Result<PageResult<QuestionVO>> getMyAnsweredQuestions(
            @RequestParam(required = false) String status,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        Long userId = SecurityUtils.getCurrentUserId();
        PageResult<QuestionVO> result = questionService.getMyAnsweredQuestions(userId, status, pageNum, pageSize);
        return Result.success("查询成功", result);
    }
}

@Service
public class QuestionService {
    
    /**
     * 获取我发布的问题列表
     */
    public PageResult<QuestionVO> getMyPublishedQuestions(Long userId, String status, Integer pageNum, Integer pageSize) {
        Page<StudyQuestion> page = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<StudyQuestion> wrapper = new LambdaQueryWrapper<>();
        
        wrapper.eq(StudyQuestion::getUserId, userId);
        
        if (StringUtils.hasText(status)) {
            wrapper.eq(StudyQuestion::getStatus, status);
        }
        
        wrapper.eq(StudyQuestion::getDeleteFlag, 0);
        wrapper.orderByDesc(StudyQuestion::getCreateTime);
        
        Page<StudyQuestion> result = questionMapper.selectPage(page, wrapper);
        
        List<QuestionVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 获取我回答的问题列表
     */
    public PageResult<QuestionVO> getMyAnsweredQuestions(Long userId, String status, Integer pageNum, Integer pageSize) {
        // 通过回答表关联查询问题
        Page<StudyAnswer> answerPage = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<StudyAnswer> answerWrapper = new LambdaQueryWrapper<>();
        answerWrapper.eq(StudyAnswer::getUserId, userId)
                    .eq(StudyAnswer::getDeleteFlag, 0)
                    .orderByDesc(StudyAnswer::getCreateTime);
        
        Page<StudyAnswer> answerResult = answerMapper.selectPage(answerPage, answerWrapper);
        
        // 获取问题ID列表
        List<Long> questionIds = answerResult.getRecords().stream()
            .map(StudyAnswer::getQuestionId)
            .distinct()
            .collect(Collectors.toList());
        
        if (questionIds.isEmpty()) {
            return new PageResult<>(Collections.emptyList(), 0, pageNum, pageSize);
        }
        
        // 查询问题
        LambdaQueryWrapper<StudyQuestion> questionWrapper = new LambdaQueryWrapper<>();
        questionWrapper.in(StudyQuestion::getId, questionIds);
        
        if (StringUtils.hasText(status)) {
            questionWrapper.eq(StudyQuestion::getStatus, status);
        }
        
        questionWrapper.eq(StudyQuestion::getDeleteFlag, 0);
        questionWrapper.orderByDesc(StudyQuestion::getCreateTime);
        
        List<StudyQuestion> questions = questionMapper.selectList(questionWrapper);
        
        List<QuestionVO> voList = questions.stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, questions.size(), pageNum, pageSize);
    }
}
```

#### 2.4.7 管理员问题审核功能（详细实现）

**审核页面：** `/admin/question-audit`

**功能说明：**
- 查看待审核问题列表
- 支持筛选（分类、关键词、触发原因）
- 审核操作（通过、拒绝）
- 审核原因填写
- 问题详情查看
- 下架违规问题

**前端页面实现：**
```vue
<template>
  <div class="question-audit-container">
    <h1>问题审核</h1>
    
    <!-- 筛选栏 -->
    <div class="filter-section">
      <el-input
        v-model="searchForm.keyword"
        placeholder="搜索问题标题或描述..."
        clearable
        @keyup.enter="loadPendingQuestions"
      >
        <template #append>
          <el-button @click="loadPendingQuestions">搜索</el-button>
        </template>
      </el-input>
      
      <el-select v-model="searchForm.category" placeholder="选择分类" clearable>
        <el-option label="全部" value="" />
        <el-option label="数学" value="MATH" />
        <el-option label="物理" value="PHYSICS" />
        <!-- 其他分类... -->
      </el-select>
      
      <el-select v-model="searchForm.triggerReason" placeholder="触发原因" clearable>
        <el-option label="全部" value="" />
        <el-option label="包含敏感词" value="敏感词" />
        <el-option label="发布频繁" value="发布频繁" />
        <el-option label="新注册用户" value="新注册用户" />
      </el-select>
    </div>
    
    <!-- 待审核问题列表 -->
    <el-table :data="questionList" stripe>
      <el-table-column prop="id" label="ID" width="80" />
      <el-table-column prop="title" label="问题标题" min-width="200" show-overflow-tooltip />
      <el-table-column prop="category" label="分类" width="100">
        <template #default="{ row }">
          <el-tag>{{ getCategoryName(row.category) }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="user.nickname" label="发布者" width="120" />
      <el-table-column prop="reward" label="悬赏金额" width="100">
        <template #default="{ row }">
          <span v-if="row.reward > 0">¥{{ row.reward }}</span>
          <span v-else>无</span>
        </template>
      </el-table-column>
      <el-table-column prop="auditTriggerReason" label="触发原因" min-width="150" show-overflow-tooltip />
      <el-table-column prop="createTime" label="发布时间" width="180">
        <template #default="{ row }">
          {{ formatTime(row.createTime) }}
        </template>
      </el-table-column>
      <el-table-column label="操作" width="280" fixed="right">
        <template #default="{ row }">
          <el-button type="primary" size="small" @click="viewDetail(row.id)">
            查看详情
          </el-button>
          <el-button type="success" size="small" @click="handleApprove(row.id)">
            通过
          </el-button>
          <el-button type="danger" size="small" @click="handleReject(row.id)">
            拒绝
          </el-button>
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 分页 -->
    <el-pagination
      v-model:current-page="pagination.pageNum"
      v-model:page-size="pagination.pageSize"
      :total="pagination.total"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handlePageChange"
    />
    
    <!-- 审核对话框 -->
    <el-dialog
      v-model="auditDialogVisible"
      :title="auditDialogTitle"
      width="600px"
    >
      <el-form :model="auditForm" :rules="auditRules" ref="auditFormRef">
        <el-form-item label="审核结果" prop="approved">
          <el-radio-group v-model="auditForm.approved">
            <el-radio :label="true">通过</el-radio>
            <el-radio :label="false">拒绝</el-radio>
          </el-radio-group>
        </el-form-item>
        
        <el-form-item
          label="拒绝原因"
          prop="reason"
          v-if="!auditForm.approved"
        >
          <el-input
            v-model="auditForm.reason"
            type="textarea"
            :rows="4"
            placeholder="请输入拒绝原因（必填）"
            maxlength="500"
            show-word-limit
          />
        </el-form-item>
      </el-form>
      
      <template #footer>
        <el-button @click="auditDialogVisible = false">取消</el-button>
        <el-button type="primary" @click="submitAudit" :loading="auditing">
          确定
        </el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { adminQuestionApi } from '@/api'

const searchForm = ref({
  keyword: '',
  category: '',
  triggerReason: ''
})

const questionList = ref([])
const pagination = ref({
  pageNum: 1,
  pageSize: 10,
  total: 0
})

const auditDialogVisible = ref(false)
const auditDialogTitle = ref('审核问题')
const auditForm = ref({
  questionId: null,
  approved: true,
  reason: ''
})
const auditing = ref(false)

// 加载待审核问题列表
const loadPendingQuestions = async () => {
  try {
    const params = {
      ...searchForm.value,
      pageNum: pagination.value.pageNum,
      pageSize: pagination.value.pageSize
    }
    const response = await adminQuestionApi.getPendingList(params)
    if (response.code === 200) {
      questionList.value = response.data.records
      pagination.value.total = response.data.total
    }
  } catch (error) {
    console.error('加载待审核问题列表失败:', error)
  }
}

// 查看详情
const viewDetail = (questionId) => {
  // 跳转到问题详情页或打开详情对话框
  window.open(`/question/detail/${questionId}`, '_blank')
}

// 审核通过
const handleApprove = (questionId) => {
  auditForm.value = {
    questionId,
    approved: true,
    reason: ''
  }
  auditDialogTitle.value = '审核通过'
  auditDialogVisible.value = true
}

// 审核拒绝
const handleReject = (questionId) => {
  auditForm.value = {
    questionId,
    approved: false,
    reason: ''
  }
  auditDialogTitle.value = '审核拒绝'
  auditDialogVisible.value = true
}

// 提交审核
const submitAudit = async () => {
  try {
    auditing.value = true
    
    if (!auditForm.value.approved && !auditForm.value.reason) {
      ElMessage.warning('拒绝审核必须填写拒绝原因')
      return
    }
    
    const response = await adminQuestionApi.auditQuestion(
      auditForm.value.questionId,
      {
        approved: auditForm.value.approved,
        reason: auditForm.value.reason
      }
    )
    
    if (response.code === 200) {
      ElMessage.success(auditForm.value.approved ? '审核通过' : '审核拒绝')
      auditDialogVisible.value = false
      loadPendingQuestions() // 重新加载列表
    }
  } catch (error) {
    console.error('提交审核失败:', error)
  } finally {
    auditing.value = false
  }
}

onMounted(() => {
  loadPendingQuestions()
})
</script>
```

**后端接口实现：**
```java
@RestController
@RequestMapping("/api/admin/question")
public class AdminQuestionController {
    
    @Autowired
    private QuestionService questionService;
    
    /**
     * 获取待审核问题列表
     */
    @GetMapping("/pending")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<PageResult<QuestionVO>> getPendingQuestionList(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) String triggerReason,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        
        PageResult<QuestionVO> result = questionService.getPendingAuditList(
            category, keyword, triggerReason, pageNum, pageSize
        );
        return Result.success("查询成功", result);
    }
    
    /**
     * 审核问题
     */
    @PostMapping("/{id}/audit")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<Void> auditQuestion(
            @PathVariable Long id,
            @RequestBody @Validated QuestionAuditDTO dto) {
        Long adminId = SecurityUtils.getCurrentUserId();
        questionService.auditQuestion(id, dto.getApproved(), dto.getReason(), adminId);
        return Result.success(dto.getApproved() ? "审核通过" : "审核拒绝", null);
    }
    
    /**
     * 下架问题
     */
    @PostMapping("/{id}/offshelf")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<Void> offshelfQuestion(
            @PathVariable Long id,
            @RequestBody @Validated OffshelfDTO dto) {
        Long adminId = SecurityUtils.getCurrentUserId();
        questionService.offshelfQuestion(id, dto.getReason(), adminId);
        return Result.success("下架成功", null);
    }
}

@Service
public class QuestionService {
    
    /**
     * 获取待审核问题列表
     */
    public PageResult<QuestionVO> getPendingAuditList(
            String category, String keyword, String triggerReason,
            Integer pageNum, Integer pageSize) {
        
        Page<StudyQuestion> page = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<StudyQuestion> wrapper = new LambdaQueryWrapper<>();
        
        // 只查询待审核的问题
        wrapper.eq(StudyQuestion::getStatus, "PENDING_REVIEW");
        wrapper.eq(StudyQuestion::getAuditStatus, "PENDING");
        wrapper.eq(StudyQuestion::getDeleteFlag, 0);
        
        // 分类筛选
        if (StringUtils.hasText(category)) {
            wrapper.eq(StudyQuestion::getCategory, category);
        }
        
        // 关键词筛选
        if (StringUtils.hasText(keyword)) {
            wrapper.and(w -> w.like(StudyQuestion::getTitle, keyword)
                            .or()
                            .like(StudyQuestion::getDescription, keyword));
        }
        
        // 触发原因筛选
        if (StringUtils.hasText(triggerReason)) {
            wrapper.like(StudyQuestion::getAuditTriggerReason, triggerReason);
        }
        
        wrapper.orderByDesc(StudyQuestion::getCreateTime);
        
        Page<StudyQuestion> result = questionMapper.selectPage(page, wrapper);
        
        List<QuestionVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 审核问题
     */
    @Transactional(rollbackFor = Exception.class)
    public void auditQuestion(Long questionId, Boolean approved, String reason, Long adminId) {
        // 1. 查询问题
        StudyQuestion question = questionMapper.selectById(questionId);
        if (question == null) {
            throw new BusinessException("问题不存在");
        }
        
        // 2. 检查审核状态
        if (!"PENDING_REVIEW".equals(question.getStatus())) {
            throw new BusinessException("该问题当前不需要审核");
        }
        
        if (approved) {
            // 审核通过
            question.setStatus("PENDING_ANSWER");
            question.setAuditStatus("APPROVED");
            question.setAuditReason(null);
        } else {
            // 审核拒绝
            if (StringUtils.isBlank(reason)) {
                throw new BusinessException("拒绝审核必须填写拒绝原因");
            }
            question.setStatus("REJECTED");
            question.setAuditStatus("REJECTED");
            question.setAuditReason(reason);
        }
        
        question.setAuditTime(new Date());
        question.setAuditAdminId(adminId);
        question.setUpdateTime(new Date());
        
        questionMapper.updateById(question);
        
        // 3. 发送系统消息给发布者
        String messageType = approved ? "QUESTION_APPROVED" : "QUESTION_REJECTED";
        String messageTitle = approved ? "问题审核通过" : "问题审核被拒绝";
        String messageContent = approved 
            ? "您发布的问题《" + question.getTitle() + "》审核通过，已发布"
            : "很抱歉，您发布的问题《" + question.getTitle() + "》审核被拒绝。原因：" + reason;
        
        systemMessageService.sendMessage(
            question.getUserId(),
            messageType,
            messageTitle,
            messageContent,
            "QUESTION",
            questionId
        );
    }
    
    /**
     * 下架问题
     */
    @Transactional(rollbackFor = Exception.class)
    public void offshelfQuestion(Long questionId, String reason, Long adminId) {
        // 1. 查询问题
        StudyQuestion question = questionMapper.selectById(questionId);
        if (question == null) {
            throw new BusinessException("问题不存在");
        }
        
        // 2. 检查问题状态（已解决的问题不能下架）
        if ("SOLVED".equals(question.getStatus())) {
            throw new BusinessException("已解决的问题不能下架");
        }
        
        // 3. 更新问题状态
        question.setStatus("ADMIN_OFFSHELF");
        question.setOffshelfType("ADMIN");
        question.setOffshelfReason(reason);
        question.setOffshelfTime(new Date());
        question.setOffshelfAdminId(adminId);
        question.setUpdateTime(new Date());
        
        questionMapper.updateById(question);
        
        // 4. 发送通知给发布者
        systemMessageService.sendMessage(
            question.getUserId(),
            "QUESTION_OFFSHELF",
            "问题被下架",
            "您发布的问题《" + question.getTitle() + "》因违规被下架。原因：" + reason + "。您可以修改后重新提交。",
            "QUESTION",
            questionId
        );
    }
}
```

#### 2.4.8 管理员问题管理功能

**管理页面：** `/admin/question/manage`

**功能说明：**
- 查看所有问题（支持筛选和搜索）
- 下架违规问题
- 查看问题详情和回答
- 统计数据展示

**后端接口：**
```java
@RestController
@RequestMapping("/api/admin/question")
public class AdminQuestionController {
    
    /**
     * 获取所有问题列表（管理员）
     */
    @GetMapping("/list")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<PageResult<QuestionVO>> getAllQuestionList(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String status,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false, defaultValue = "1") Integer pageNum,
            @RequestParam(required = false, defaultValue = "10") Integer pageSize) {
        
        PageResult<QuestionVO> result = questionService.getAllQuestionList(
            category, status, keyword, pageNum, pageSize
        );
        return Result.success("查询成功", result);
    }
    
    /**
     * 获取问题统计信息
     */
    @GetMapping("/statistics")
    @PreAuthorize("hasAuthority('ROLE_ADMIN')")
    public Result<QuestionStatisticsVO> getQuestionStatistics() {
        QuestionStatisticsVO statistics = questionService.getQuestionStatistics();
        return Result.success("查询成功", statistics);
    }
}

@Service
public class QuestionService {
    
    /**
     * 获取所有问题列表（管理员，不限制状态）
     */
    public PageResult<QuestionVO> getAllQuestionList(
            String category, String status, String keyword,
            Integer pageNum, Integer pageSize) {
        
        Page<StudyQuestion> page = new Page<>(pageNum, pageSize);
        LambdaQueryWrapper<StudyQuestion> wrapper = new LambdaQueryWrapper<>();
        
        // 分类筛选
        if (StringUtils.hasText(category)) {
            wrapper.eq(StudyQuestion::getCategory, category);
        }
        
        // 状态筛选
        if (StringUtils.hasText(status)) {
            wrapper.eq(StudyQuestion::getStatus, status);
        }
        
        // 关键词搜索
        if (StringUtils.hasText(keyword)) {
            wrapper.and(w -> w.like(StudyQuestion::getTitle, keyword)
                            .or()
                            .like(StudyQuestion::getDescription, keyword));
        }
        
        wrapper.eq(StudyQuestion::getDeleteFlag, 0);
        wrapper.orderByDesc(StudyQuestion::getCreateTime);
        
        Page<StudyQuestion> result = questionMapper.selectPage(page, wrapper);
        
        List<QuestionVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 获取问题统计信息
     */
    public QuestionStatisticsVO getQuestionStatistics() {
        QuestionStatisticsVO statistics = new QuestionStatisticsVO();
        
        // 总问题数
        statistics.setTotal(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        // 待审核数
        statistics.setPendingReview(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .eq(StudyQuestion::getStatus, "PENDING_REVIEW")
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        // 待解答数
        statistics.setPendingAnswer(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .eq(StudyQuestion::getStatus, "PENDING_ANSWER")
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        // 已回答数
        statistics.setAnswered(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .eq(StudyQuestion::getStatus, "ANSWERED")
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        // 已解决数
        statistics.setSolved(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .eq(StudyQuestion::getStatus, "SOLVED")
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        // 今日新增
        LocalDateTime startOfDay = LocalDateTime.now().withHour(0).withMinute(0).withSecond(0);
        statistics.setTodayNew(questionMapper.selectCount(
            new LambdaQueryWrapper<StudyQuestion>()
                .ge(StudyQuestion::getCreateTime, startOfDay)
                .eq(StudyQuestion::getDeleteFlag, 0)
        ));
        
        return statistics;
    }
}
```

---

### 2.5 消息通知模块

#### 2.5.1 私信功能（详细实现）

**私信列表页：** `/message/chat`

**页面布局：**
```
┌────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌─────────────────────────────┐ │
│  │ 会话列表     │  │ 聊天内容                    │ │
│  ├──────────────┤  ├─────────────────────────────┤ │
│  │ [头像] 用户A │  │ 对方: 你好                  │ │
│  │    最后消息  │  │ 我: 你好，请问...          │ │
│  │    时间      │  │ 对方: [图片]                │ │
│  ├──────────────┤  │ ...                         │ │
│  │ [头像] 用户B │  ├─────────────────────────────┤ │
│  │    最后消息  │  │ [输入框]                    │ │
│  │    时间      │  │ [图片] [发送]               │ │
│  └──────────────┘  └─────────────────────────────┘ │
└────────────────────────────────────────────────────┘
```

**自动创建私信会话：**
```java
@Service
public class ChatSessionService {
    
    /**
     * 创建会话（如果不存在）
     */
    @Transactional
    public Long createSession(Long userId1, Long userId2, String relatedType, Long relatedId) {
        // 1. 查询是否已存在会话
        QueryWrapper<ChatSession> wrapper = new QueryWrapper<>();
        wrapper.and(w -> w.eq("user1_id", userId1).eq("user2_id", userId2)
                       .or()
                       .eq("user1_id", userId2).eq("user2_id", userId1));
        wrapper.eq("related_type", relatedType);
        wrapper.eq("related_id", relatedId);
        
        ChatSession session = sessionMapper.selectOne(wrapper);
        
        if (session != null) {
            return session.getId();
        }
        
        // 2. 创建新会话
        session = new ChatSession();
        session.setUser1Id(userId1);
        session.setUser2Id(userId2);
        session.setRelatedType(relatedType);
        session.setRelatedId(relatedId);
        session.setCreateTime(new Date());
        sessionMapper.insert(session);
        
        return session.getId();
    }
    
    /**
     * 发送消息
     */
    @Transactional
    public void sendMessage(SendMessageDTO dto, Long senderId) {
        // 1. 验证会话是否存在
        ChatSession session = sessionMapper.selectById(dto.getSessionId());
        if (session == null) {
            throw new BusinessException("会话不存在");
        }
        
        // 2. 验证发送者是会话参与者
        if (!session.getUser1Id().equals(senderId) && !session.getUser2Id().equals(senderId)) {
            throw new BusinessException("无权发送消息");
        }
        
        // 3. 确定接收者
        Long receiverId = session.getUser1Id().equals(senderId) ? session.getUser2Id() : session.getUser1Id();
        
        // 4. 敏感词检测（如果是文字消息）
        if ("TEXT".equals(dto.getMessageType())) {
            SensitiveWordCheckResult checkResult = sensitiveWordService.check(dto.getContent());
            if (!checkResult.isPass()) {
                throw new BusinessException("消息包含敏感词，请修改后发送");
            }
        }
        
        // 5. 创建消息
        ChatMessage message = new ChatMessage();
        message.setSessionId(dto.getSessionId());
        message.setSenderId(senderId);
        message.setReceiverId(receiverId);
        message.setMessageType(dto.getMessageType());
        message.setContent(dto.getContent());
        message.setImages(JSON.toJSONString(dto.getImages()));
        message.setIsRead(0);
        message.setCreateTime(new Date());
        messageMapper.insert(message);
        
        // 6. 更新会话的最后消息时间
        session.setLastMessageTime(new Date());
        session.setLastMessageContent(dto.getContent());
        sessionMapper.updateById(session);
        
        // 7. WebSocket 实时推送给接收者
        webSocketService.sendMessageToUser(receiverId, message);
    }
}
```

#### 2.5.2 WebSocket 实时推送（详细实现）

**后端 WebSocket 配置：**
```java
@Configuration
@EnableWebSocket
public class WebSocketConfig implements WebSocketConfigurer {
    
    @Autowired
    private WebSocketHandler webSocketHandler;
    
    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(webSocketHandler, "/ws")
                .setAllowedOrigins("*")
                .addInterceptors(new WebSocketInterceptor());
    }
}

@Component
public class WebSocketHandler extends TextWebSocketHandler {
    
    // 存储用户ID和WebSocket会话的映射
    private static final Map<Long, WebSocketSession> USER_SESSIONS = new ConcurrentHashMap<>();
    
    @Override
    public void afterConnectionEstablished(WebSocketSession session) throws Exception {
        // 从URL参数获取token
        String token = getTokenFromSession(session);
        Long userId = jwtUtil.getUserIdFromToken(token);
        
        // 保存会话
        USER_SESSIONS.put(userId, session);
        
        System.out.println("用户 " + userId + " WebSocket连接成功");
    }
    
    @Override
    public void afterConnectionClosed(WebSocketSession session, CloseStatus status) throws Exception {
        // 移除会话
        Long userId = getUserIdFromSession(session);
        USER_SESSIONS.remove(userId);
        
        System.out.println("用户 " + userId + " WebSocket连接关闭");
    }
    
    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) throws Exception {
        // 处理客户端发送的消息（心跳等）
        String payload = message.getPayload();
        if ("ping".equals(payload)) {
            session.sendMessage(new TextMessage("pong"));
        }
    }
    
    /**
     * 发送消息给指定用户
     */
    public void sendMessageToUser(Long userId, Object message) {
        WebSocketSession session = USER_SESSIONS.get(userId);
        if (session != null && session.isOpen()) {
            try {
                String json = JSON.toJSONString(message);
                session.sendMessage(new TextMessage(json));
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }
}
```

**前端 WebSocket 连接：**
```javascript
// WebSocket 连接管理
class WebSocketManager {
  constructor() {
    this.ws = null
    this.reconnectTimer = null
    this.heartbeatTimer = null
    this.messageHandlers = []
  }
  
  // 连接
  connect(token) {
    const wsUrl = `ws://localhost:8080/ws?token=${token}`
    this.ws = new WebSocket(wsUrl)
    
    this.ws.onopen = () => {
      console.log('WebSocket 连接成功')
      this.startHeartbeat()
    }
    
    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data)
      this.handleMessage(message)
    }
    
    this.ws.onerror = (error) => {
      console.error('WebSocket 错误:', error)
    }
    
    this.ws.onclose = () => {
      console.log('WebSocket 连接关闭')
      this.stopHeartbeat()
      this.reconnect(token)
    }
  }
  
  // 断线重连
  reconnect(token) {
    if (this.reconnectTimer) return
    
    this.reconnectTimer = setTimeout(() => {
      console.log('尝试重新连接 WebSocket...')
      this.connect(token)
      this.reconnectTimer = null
    }, 3000)
  }
  
  // 心跳
  startHeartbeat() {
    this.heartbeatTimer = setInterval(() => {
      if (this.ws && this.ws.readyState === WebSocket.OPEN) {
        this.ws.send('ping')
      }
    }, 30000) // 30秒一次心跳
  }
  
  stopHeartbeat() {
    if (this.heartbeatTimer) {
      clearInterval(this.heartbeatTimer)
      this.heartbeatTimer = null
    }
  }
  
  // 处理消息
  handleMessage(message) {
    this.messageHandlers.forEach(handler => {
      handler(message)
    })
  }
  
  // 添加消息处理器
  addMessageHandler(handler) {
    this.messageHandlers.push(handler)
  }
  
  // 关闭连接
  close() {
    this.stopHeartbeat()
    if (this.reconnectTimer) {
      clearTimeout(this.reconnectTimer)
      this.reconnectTimer = null
    }
    if (this.ws) {
      this.ws.close()
      this.ws = null
    }
  }
}

// 使用
const wsManager = new WebSocketManager()

// 登录成功后连接
wsManager.connect(token)

// 添加消息处理器
wsManager.addMessageHandler((message) => {
  if (message.type === 'CHAT') {
    // 处理私信消息
    handleChatMessage(message)
  } else if (message.type === 'SYSTEM') {
    // 处理系统消息
    handleSystemMessage(message)
  }
})
```

---

### 2.7 智能审核机制（详细实现）

#### 2.7.1 敏感词检测（AC自动机算法）

**AC自动机实现：**
```java
/**
 * AC自动机节点
 */
class TrieNode {
    Map<Character, TrieNode> children = new HashMap<>();
    TrieNode fail; // 失败指针
    boolean isEnd; // 是否是敏感词结尾
    String word; // 敏感词
}

/**
 * AC自动机
 */
public class AhoCorasick {
    private TrieNode root = new TrieNode();
    
    /**
     * 构建AC自动机
     */
    public void build(List<String> words) {
        // 1. 构建Trie树
        for (String word : words) {
            TrieNode node = root;
            for (char c : word.toCharArray()) {
                node = node.children.computeIfAbsent(c, k -> new TrieNode());
            }
            node.isEnd = true;
            node.word = word;
        }
        
        // 2. 构建失败指针（BFS）
        Queue<TrieNode> queue = new LinkedList<>();
        root.fail = root;
        
        for (TrieNode child : root.children.values()) {
            child.fail = root;
            queue.offer(child);
        }
        
        while (!queue.isEmpty()) {
            TrieNode node = queue.poll();
            
            for (Map.Entry<Character, TrieNode> entry : node.children.entrySet()) {
                char c = entry.getKey();
                TrieNode child = entry.getValue();
                queue.offer(child);
                
                TrieNode failNode = node.fail;
                while (failNode != root && !failNode.children.containsKey(c)) {
                    failNode = failNode.fail;
                }
                
                child.fail = failNode.children.getOrDefault(c, root);
            }
        }
    }
    
    /**
     * 检测文本中的敏感词
     */
    public List<String> search(String text) {
        List<String> result = new ArrayList<>();
        TrieNode node = root;
        
        for (int i = 0; i < text.length(); i++) {
            char c = text.charAt(i);
            
            // 转小写
            c = Character.toLowerCase(c);
            
            // 跳过空格等特殊字符
            if (!Character.isLetterOrDigit(c) && !isChinese(c)) {
                continue;
            }
            
            while (node != root && !node.children.containsKey(c)) {
                node = node.fail;
            }
            
            node = node.children.getOrDefault(c, root);
            
            // 检查所有匹配的敏感词
            TrieNode temp = node;
            while (temp != root) {
                if (temp.isEnd) {
                    result.add(temp.word);
                }
                temp = temp.fail;
            }
        }
        
        return result;
    }
    
    private boolean isChinese(char c) {
        return c >= 0x4E00 && c <= 0x9FA5;
    }
}

/**
 * 敏感词服务
 */
@Service
public class SensitiveWordService {
    
    private AhoCorasick ahoCorasick = new AhoCorasick();
    
    @Autowired
    private SensitiveWordMapper sensitiveWordMapper;
    
    @PostConstruct
    public void init() {
        // 从数据库加载敏感词
        List<SensitiveWord> words = sensitiveWordMapper.selectList(null);
        List<String> wordList = words.stream()
            .map(SensitiveWord::getWord)
            .collect(Collectors.toList());
        
        // 构建AC自动机
        ahoCorasick.build(wordList);
        
        System.out.println("敏感词库加载完成，共 " + words.size() + " 个敏感词");
    }
    
    /**
     * 检测文本
     */
    public SensitiveWordCheckResult check(String text) {
        List<String> foundWords = ahoCorasick.search(text);
        
        if (foundWords.isEmpty()) {
            return SensitiveWordCheckResult.pass();
        }
        
        // 检查敏感词严重程度
        for (String word : foundWords) {
            SensitiveWord sw = sensitiveWordMapper.selectByWord(word);
            if (sw != null && "SEVERE".equals(sw.getLevel())) {
                return SensitiveWordCheckResult.reject("包含严重敏感词：" + word);
            }
        }
        
        return SensitiveWordCheckResult.needManualReview("包含敏感词：" + String.join("、", foundWords));
    }
}
```

---

### 2.8 管理员功能（详细实现）

#### 2.8.1 内容管理

**内容列表页：** `/admin/content/list`

**功能：**
- 查看所有已发布的内容（失物、商品、任务）
- 按类型筛选、按状态筛选、搜索
- 下架违规内容
- 删除不当内容

**下架功能实现：**
```java
@Service
public class ContentManageService {
    
    /**
     * 下架内容
     */
    @Transactional
    public void offshelfContent(String contentType, Long contentId, OffshelfDTO dto, Long adminId) {
        if ("LOST_FOUND".equals(contentType)) {
            // 下架失物
            LostFound lostFound = lostFoundMapper.selectById(contentId);
            if (lostFound == null) {
                throw new BusinessException("内容不存在");
            }
            
            lostFound.setStatus("ADMIN_OFFSHELF");
            lostFound.setOffshelfType("ADMIN");
            lostFound.setOffshelfReason(dto.getReason());
            lostFound.setOffshelfTime(new Date());
            lostFound.setOffshelfAdminId(adminId);
            lostFoundMapper.updateById(lostFound);
            
            // 发送通知给发布者
            notifyUser(lostFound.getUserId(), "内容被下架", 
                "您发布的失物《" + lostFound.getTitle() + "》因违规被下架。原因：" + dto.getReason() + "。您可以修改后重新提交。");
                
        } else if ("GOODS".equals(contentType)) {
            // 下架商品（类似）
            // ...
        } else if ("TASK".equals(contentType)) {
            // 下架任务（类似）
            // ...
        }
    }
}
```

#### 2.8.2 用户管理

**封禁功能实现：**
```java
@Service
public class UserManageService {
    
    /**
     * 封禁用户
     */
    @Transactional
    public void banUser(Long userId, BanDTO dto, Long adminId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 不能封禁管理员
        if ("ADMIN".equals(user.getRole())) {
            throw new BusinessException("不能封禁管理员");
        }
        
        // 3. 更新用户状态
        user.setStatus(2); // 已封禁
        user.setBanType(dto.getBanType());
        user.setBanReason(dto.getReason());
        user.setBanTime(new Date());
        user.setBanAdminId(adminId);
        
        if ("TEMPORARY".equals(dto.getBanType())) {
            user.setBanDays(dto.getBanDays());
            // 计算解封时间
            Calendar calendar = Calendar.getInstance();
            calendar.add(Calendar.DAY_OF_MONTH, dto.getBanDays());
            user.setUnbanTime(calendar.getTime());
        }
        
        userMapper.updateById(user);
        
        // 4. 保存封禁记录
        UserBanRecord record = new UserBanRecord();
        record.setUserId(userId);
        record.setBanType(dto.getBanType());
        record.setBanReason(dto.getReason());
        record.setBanDays(dto.getBanDays());
        record.setBanTime(new Date());
        record.setUnbanTime(user.getUnbanTime());
        record.setBanAdminId(adminId);
        banRecordMapper.insert(record);
        
        // 5. 踢出当前登录
        String key = "token:" + userId;
        redisTemplate.delete(key);
        
        // 6. 发送封禁通知（邮件）
        sendEmailNotification(user.getEmail(), "账号封禁通知", 
            "您的账号因违规被封禁。\n" +
            "封禁类型：" + (dto.getBanType().equals("PERMANENT") ? "永久封禁" : "临时封禁" + dto.getBanDays() + "天") + "\n" +
            "封禁原因：" + dto.getReason() + "\n" +
            "如有疑问，请联系管理员。");
    }
}
```

---

## 三、数据库设计

### 3.1 数据库设计说明

**设计原则：**
1. 遵循第三范式（3NF），减少数据冗余
2. 合理使用索引，提升查询性能
3. 使用乐观锁（version字段）处理并发问题
4. 所有表使用逻辑删除（delete_flag字段）
5. 统一时间字段（create_time、update_time）

**关键字段说明：**
- **view_count**：浏览次数，用于热度排序和统计
- **version**：乐观锁版本号，用于并发控制
- **audit_trigger_reason**：触发人工审核的原因，用于审核优化

**注意：** 失物招领模块不包含收藏和评论功能，因此不包含 `favorite_count` 和 `comment_count` 字段。其他模块（闲置交易、跑腿服务）可能包含这些字段。

### 3.2 用户表（user）

```sql
CREATE TABLE `user` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `email` VARCHAR(100) NOT NULL COMMENT '邮箱（用于登录）',
  `password` VARCHAR(255) NOT NULL COMMENT '密码（加密存储）',
  `nickname` VARCHAR(50) NOT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `gender` TINYINT(1) DEFAULT NULL COMMENT '性别：0-女，1-男',
  `grade` VARCHAR(20) DEFAULT NULL COMMENT '年级',
  `major` VARCHAR(100) DEFAULT NULL COMMENT '专业',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  
  -- 实名认证字段
  `real_name` VARCHAR(50) DEFAULT NULL COMMENT '真实姓名（实名认证）',
  `id_card` VARCHAR(18) DEFAULT NULL COMMENT '身份证号（实名认证）',
  `student_id` VARCHAR(50) DEFAULT NULL COMMENT '学号（实名认证）',
  `user_type` VARCHAR(20) DEFAULT '学生' COMMENT '用户类型：学生、教师',
  `is_verified` TINYINT(1) DEFAULT 0 COMMENT '是否实名认证：0-未认证，1-已认证',
  `verification_status` VARCHAR(20) DEFAULT 'NOT_VERIFIED' COMMENT '认证状态：NOT_VERIFIED、PENDING、VERIFIED、REJECTED',
  `verification_proof` TEXT DEFAULT NULL COMMENT '认证证明文件（JSON数组）',
  `verification_submit_time` DATETIME DEFAULT NULL COMMENT '认证提交时间',
  `verification_audit_time` DATETIME DEFAULT NULL COMMENT '认证审核时间',
  `verification_audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '认证拒绝原因',
  `verification_audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  
  -- 用户角色和状态
  `role` VARCHAR(20) DEFAULT 'USER' COMMENT '用户角色：USER-普通用户，ADMIN-管理员',
  `status` TINYINT(1) DEFAULT 1 COMMENT '账号状态：1-正常，2-已封禁',
  `ban_type` VARCHAR(20) DEFAULT NULL COMMENT '封禁类型：TEMPORARY-临时，PERMANENT-永久',
  `ban_reason` VARCHAR(500) DEFAULT NULL COMMENT '封禁原因',
  `ban_days` INT(11) DEFAULT NULL COMMENT '封禁天数',
  `ban_time` DATETIME DEFAULT NULL COMMENT '封禁时间',
  `unban_time` DATETIME DEFAULT NULL COMMENT '解封时间',
  `ban_admin_id` BIGINT(20) DEFAULT NULL COMMENT '封禁操作管理员ID',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_email` (`email`),
  KEY `idx_is_verified` (`is_verified`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 3.3 失物招领表（lost_found）

```sql
CREATE TABLE `lost_found` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '失物ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '发布者ID',
  `title` VARCHAR(200) NOT NULL COMMENT '物品名称',
  `category` VARCHAR(50) NOT NULL COMMENT '物品分类：证件类、电子产品、生活用品、书籍、其他',
  `description` TEXT NOT NULL COMMENT '物品描述',
  `type` VARCHAR(20) NOT NULL COMMENT '类型：LOST-失物，FOUND-招领',
  `lost_time` DATETIME NOT NULL COMMENT '丢失/拾取时间',
  `lost_location` VARCHAR(200) NOT NULL COMMENT '丢失/拾取地点',
  `contact_info` VARCHAR(200) DEFAULT NULL COMMENT '联系方式',
  `images` TEXT DEFAULT NULL COMMENT '物品图片（JSON数组）',
  `reward` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '悬赏金额',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，PENDING_CLAIM-待认领，CLAIMING-认领中，CLAIMED-已认领，CLOSED-已关闭，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态：PENDING-待审核，APPROVED-已通过，REJECTED-已拒绝',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因（敏感词、发布频繁等）',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型：USER-用户自行下架，ADMIN-管理员下架',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  
  -- 并发控制
  `version` INT(11) DEFAULT 0 COMMENT '乐观锁版本号',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_type` (`type`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_status_type_create_time` (`status`, `type`, `create_time` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_lost_found_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='失物招领表';
```

### 3.4 商品表（goods）

```sql
CREATE TABLE `goods` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '商品ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '卖家ID',
  `title` VARCHAR(200) NOT NULL COMMENT '商品标题',
  `category` VARCHAR(50) NOT NULL COMMENT '商品分类：数码产品、图书教材、服装鞋包、生活用品、运动健身、乐器、文创用品、其他',
  `description` TEXT NOT NULL COMMENT '商品描述',
  `current_price` DECIMAL(10, 2) NOT NULL COMMENT '售价',
  `condition` VARCHAR(50) DEFAULT NULL COMMENT '商品成色：全新、几乎全新、轻微使用痕迹、明显使用痕迹',
  `images` TEXT DEFAULT NULL COMMENT '商品图片（JSON数组，1-9张）',
  `trade_method` VARCHAR(50) DEFAULT NULL COMMENT '交易方式：FACE_TO_FACE-自提，MAIL-邮寄',
  `trade_location` VARCHAR(200) DEFAULT NULL COMMENT '自提地点（自提时必填）',
  `shipping_fee` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '邮寄费用（邮寄时可选）',
  
  -- 库存字段
  `stock` INT(11) NOT NULL DEFAULT 1 COMMENT '库存数量（最少1件）',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，ON_SALE-在售，SOLD_OUT-已售完，CLOSED-已关闭，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  
  -- 并发控制
  `version` INT(11) DEFAULT 0 COMMENT '乐观锁版本号（用于防止超卖）',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_current_price` (`current_price`),
  KEY `idx_stock` (`stock`),
  KEY `idx_view_count` (`view_count`),
  KEY `idx_status_create_time` (`status`, `create_time` DESC),
  KEY `idx_status_view_count` (`status`, `view_count` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_goods_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

### 3.4 订单表（order）

```sql
CREATE TABLE `order` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '订单ID',
  `order_no` VARCHAR(50) NOT NULL UNIQUE COMMENT '唯一订单号（格式：GO+时间戳13位+用户ID后2位，示例：GO170406720000012）',
  `goods_id` BIGINT(20) NOT NULL COMMENT '商品ID',
  `session_id` BIGINT(20) DEFAULT NULL COMMENT '关联聊天会话ID',
  `buyer_id` BIGINT(20) NOT NULL COMMENT '买家ID',
  `seller_id` BIGINT(20) NOT NULL COMMENT '卖家ID',
  
  -- 订单商品信息
  `quantity` INT(11) NOT NULL DEFAULT 1 COMMENT '购买数量',
  `price` DECIMAL(10, 2) NOT NULL COMMENT '单价',
  `total_amount` DECIMAL(10, 2) NOT NULL COMMENT '订单总金额（单价×数量）',
  `shipping_fee` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '邮寄费用（邮寄时）',
  
  -- 交易方式
  `trade_method` VARCHAR(50) NOT NULL COMMENT '交易方式：FACE_TO_FACE-自提，MAIL-邮寄',
  
  -- 邮寄信息（邮寄时必填）
  `receiver_name` VARCHAR(50) DEFAULT NULL COMMENT '收货人姓名',
  `receiver_phone` VARCHAR(20) DEFAULT NULL COMMENT '收货人电话',
  `receiver_address` VARCHAR(500) DEFAULT NULL COMMENT '收货地址',
  
  -- 自提信息（自提时）
  `pickup_location` VARCHAR(200) DEFAULT NULL COMMENT '自提地点（自提方式时使用）',
  
  -- 物流信息（邮寄时）
  `tracking_number` VARCHAR(100) DEFAULT NULL COMMENT '物流单号',
  `logistics_company` VARCHAR(100) DEFAULT NULL COMMENT '物流公司',
  `ship_time` DATETIME DEFAULT NULL COMMENT '发货时间',
  
  -- 订单状态
  `status` VARCHAR(20) NOT NULL DEFAULT 'PENDING_PAYMENT' COMMENT '订单状态：PENDING_PAYMENT-待付款，PAID-已付款（仅邮寄方式，等待发货），SHIPPED-已发货（仅邮寄方式），PENDING_PICKUP-待自提（仅自提方式，付款后自动进入，无需发货），COMPLETED-已完成，CANCELLED-已取消，REFUNDED-已退款',
  
  -- 订单时间
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `pay_time` DATETIME DEFAULT NULL COMMENT '付款时间',
  `complete_time` DATETIME DEFAULT NULL COMMENT '完成时间',
  `cancel_time` DATETIME DEFAULT NULL COMMENT '取消时间',
  `cancel_reason` VARCHAR(500) DEFAULT NULL COMMENT '取消原因',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_goods_id` (`goods_id`),
  KEY `idx_session_id` (`session_id`),
  KEY `idx_buyer_id` (`buyer_id`),
  KEY `idx_seller_id` (`seller_id`),
  KEY `idx_status` (`status`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_buyer_status` (`buyer_id`, `status`),
  KEY `idx_seller_status` (`seller_id`, `status`),
  CONSTRAINT `fk_order_goods` FOREIGN KEY (`goods_id`) REFERENCES `goods` (`id`),
  CONSTRAINT `fk_order_session` FOREIGN KEY (`session_id`) REFERENCES `chat_session` (`id`),
  CONSTRAINT `fk_order_buyer` FOREIGN KEY (`buyer_id`) REFERENCES `user` (`id`),
  CONSTRAINT `fk_order_seller` FOREIGN KEY (`seller_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

### 3.5 收货地址表（address）

```sql
CREATE TABLE `address` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '地址ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '用户ID',
  `receiver_name` VARCHAR(50) NOT NULL COMMENT '收货人姓名',
  `receiver_phone` VARCHAR(20) NOT NULL COMMENT '收货人电话',
  `province` VARCHAR(50) NOT NULL COMMENT '省份',
  `city` VARCHAR(50) NOT NULL COMMENT '城市',
  `district` VARCHAR(50) NOT NULL COMMENT '区县',
  `detail_address` VARCHAR(200) NOT NULL COMMENT '详细地址',
  `full_address` VARCHAR(500) NOT NULL COMMENT '完整地址（省+市+区+详细地址，用于订单显示）',
  `postal_code` VARCHAR(10) DEFAULT NULL COMMENT '邮政编码',
  `is_default` TINYINT(1) DEFAULT 0 COMMENT '是否默认地址：0-否，1-是',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_user_default` (`user_id`, `is_default`),
  CONSTRAINT `fk_address_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收货地址表';
```

### 3.5 学习问题表（study_question）

```sql
CREATE TABLE `study_question` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '问题ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '发布者ID',
  `title` VARCHAR(200) NOT NULL COMMENT '问题标题',
  `category` VARCHAR(50) NOT NULL COMMENT '问题分类：MATH-数学，PHYSICS-物理，CHEMISTRY-化学，BIOLOGY-生物，COMPUTER-计算机，ENGLISH-英语，LITERATURE-文学，HISTORY-历史，PHILOSOPHY-哲学，ECONOMICS-经济，MANAGEMENT-管理，LAW-法律，EDUCATION-教育，ART-艺术，ENGINEERING-工程，MEDICINE-医学，AGRICULTURE-农学，OTHER-其他',
  `description` TEXT NOT NULL COMMENT '问题描述',
  `images` TEXT DEFAULT NULL COMMENT '问题图片（JSON数组，最多3张）',
  `reward` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '悬赏金额（可选，0表示无酬劳）',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，PENDING_ANSWER-待解答，ANSWERED-已回答，SOLVED-已解决，CANCELLED-已取消，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态：PENDING-待审核，APPROVED-已通过，REJECTED-已拒绝',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 采纳信息
  `accepted_answer_id` BIGINT(20) DEFAULT NULL COMMENT '已采纳的回答ID',
  `accept_time` DATETIME DEFAULT NULL COMMENT '采纳时间',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  `answer_count` INT(11) DEFAULT 0 COMMENT '回答数量',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_reward` (`reward`),
  KEY `idx_view_count` (`view_count`),
  KEY `idx_answer_count` (`answer_count`),
  KEY `idx_status_reward` (`status`, `reward` DESC),
  KEY `idx_status_create_time` (`status`, `create_time` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_study_question_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
  CONSTRAINT `fk_study_question_answer` FOREIGN KEY (`accepted_answer_id`) REFERENCES `study_answer` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习问题表';
```

### 3.5.1 学习回答表（study_answer）

```sql
CREATE TABLE `study_answer` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '回答ID',
  `question_id` BIGINT(20) NOT NULL COMMENT '问题ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '回答者ID',
  `content` TEXT NOT NULL COMMENT '回答内容',
  `images` TEXT DEFAULT NULL COMMENT '回答图片（JSON数组，最多3张）',
  `is_accepted` TINYINT(1) DEFAULT 0 COMMENT '是否被采纳：0-未采纳，1-已采纳',
  `accept_time` DATETIME DEFAULT NULL COMMENT '采纳时间',
  
  -- 统计字段
  `like_count` INT(11) DEFAULT 0 COMMENT '点赞数量（可选功能）',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '回答时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_question_id` (`question_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_is_accepted` (`is_accepted`),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_study_answer_question` FOREIGN KEY (`question_id`) REFERENCES `study_question` (`id`),
  CONSTRAINT `fk_study_answer_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习回答表';
```

### 3.6 其他核心表

**（完整的数据库设计包含以下表，具体SQL见数据库操作文档）**

- **订单表（order）**：管理闲置交易订单（详见3.4节）
- **认领记录表（claim_record）**：管理失物认领申请
- **私信会话表（chat_session）**：私信会话管理（需增加 `related_id` 字段关联商品/任务ID）
- **私信消息表（chat_message）**：私信消息记录
- **系统消息表（system_message）**：系统通知
- **敏感词表（sensitive_word）**：敏感词库
- **搜索历史表（search_history）**：搜索历史记录

### 3.7 数据库设计亮点

1. **统计字段**：view_count支持数据展示和排序（失物招领模块不包含收藏和评论统计）
2. **完善的审核机制**：audit_status、audit_trigger_reason支持智能审核
3. **灵活的下架管理**：offshelf_type区分用户自行下架和管理员下架
4. **并发控制**：version字段支持乐观锁，避免并发问题
5. **性能优化**：合理的索引设计，支持多维度查询

### 3.8 MainLayout.vue 数据支持分析

**数据完整性检查：**

| 模块 | 所需字段 | 数据库支持 | 状态 |
|------|---------|-----------|------|
| **失物招领** | id, title, description, image, type, location, time, userAvatar, userName | ✅ 全部支持 | ✅ |
| **闲置交易** | id, title, price, views, image, userAvatar, userName, time, badge | ✅ 全部支持（views=view_count） | ✅ |
| **学习互助** | id, title, description, category, reward, answerCount, userAvatar, userName | ✅ 全部支持 | ✅ |
| **数据统计** | 总数、活跃用户、趋势图 | ✅ 可实时查询 | ✅ |

**结论：** ✅ 数据库设计完全支持 MainLayout.vue 的所有数据展示需求！

---

## 四、开发计划

**第一阶段：基础框架搭建（1周）**
- 后端：Spring Boot 项目初始化，配置 MyBatis-Plus、Redis、MySQL
- 前端：Vue 项目初始化，配置 Element Plus、Pinia、Vue Router
- 数据库设计和创建

**第二阶段：用户管理模块（1周）**
- 用户注册登录（邮箱验证码）
- 用户信息管理
- 实名认证功能（表单、审核）
- 权限验证

**第三阶段：失物招领模块（1周）**
- 失物发布（智能审核）
- 失物浏览搜索
- 认领功能（私信沟通）

**第四阶段：闲置交易模块（1.5周）**
- 商品发布（需实名认证，支持库存、发货方式选择）
- 商品浏览搜索（关键词搜索、搜索历史）
- 订单管理（防止超卖、订单状态流转、卖家改价、买家付款、发货、确认收货）
- 交易流程（邮寄/自提）
- 聊天页面集成订单功能（订单卡片、订单操作）

**第五阶段：学习互助模块（1.5周）**
- 问题发布（无需实名认证）
- 问题浏览搜索
- 回答功能
- 采纳答案功能
- 问题管理

**第六阶段：消息通知模块（1周）**
- 私信功能（自动创建会话）
- 系统消息
- WebSocket 实时推送

**第七阶段：审核和管理模块（1周）**
- 智能审核机制（AC自动机）
- 管理员后台
- 内容管理（下架功能）
- 用户管理（封禁功能）

**第八阶段：搜索功能优化（0.5周）**
- 关键词搜索功能
- 搜索历史功能

**第九阶段：数据统计（0.5周）**
- 数据统计页面
- ECharts 图表

**第十阶段：测试和优化（1周）**
- 功能测试
- 性能优化
- Bug 修复

**总计：约10周**

---

## 五、总结

本文档定义了校园互助系统的核心功能和详细实现方案：

**核心特点：**
1. **功能聚焦**：失物招领、闲置交易、学习互助三大核心功能
2. **权限清晰**：
   - 失物招领：所有用户可用
   - 学习互助：所有登录用户可用（无需实名认证）
   - 闲置交易：需要实名认证
3. **智能审核**：自动审核 + 人工审核，减轻管理负担
4. **简洁易用**：界面简洁，操作便捷
5. **技术成熟**：Spring Boot + Vue + MySQL，开发难度可控

**实名认证权限控制：**
- ⚠️ 未认证用户：只能使用失物招领和学习互助（浏览）
- ✅ 已认证用户：可使用所有功能（闲置交易、学习互助）
- 📚 学习互助：所有登录用户都可以发布和回答问题（无需实名认证）

**下一步工作：**
1. 创建数据库和表
2. 搭建后端项目
3. 搭建前端项目
4. 按阶段逐步开发

---

**文档版本：** v2.1  
**创建日期：** 2024-01-01  
**最近更新：** 2024-01-02  
**文档维护：** 开发团队