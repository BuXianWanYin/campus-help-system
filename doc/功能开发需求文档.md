# 校园互助系统 - 功能开发需求文档

**文档版本：** v2.1  
**创建日期：** 2024-01-01  
**最近更新：** 2024-01-02  
**文档维护：** 开发团队

---

## 📋 版本说明

### v2.1 (2024-01-02) - 数据库完善版
- ✅ 补充完整的数据库表设计（14张核心表）
- ✅ 新增统计字段：view_count、favorite_count、comment_count
- ✅ 完善索引设计，提升查询性能
- ✅ 验证 MainLayout.vue 数据支持完整性
- ✅ 所有功能模块数据库字段完全覆盖

### v2.0 (2024-01-02) - 简化版
- ✅ 简化功能，聚焦核心需求
- ✅ 移除信用分系统（简化评价体系）
- ✅ 保留智能审核机制（自动审核 + 人工审核）
- ✅ 保留管理员内容管理和账号管理功能
- ✅ 明确实名认证权限控制
- ✅ 补充详细的功能实现说明
- ✅ 整合所有文档内容到一个文件

**简化原则：**
- 聚焦失物招领、闲置交易、跑腿服务三大核心功能
- 保留必要的审核和管理功能
- 简化用户权限和评价体系
- 确保系统简洁易用，便于开发和维护

---

## 一、项目概述

### 1.1 项目背景

**国外研究现状：**

在国外，校园互助类数字化服务发展较早，呈现多元化特点。例如：
- **Facebook Campus**：学生可组建学习小组、发布二手物品交易、寻找失物招领线索，依托社交平台快速传播，但功能较为分散，缺乏专门的整合性互助系统设计
- **Nextdoor**：虽以社区为服务范围，但其中针对校园区域的子板块，允许学生发布互助需求（如学习辅导、物品借用），其基于地理位置的信息推送模式，为校园互助系统在精准触达用户方面提供了参考

**国内研究现状：**

在国内，校园互助类系统的研究与应用处于快速发展阶段：
- **校园二手交易平台**：部分高校自主开发了校园二手交易平台，专注于闲置物品的流转，功能涵盖物品发布、在线议价等，解决了校园内闲置资源浪费的问题，但仅聚焦单一场景，未整合失物招领等需求
- **校园失物招领小程序**（如"校园拾物"类工具）：在多个高校推广，用户可快速发布和查询失物信息，部分小程序还支持物品特征拍照识别，但与其他校园服务的联动性不足
- **课程答疑系统**：一些高校平台为学生提供了课程资源共享、学习组队功能，助力学习协作，但覆盖的校园互助场景有限，尚未形成综合化的校园互助生态

**存在问题：**
- 信息分散，缺乏统一平台
- 功能单一，未整合多种互助场景
- 用户体验有待提升

### 1.2 项目目标

本项目旨在设计并实现一个基于 **Spring Boot + Vue** 的校园帮系统，通过该系统实现对**失物招领、闲置交易、跑腿服务**等校园互助场景的统一管理。系统将服务于高校师生，提供一个全面、便捷、安全的校园互助平台，解决传统互助渠道信息分散、效率低的问题。

**核心目标：**
1. 整合校园内各类互助服务，提供统一的信息发布和管理平台
2. 提高校园互助服务的效率和用户体验
3. 实现智能审核机制，保障信息安全
4. 实现实时消息通知，提升用户参与度
5. 提供简洁的管理后台，支持内容和用户管理

### 1.3 技术架构

**后端技术：**
- **Spring Boot 2.7.18**：采用 Spring Boot 搭建项目基础框架，利用其自动化配置、一键启动特性，简化开发流程，缩短项目周期
- **MyBatis-Plus 3.5.3**：数据库操作框架，简化 CRUD 操作
- **Spring Security + JWT**：用户认证和授权
- **Redis**：缓存和会话管理，提升系统性能
- **MySQL 8.0**：选用 MySQL 作为数据库，依托其成熟稳定的特性，保障用户信息、需求数据的安全存储，同时通过索引优化提升数据查询性能
- **WebSocket**：实时消息推送
- **Spring Mail**：邮件通知功能

**前端技术：**
- **Vue 3.x**：Vue能够构建一个响应式的用户界面，其渐进式特性允许按需选择所需功能。轻量级与高效，易于维护
- **Vite**：快速的前端构建工具
- **Element Plus**：UI 组件库，提供丰富的组件
- **Pinia**：状态管理库
- **Vue Router**：路由管理，实现单页应用
- **Axios**：HTTP 请求库
- **WebSocket 客户端**：实时消息接收

**技术可行性分析：**

本系统采用 Spring Boot 作为后端开发框架，搭配 Vue 构建前端界面，以 MySQL 作为数据存储方案。三者均为成熟且广泛应用的技术，学习资料丰富，适配大学生的技术掌握范围。

- **后端**：通过 Spring Boot 可快速搭建项目框架，简化配置流程，高效处理用户管理、需求发布等核心业务逻辑
- **前端**：借助 Vue 的轻量特性，能高效构建符合校园场景的简洁界面，适配 PC 端操作需求
- **数据库**：MySQL 的稳定性可满足系统数据存储需求，支持数据的安全存储与高效查询

整体技术栈无需复杂的技术突破，开发难度可控，适合独立或小团队完成。

---

## 二、功能模块详细说明

### 2.1 用户管理模块

#### 2.1.1 用户注册与登录

**注册功能详细实现：**

1. **注册页面布局**
   - 邮箱输入框（必填）
   - 邮箱验证码输入框（必填）
   - 获取验证码按钮（60秒倒计时）
   - 密码输入框（必填，密码强度提示）
   - 确认密码输入框（必填）
   - 用户协议复选框（必填）
   - 注册按钮

2. **注册流程**
   ```
   用户输入邮箱
       ↓
   点击"获取验证码"
       ↓
   系统验证邮箱格式
       ↓
   系统检查邮箱是否已注册
       ↓
   生成6位数字验证码
       ↓
   发送邮件（使用 Spring Mail）
       ↓
   Redis 存储验证码（有效期5分钟）
       ↓
   用户输入验证码和密码
       ↓
   前端验证：
       - 验证码不能为空
       - 密码长度至少8位
       - 密码必须包含字母和数字
       - 两次密码输入必须一致
       - 必须同意用户协议
       ↓
   提交注册请求
       ↓
   后端验证：
       - 验证码是否正确（从 Redis 获取）
       - 验证码是否过期
       - 邮箱是否已被注册
       - 密码强度是否符合要求
       ↓
   密码加密（BCrypt）
       ↓
   保存用户信息到数据库
       ↓
   返回注册成功
       ↓
   自动跳转到登录页面
   ```

3. **后端实现示例**
   ```java
   @Service
   public class UserService {
       
       @Autowired
       private RedisTemplate<String, String> redisTemplate;
       
       @Autowired
       private JavaMailSender mailSender;
       
       @Autowired
       private UserMapper userMapper;
       
       /**
        * 发送邮箱验证码
        */
       public void sendEmailCode(String email) {
           // 1. 验证邮箱格式
           if (!email.matches("^[A-Za-z0-9+_.-]+@(.+)$")) {
               throw new BusinessException("邮箱格式不正确");
           }
           
           // 2. 检查邮箱是否已注册
           User existUser = userMapper.selectByEmail(email);
           if (existUser != null) {
               throw new BusinessException("该邮箱已被注册");
           }
           
           // 3. 生成6位数字验证码
           String code = String.format("%06d", new Random().nextInt(999999));
           
           // 4. 存储到 Redis（有效期5分钟）
           String key = "email:code:" + email;
           redisTemplate.opsForValue().set(key, code, 5, TimeUnit.MINUTES);
           
           // 5. 发送邮件
           SimpleMailMessage message = new SimpleMailMessage();
           message.setTo(email);
           message.setSubject("校园互助系统 - 注册验证码");
           message.setText("您的验证码是：" + code + "，有效期5分钟，请勿泄露给他人。");
           mailSender.send(message);
       }
       
       /**
        * 用户注册
        */
       @Transactional
       public void register(RegisterDTO dto) {
           // 1. 验证验证码
           String key = "email:code:" + dto.getEmail();
           String cachedCode = redisTemplate.opsForValue().get(key);
           if (cachedCode == null) {
               throw new BusinessException("验证码已过期，请重新获取");
           }
           if (!cachedCode.equals(dto.getCode())) {
               throw new BusinessException("验证码错误");
           }
           
           // 2. 检查邮箱是否已注册
           User existUser = userMapper.selectByEmail(dto.getEmail());
           if (existUser != null) {
               throw new BusinessException("该邮箱已被注册");
           }
           
           // 3. 验证密码强度
           if (!dto.getPassword().matches("^(?=.*[A-Za-z])(?=.*\\d)[A-Za-z\\d]{8,}$")) {
               throw new BusinessException("密码必须至少8位，包含字母和数字");
           }
           
           // 4. 创建用户
           User user = new User();
           user.setEmail(dto.getEmail());
           user.setPassword(BCrypt.hashpw(dto.getPassword(), BCrypt.gensalt()));
           user.setNickname("用户" + System.currentTimeMillis());
           user.setRole("USER");
           user.setStatus(1);
           user.setCreateTime(new Date());
           
           // 5. 保存到数据库
           userMapper.insert(user);
           
           // 6. 删除验证码
           redisTemplate.delete(key);
       }
   }
   ```

**登录功能详细实现：**

1. **登录页面布局**
   - Tab 切换：密码登录 / 验证码登录
   - 邮箱输入框
   - 密码输入框 / 验证码输入框
   - 记住我复选框
   - 登录按钮
   - 忘记密码链接
   - 注册链接

2. **登录流程（密码登录）**
   ```
   用户输入邮箱和密码
       ↓
   前端验证：
       - 邮箱不能为空
       - 密码不能为空
       ↓
   提交登录请求
       ↓
   后端验证：
       - 查询用户是否存在
       - 验证密码是否正确
       - 检查账号状态（是否被封禁）
       ↓
   生成 JWT Token
       ↓
   Token 存储到 Redis（有效期7天）
       ↓
   返回 Token 和用户信息
       ↓
   前端保存 Token 到 localStorage
       ↓
   跳转到首页
   ```

3. **后端实现示例**
   ```java
   @Service
   public class AuthService {
       
       @Autowired
       private UserMapper userMapper;
       
       @Autowired
       private RedisTemplate<String, String> redisTemplate;
       
       @Autowired
       private JwtUtil jwtUtil;
       
       /**
        * 密码登录
        */
       public LoginVO login(LoginDTO dto) {
           // 1. 查询用户
           User user = userMapper.selectByEmail(dto.getEmail());
           if (user == null) {
               throw new BusinessException("用户不存在");
           }
           
           // 2. 验证密码
           if (!BCrypt.checkpw(dto.getPassword(), user.getPassword())) {
               throw new BusinessException("密码错误");
           }
           
           // 3. 检查账号状态
           if (user.getStatus() == 2) {
               throw new BusinessException("账号已被封禁");
           }
           
           // 4. 生成 JWT Token
           String token = jwtUtil.generateToken(user.getId(), user.getEmail(), user.getRole());
           
           // 5. 存储 Token 到 Redis（7天）
           String key = "token:" + user.getId();
           redisTemplate.opsForValue().set(key, token, 7, TimeUnit.DAYS);
           
           // 6. 返回登录信息
           LoginVO vo = new LoginVO();
           vo.setToken(token);
           vo.setUserId(user.getId());
           vo.setEmail(user.getEmail());
           vo.setNickname(user.getNickname());
           vo.setAvatar(user.getAvatar());
           vo.setIsVerified(user.getIsVerified());
           vo.setRole(user.getRole());
           
           return vo;
       }
   }
   ```

#### 2.1.2 用户信息管理

**基本信息编辑：**
- 昵称、头像、性别、年级、专业、手机号
- 实时保存，无需点击保存按钮
- 头像上传支持裁剪功能

#### 2.1.3 实名认证（可选，但跑腿和交易必须认证）

**实名认证的重要性：**
- ⚠️ **未认证用户只能使用失物招领功能**
- ✅ **实名认证后才能发布和参与闲置交易**
- ✅ **实名认证后才能发布和接单跑腿任务**

**认证页面设计：**

1. **未认证状态（显示认证表单）**
   
   **页面提示：**
   ```
   📌 实名认证说明
   - 实名认证后可以发布闲置商品和跑腿任务
   - 实名认证后可以参与商品交易和接单跑腿
   - 未认证用户只能使用失物招领功能
   - 认证信息仅用于平台安全管理，不会泄露给其他用户
   - 提交后，管理员将在1-3个工作日内完成审核
   ```
   
   **认证表单：**
   - 真实姓名（必填，2-10个中文字符）
   - 身份证号（必填，18位）
   - 学号/工号（必填，6-20个字符）
   - 用户类型（必填，单选：学生/教师）
   - 证明文件上传（必填）：
     - 学生：学生证照片、校园卡照片、教务系统截图
     - 教师：教师证照片、工作证照片、校园系统截图
     - 要求：至少1张，最多3张，jpg/jpeg/png，单张不超过5MB

2. **认证中状态（显示审核进度）**
   ```
   ⏳ 您的实名认证正在审核中
   - 提交时间：2024-01-02 14:30:25
   - 预计审核时间：1-3个工作日
   - 请耐心等待管理员审核
   - 审核结果将通过站内消息和邮件通知您
   ```

3. **已认证状态（显示认证信息）**
   ```
   ✅ 恭喜您，实名认证已通过！
   - 认证时间：2024-01-02 16:30:25
   - 您现在可以发布闲置商品和跑腿任务
   - 您现在可以参与商品交易和接单跑腿
   ```

4. **已拒绝状态（显示拒绝原因）**
   ```
   ❌ 您的实名认证未通过审核
   - 审核时间：2024-01-02 16:30:25
   - 拒绝原因：上传的证明文件不清晰，请重新上传高清照片
   - 您可以修改信息后重新提交认证
   ```

**认证实现示例：**
```java
@Service
public class VerificationService {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 提交实名认证
     */
    @Transactional
    public void submitVerification(VerificationDTO dto, Long userId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 检查是否已认证
        if (user.getIsVerified() == 1) {
            throw new BusinessException("您已完成实名认证");
        }
        
        // 3. 检查是否有待审核的认证
        if ("PENDING".equals(user.getVerificationStatus())) {
            throw new BusinessException("您的认证正在审核中，请勿重复提交");
        }
        
        // 4. 更新用户信息
        user.setRealName(dto.getRealName());
        user.setIdCard(dto.getIdCard());
        user.setStudentId(dto.getStudentId());
        user.setUserType(dto.getUserType());
        user.setVerificationProof(JSON.toJSONString(dto.getProofImages()));
        user.setVerificationStatus("PENDING");
        user.setVerificationSubmitTime(new Date());
        
        // 5. 保存到数据库
        userMapper.updateById(user);
        
        // 6. 发送通知给管理员（WebSocket）
        // ...
    }
    
    /**
     * 管理员审核实名认证
     */
    @Transactional
    public void auditVerification(Long userId, Boolean approved, String reason, Long adminId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 检查认证状态
        if (!"PENDING".equals(user.getVerificationStatus())) {
            throw new BusinessException("该认证不是待审核状态");
        }
        
        if (approved) {
            // 审核通过
            user.setIsVerified(1);
            user.setVerificationStatus("VERIFIED");
            user.setVerificationAuditTime(new Date());
            user.setVerificationAuditAdminId(adminId);
            
            // 发送通知给用户
            sendNotification(userId, "实名认证已通过", "恭喜您，实名认证已通过！您现在可以发布闲置商品和跑腿任务了。");
        } else {
            // 审核拒绝
            user.setVerificationStatus("REJECTED");
            user.setVerificationAuditTime(new Date());
            user.setVerificationAuditReason(reason);
            user.setVerificationAuditAdminId(adminId);
            
            // 发送通知给用户
            sendNotification(userId, "实名认证未通过", "很抱歉，您的实名认证未通过审核。拒绝原因：" + reason);
        }
        
        // 3. 保存到数据库
        userMapper.updateById(user);
    }
}
```

#### 2.1.4 用户权限说明

**权限矩阵：**

| 功能 | 未认证用户 | 已认证用户 | 管理员 |
|------|-----------|-----------|--------|
| 注册登录 | ✅ | ✅ | ✅ |
| **失物招领** |  |  |  |
| - 浏览失物信息 | ✅ | ✅ | ✅ |
| - 发布失物信息 | ✅ | ✅ | ✅ |
| - 认领失物 | ✅ | ✅ | ✅ |
| **闲置交易** |  |  |  |
| - 浏览商品 | ✅ | ✅ | ✅ |
| - 发布商品 | ❌ **需要实名认证** | ✅ | ✅ |
| - 购买商品 | ❌ **需要实名认证** | ✅ | ✅ |
| **跑腿服务** |  |  |  |
| - 浏览任务 | ✅ | ✅ | ✅ |
| - 发布任务 | ❌ **需要实名认证** | ✅ | ✅ |
| - 接单任务 | ❌ **需要实名认证** | ✅ | ✅ |
| **其他功能** |  |  |  |
| - 评论收藏 | ✅ | ✅ | ✅ |
| - 私信沟通 | ✅ | ✅ | ✅ |
| - 内容审核 | ❌ | ❌ | ✅ |
| - 用户管理 | ❌ | ❌ | ✅ |

**权限验证实现：**
```java
@Aspect
@Component
public class VerificationAspect {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 需要实名认证的接口
     */
    @Around("@annotation(requireVerification)")
    public Object checkVerification(ProceedingJoinPoint joinPoint, RequireVerification requireVerification) throws Throwable {
        // 1. 获取当前用户
        Long userId = SecurityUtils.getCurrentUserId();
        User user = userMapper.selectById(userId);
        
        // 2. 检查是否已认证
        if (user.getIsVerified() != 1) {
            throw new BusinessException("该功能需要实名认证，请先完成实名认证");
        }
        
        // 3. 执行原方法
        return joinPoint.proceed();
    }
}

// 使用注解
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 发布商品（需要实名认证）
     */
    @PostMapping("/publish")
    @RequireVerification
    public Result publishGoods(@RequestBody GoodsDTO dto) {
        // ...
    }
}
```

---

### 2.2 失物招领模块

#### 2.2.1 失物发布（详细实现）

**发布页面：** `/lost-found/publish`

**表单设计：**
1. 物品名称（必填，单行输入框，最多50字）
2. 物品分类（必填，下拉选择）
   - 证件类、电子产品、生活用品、书籍、其他
3. 物品描述（必填，多行文本框，最少10字，最多500字）
4. 丢失时间（必填，日期时间选择器）
5. 丢失地点（必填，单行输入框，最多100字）
6. 联系方式（可选，单行输入框，默认使用私信）
7. 物品图片（可选，图片上传组件）
   - 最多上传9张
   - 支持拖拽上传
   - 支持图片预览和删除
   - 图片压缩（前端）
8. 悬赏金额（可选，数字输入框，0-10000元）

**发布流程：**
```
用户填写失物信息
    ↓
前端验证：
    - 必填项不能为空
    - 物品描述至少10字
    - 图片格式和大小检查
    - 悬赏金额不能为负数
    ↓
上传图片到服务器
    ↓
提交发布请求
    ↓
后端验证：
    - 数据格式验证（@Validated）
    - 敏感词检测
    - 发布频率检测
    - 用户信用检测
    ↓
判断审核结果：
    ├─ 自动审核通过 → 状态设置为"待认领"，立即发布
    └─ 转人工审核 → 状态设置为"待审核"，进入审核队列
    ↓
保存到数据库
    ↓
返回发布成功
    ↓
跳转到详情页 / 我的发布页
```

**后端实现：**
```java
@Service
public class LostFoundService {
    
    @Autowired
    private LostFoundMapper lostFoundMapper;
    
    @Autowired
    private SensitiveWordService sensitiveWordService;
    
    @Autowired
    private PublishFrequencyService publishFrequencyService;
    
    /**
     * 发布失物
     */
    @Transactional
    public Long publishLostFound(LostFoundDTO dto, Long userId) {
        // 1. 敏感词检测
        SensitiveWordCheckResult checkResult = sensitiveWordService.check(dto.getTitle() + " " + dto.getDescription());
        
        // 2. 发布频率检测
        boolean frequencyOk = publishFrequencyService.checkFrequency(userId, "LOST_FOUND");
        
        // 3. 用户信用检测（新注册用户7天内）
        User user = userMapper.selectById(userId);
        boolean newUser = Duration.between(user.getCreateTime().toInstant(), Instant.now()).toDays() < 7;
        
        // 4. 创建失物信息
        LostFound lostFound = new LostFound();
        BeanUtils.copyProperties(dto, lostFound);
        lostFound.setUserId(userId);
        lostFound.setImages(JSON.toJSONString(dto.getImages()));
        
        // 5. 判断审核状态
        if (checkResult.isPass() && frequencyOk && !newUser) {
            // 自动审核通过
            lostFound.setStatus("PENDING_CLAIM");
            lostFound.setAuditStatus("APPROVED");
            lostFound.setAuditTime(new Date());
        } else {
            // 转人工审核
            lostFound.setStatus("PENDING_REVIEW");
            lostFound.setAuditStatus("PENDING");
            
            // 记录触发原因
            String reason = "";
            if (!checkResult.isPass()) reason += "包含敏感词；";
            if (!frequencyOk) reason += "发布频繁；";
            if (newUser) reason += "新注册用户；";
            // 可以保存到日志表
        }
        
        lostFound.setCreateTime(new Date());
        
        // 6. 保存到数据库
        lostFoundMapper.insert(lostFound);
        
        // 7. 如果需要人工审核，发送通知给管理员
        if ("PENDING_REVIEW".equals(lostFound.getStatus())) {
            // 通知管理员审核
            notifyAdminForReview("LOST_FOUND", lostFound.getId());
        }
        
        return lostFound.getId();
    }
}
```

#### 2.2.2 失物浏览与搜索（详细实现）

**列表页：** `/lost-found/list`

**页面布局：**
```
┌─────────────────────────────────────────────────────────┐
│  [搜索框]  [搜索按钮]                    [发布失物按钮]  │
├─────────────────────────────────────────────────────────┤
│  分类筛选: [全部] [证件类] [电子产品] [生活用品] ...    │
│  状态筛选: [全部] [待认领] [认领中] [已认领] [已关闭]   │
│  地点筛选: [全部] [教学楼] [宿舍] [食堂] ...           │
│  排序方式: [最新] [浏览最多] [悬赏最高]                 │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [图片]  物品名称                               │   │
│  │          分类 | 丢失地点 | 发布时间             │   │
│  │          物品描述...                            │   │
│  │          悬赏: ¥50  浏览: 128次                │   │
│  │          [查看详情]                             │   │
│  └─────────────────────────────────────────────────┘   │
│  ...                                                    │
│  [分页器]                                              │
└─────────────────────────────────────────────────────────┘
```

**搜索实现：**
```java
@Service
public class LostFoundService {
    
    /**
     * 搜索失物列表
     */
    public PageResult<LostFoundVO> searchLostFound(LostFoundSearchDTO dto) {
        // 1. 构建查询条件
        Page<LostFound> page = new Page<>(dto.getPageNum(), dto.getPageSize());
        QueryWrapper<LostFound> wrapper = new QueryWrapper<>();
        
        // 关键词搜索（标题或描述）
        if (StringUtils.isNotBlank(dto.getKeyword())) {
            wrapper.and(w -> w.like("title", dto.getKeyword())
                              .or()
                              .like("description", dto.getKeyword()));
        }
        
        // 分类筛选
        if (StringUtils.isNotBlank(dto.getCategory())) {
            wrapper.eq("category", dto.getCategory());
        }
        
        // 状态筛选
        if (StringUtils.isNotBlank(dto.getStatus())) {
            wrapper.eq("status", dto.getStatus());
        } else {
            // 默认只显示待认领、认领中、已认领的
            wrapper.in("status", "PENDING_CLAIM", "CLAIMING", "CLAIMED");
        }
        
        // 地点筛选
        if (StringUtils.isNotBlank(dto.getLocation())) {
            wrapper.like("lost_location", dto.getLocation());
        }
        
        // 悬赏筛选
        if (dto.getHasReward() != null && dto.getHasReward()) {
            wrapper.gt("reward", 0);
        }
        
        // 不显示已删除的
        wrapper.eq("delete_flag", 0);
        
        // 排序
        if ("view".equals(dto.getSortBy())) {
            wrapper.orderByDesc("view_count");
        } else if ("reward".equals(dto.getSortBy())) {
            wrapper.orderByDesc("reward");
        } else {
            wrapper.orderByDesc("create_time");
        }
        
        // 2. 查询
        Page<LostFound> result = lostFoundMapper.selectPage(page, wrapper);
        
        // 3. 转换为 VO
        List<LostFoundVO> voList = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());
        
        return new PageResult<>(voList, result.getTotal(), result.getCurrent(), result.getSize());
    }
    
    /**
     * 转换为 VO
     */
    private LostFoundVO convertToVO(LostFound lostFound) {
        LostFoundVO vo = new LostFoundVO();
        BeanUtils.copyProperties(lostFound, vo);
        
        // 解析图片
        if (StringUtils.isNotBlank(lostFound.getImages())) {
            vo.setImageList(JSON.parseArray(lostFound.getImages(), String.class));
        }
        
        // 获取发布者信息
        User user = userMapper.selectById(lostFound.getUserId());
        vo.setPublisherNickname(user.getNickname());
        vo.setPublisherAvatar(user.getAvatar());
        vo.setPublisherIsVerified(user.getIsVerified());
        
        // 格式化时间
        vo.setCreateTimeStr(formatTime(lostFound.getCreateTime()));
        
        return vo;
    }
}
```

#### 2.2.3 认领功能（详细实现）

**认领流程完整实现：**

1. **用户查看详情，点击"申请认领"**
   - 弹出认领申请对话框

2. **填写认领信息**
   ```vue
   <el-dialog title="申请认领" v-model="claimDialogVisible">
     <el-form :model="claimForm" :rules="claimRules" ref="claimFormRef">
       <el-form-item label="物品特征描述" prop="description">
         <el-input
           type="textarea"
           v-model="claimForm.description"
           placeholder="请详细描述物品的特征，以便发布者确认"
           :rows="4"
           maxlength="200"
           show-word-limit
         />
       </el-form-item>
       
       <el-form-item label="丢失时间" prop="lostTime">
         <el-date-picker
           v-model="claimForm.lostTime"
           type="datetime"
           placeholder="您记得的丢失时间"
         />
       </el-form-item>
       
       <el-form-item label="其他信息" prop="otherInfo">
         <el-input
           type="textarea"
           v-model="claimForm.otherInfo"
           placeholder="其他能证明物品归属的信息（可选）"
           :rows="2"
         />
       </el-form-item>
       
       <el-form-item label="证明文件" prop="proofImages">
         <el-upload
           :action="uploadUrl"
           :headers="uploadHeaders"
           :file-list="claimForm.proofImages"
           :on-success="handleUploadSuccess"
           :before-upload="beforeUpload"
           :limit="3"
           list-type="picture-card"
         >
           <el-icon><Plus /></el-icon>
         </el-upload>
         <div class="upload-tip">最多上传3张证明文件（可选）</div>
       </el-form-item>
     </el-form>
     
     <template #footer>
       <el-button @click="claimDialogVisible = false">取消</el-button>
       <el-button type="primary" @click="submitClaim" :loading="submitting">
         提交认领
       </el-button>
     </template>
   </el-dialog>
   ```

3. **后端处理认领申请**
   ```java
   @Service
   public class ClaimService {
       
       @Autowired
       private LostFoundMapper lostFoundMapper;
       
       @Autowired
       private ClaimRecordMapper claimRecordMapper;
       
       @Autowired
       private ChatSessionService chatSessionService;
       
       /**
        * 申请认领
        */
       @Transactional
       public void applyClaim(ClaimDTO dto, Long userId) {
           // 1. 查询失物信息
           LostFound lostFound = lostFoundMapper.selectById(dto.getLostFoundId());
           if (lostFound == null) {
               throw new BusinessException("失物信息不存在");
           }
           
           // 2. 验证状态
           if (!"PENDING_CLAIM".equals(lostFound.getStatus()) && !"CLAIMING".equals(lostFound.getStatus())) {
               throw new BusinessException("该失物当前不可认领");
           }
           
           // 3. 不能认领自己发布的
           if (lostFound.getUserId().equals(userId)) {
               throw new BusinessException("不能认领自己发布的失物");
           }
           
           // 4. 检查是否已经申请过
           QueryWrapper<ClaimRecord> wrapper = new QueryWrapper<>();
           wrapper.eq("lost_found_id", dto.getLostFoundId())
                  .eq("claimer_id", userId);
           ClaimRecord existRecord = claimRecordMapper.selectOne(wrapper);
           if (existRecord != null) {
               throw new BusinessException("您已申请认领过该失物");
           }
           
           // 5. 使用乐观锁更新失物状态
           int rows = lostFoundMapper.updateStatusWithVersion(
               dto.getLostFoundId(),
               "PENDING_CLAIM",
               "CLAIMING",
               lostFound.getVersion()
           );
           
           if (rows == 0) {
               throw new BusinessException("认领失败，请重试");
           }
           
           // 6. 创建认领记录
           ClaimRecord record = new ClaimRecord();
           record.setLostFoundId(dto.getLostFoundId());
           record.setClaimerId(userId);
           record.setDescription(dto.getDescription());
           record.setLostTime(dto.getLostTime());
           record.setOtherInfo(dto.getOtherInfo());
           record.setProofImages(JSON.toJSONString(dto.getProofImages()));
           record.setStatus("PENDING"); // 待确认
           record.setCreateTime(new Date());
           claimRecordMapper.insert(record);
           
           // 7. 创建私信会话
           chatSessionService.createSession(userId, lostFound.getUserId(), "LOST_FOUND", lostFound.getId());
           
           // 8. 发送通知给发布者
           notifyPublisher(lostFound.getUserId(), "收到认领申请", 
               "您发布的失物《" + lostFound.getTitle() + "》收到了认领申请，请及时查看");
       }
       
       /**
        * 确认认领
        */
       @Transactional
       public void confirmClaim(Long claimRecordId, Long userId) {
           // 1. 查询认领记录
           ClaimRecord record = claimRecordMapper.selectById(claimRecordId);
           if (record == null) {
               throw new BusinessException("认领记录不存在");
           }
           
           // 2. 查询失物信息
           LostFound lostFound = lostFoundMapper.selectById(record.getLostFoundId());
           if (lostFound == null) {
               throw new BusinessException("失物信息不存在");
           }
           
           // 3. 验证权限（只有发布者可以确认）
           if (!lostFound.getUserId().equals(userId)) {
               throw new BusinessException("无权操作");
           }
           
           // 4. 验证状态
           if (!"PENDING".equals(record.getStatus())) {
               throw new BusinessException("该认领已处理");
           }
           
           // 5. 更新认领记录
           record.setStatus("CONFIRMED");
           record.setConfirmTime(new Date());
           claimRecordMapper.updateById(record);
           
           // 6. 更新失物状态
           lostFound.setStatus("CLAIMED");
           lostFoundMapper.updateById(lostFound);
           
           // 7. 发送通知给认领者
           notifyUser(record.getClaimerId(), "认领成功", 
               "您认领的失物《" + lostFound.getTitle() + "》已被确认，请联系发布者取回物品");
       }
       
       /**
        * 拒绝认领
        */
       @Transactional
       public void rejectClaim(Long claimRecordId, String reason, Long userId) {
           // 类似确认认领的流程
           // ...
           
           // 更新认领记录为拒绝
           record.setStatus("REJECTED");
           record.setRejectReason(reason);
           record.setRejectTime(new Date());
           claimRecordMapper.updateById(record);
           
           // 失物状态恢复为待认领
           lostFound.setStatus("PENDING_CLAIM");
           lostFoundMapper.updateById(lostFound);
           
           // 发送通知
           notifyUser(record.getClaimerId(), "认领被拒绝", 
               "很抱歉，您认领的失物《" + lostFound.getTitle() + "》被拒绝。原因：" + reason);
       }
   }
   ```

---

### 2.3 闲置交易模块

#### 2.3.1 商品发布（需要实名认证）

**权限验证：**
```java
@RestController
@RequestMapping("/api/goods")
public class GoodsController {
    
    /**
     * 发布商品（需要实名认证）
     */
    @PostMapping("/publish")
    @RequireVerification // 自定义注解，验证实名认证
    public Result publishGoods(@RequestBody @Validated GoodsDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long goodsId = goodsService.publishGoods(dto, userId);
        return Result.success(goodsId);
    }
}
```

**发布流程同失物招领，额外增加：**
- 原价和现价验证（现价不能高于原价）
- 商品成色必选
- 至少上传1张图片

#### 2.3.2 交易流程（需要实名认证）

**买家购买流程：**
```
买家浏览商品
    ↓
点击"立即购买"
    ↓
验证实名认证（未认证提示"该功能需要实名认证"）
    ↓
通过私信咨询卖家
    ↓
双方协商价格和交易方式
    ↓
买家创建订单
    ↓
选择交易方式：
    ├─ 邮寄 → 填写收货地址 → 支付（模拟） → 卖家发货 → 买家确认收货
    └─ 面交 → 填写联系方式 → 约定面交 → 买家确认完成
    ↓
双方评价
```

**订单创建实现：**
```java
@Service
public class OrderService {
    
    /**
     * 创建订单
     */
    @Transactional
    public Long createOrder(OrderDTO dto, Long userId) {
        // 1. 验证实名认证
        User user = userMapper.selectById(userId);
        if (user.getIsVerified() != 1) {
            throw new BusinessException("该功能需要实名认证，请先完成实名认证");
        }
        
        // 2. 查询商品
        Goods goods = goodsMapper.selectById(dto.getGoodsId());
        if (goods == null) {
            throw new BusinessException("商品不存在");
        }
        
        // 3. 验证商品状态
        if (!"ON_SALE".equals(goods.getStatus())) {
            throw new BusinessException("商品已下架或已售出");
        }
        
        // 4. 不能购买自己的商品
        if (goods.getUserId().equals(userId)) {
            throw new BusinessException("不能购买自己的商品");
        }
        
        // 5. 使用乐观锁更新商品状态
        int rows = goodsMapper.updateStatusWithVersion(
            dto.getGoodsId(),
            "ON_SALE",
            "SOLD_OUT",
            goods.getVersion()
        );
        
        if (rows == 0) {
            throw new BusinessException("商品已被其他买家购买");
        }
        
        // 6. 创建订单
        Order order = new Order();
        order.setGoodsId(dto.getGoodsId());
        order.setBuyerId(userId);
        order.setSellerId(goods.getUserId());
        order.setPrice(goods.getCurrentPrice());
        order.setTradeMethod(dto.getTradeMethod());
        order.setReceiverAddress(dto.getReceiverAddress());
        order.setReceiverPhone(dto.getReceiverPhone());
        
        if ("MAIL".equals(dto.getTradeMethod())) {
            order.setStatus("PENDING_PAYMENT");
        } else {
            order.setStatus("PENDING_SHIPMENT");
        }
        
        order.setCreateTime(new Date());
        orderMapper.insert(order);
        
        // 7. 创建私信会话
        chatSessionService.createSession(userId, goods.getUserId(), "GOODS", goods.getId());
        
        // 8. 发送通知给卖家
        notifyUser(goods.getUserId(), "收到新订单", 
            "您的商品《" + goods.getTitle() + "》被购买了，请及时处理");
        
        return order.getId();
    }
}
```

---

### 2.4 跑腿服务模块

#### 2.4.1 任务发布（需要实名认证）

**权限验证：**
```java
@RestController
@RequestMapping("/api/task")
public class TaskController {
    
    /**
     * 发布任务（需要实名认证）
     */
    @PostMapping("/publish")
    @RequireVerification // 验证实名认证
    public Result publishTask(@RequestBody @Validated TaskDTO dto) {
        Long userId = SecurityUtils.getCurrentUserId();
        Long taskId = taskService.publishTask(dto, userId);
        return Result.success(taskId);
    }
}
```

#### 2.4.2 接单流程（需要实名认证）

**接单流程：**
```
用户浏览任务
    ↓
点击"接单"
    ↓
验证实名认证（未认证提示"接单需要实名认证"）
    ↓
确认接单
    ↓
系统创建私信会话
    ↓
双方沟通任务细节
    ↓
接单用户开始任务
    ↓
更新任务进度
    ↓
完成任务，上传完成凭证
    ↓
发布者确认完成
    ↓
系统处理报酬支付（模拟）
    ↓
双方评价
```

**接单实现：**
```java
@Service
public class TaskService {
    
    /**
     * 接单
     */
    @Transactional
    public void acceptTask(Long taskId, Long userId) {
        // 1. 验证实名认证
        User user = userMapper.selectById(userId);
        if (user.getIsVerified() != 1) {
            throw new BusinessException("接单需要实名认证，请先完成实名认证");
        }
        
        // 2. 查询任务
        ErrandTask task = taskMapper.selectById(taskId);
        if (task == null) {
            throw new BusinessException("任务不存在");
        }
        
        // 3. 验证任务状态
        if (!"PENDING_ACCEPT".equals(task.getStatus())) {
            throw new BusinessException("该任务当前不可接单");
        }
        
        // 4. 不能接自己的任务
        if (task.getUserId().equals(userId)) {
            throw new BusinessException("不能接自己发布的任务");
        }
        
        // 5. 使用乐观锁更新任务状态
        int rows = taskMapper.updateStatusWithVersion(
            taskId,
            "PENDING_ACCEPT",
            "ACCEPTED",
            task.getVersion()
        );
        
        if (rows == 0) {
            throw new BusinessException("接单失败，任务已被其他人接单");
        }
        
        // 6. 更新任务信息
        task.setAccepterId(userId);
        task.setAcceptTime(new Date());
        taskMapper.updateById(task);
        
        // 7. 创建私信会话
        chatSessionService.createSession(userId, task.getUserId(), "TASK", taskId);
        
        // 8. 发送通知给发布者
        notifyUser(task.getUserId(), "任务已被接单", 
            "您发布的任务《" + task.getTitle() + "》已被接单，请及时联系接单用户");
    }
}
```

---

### 2.5 消息通知模块

#### 2.5.1 私信功能（详细实现）

**私信列表页：** `/message/chat`

**页面布局：**
```
┌────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌─────────────────────────────┐ │
│  │ 会话列表     │  │ 聊天内容                    │ │
│  ├──────────────┤  ├─────────────────────────────┤ │
│  │ [头像] 用户A │  │ 对方: 你好                  │ │
│  │    最后消息  │  │ 我: 你好，请问...          │ │
│  │    时间      │  │ 对方: [图片]                │ │
│  ├──────────────┤  │ ...                         │ │
│  │ [头像] 用户B │  ├─────────────────────────────┤ │
│  │    最后消息  │  │ [输入框]                    │ │
│  │    时间      │  │ [图片] [发送]               │ │
│  └──────────────┘  └─────────────────────────────┘ │
└────────────────────────────────────────────────────┘
```

**自动创建私信会话：**
```java
@Service
public class ChatSessionService {
    
    /**
     * 创建会话（如果不存在）
     */
    @Transactional
    public Long createSession(Long userId1, Long userId2, String relatedType, Long relatedId) {
        // 1. 查询是否已存在会话
        QueryWrapper<ChatSession> wrapper = new QueryWrapper<>();
        wrapper.and(w -> w.eq("user1_id", userId1).eq("user2_id", userId2)
                       .or()
                       .eq("user1_id", userId2).eq("user2_id", userId1));
        wrapper.eq("related_type", relatedType);
        wrapper.eq("related_id", relatedId);
        
        ChatSession session = sessionMapper.selectOne(wrapper);
        
        if (session != null) {
            return session.getId();
        }
        
        // 2. 创建新会话
        session = new ChatSession();
        session.setUser1Id(userId1);
        session.setUser2Id(userId2);
        session.setRelatedType(relatedType);
        session.setRelatedId(relatedId);
        session.setCreateTime(new Date());
        sessionMapper.insert(session);
        
        return session.getId();
    }
    
    /**
     * 发送消息
     */
    @Transactional
    public void sendMessage(SendMessageDTO dto, Long senderId) {
        // 1. 验证会话是否存在
        ChatSession session = sessionMapper.selectById(dto.getSessionId());
        if (session == null) {
            throw new BusinessException("会话不存在");
        }
        
        // 2. 验证发送者是会话参与者
        if (!session.getUser1Id().equals(senderId) && !session.getUser2Id().equals(senderId)) {
            throw new BusinessException("无权发送消息");
        }
        
        // 3. 确定接收者
        Long receiverId = session.getUser1Id().equals(senderId) ? session.getUser2Id() : session.getUser1Id();
        
        // 4. 敏感词检测（如果是文字消息）
        if ("TEXT".equals(dto.getMessageType())) {
            SensitiveWordCheckResult checkResult = sensitiveWordService.check(dto.getContent());
            if (!checkResult.isPass()) {
                throw new BusinessException("消息包含敏感词，请修改后发送");
            }
        }
        
        // 5. 创建消息
        ChatMessage message = new ChatMessage();
        message.setSessionId(dto.getSessionId());
        message.setSenderId(senderId);
        message.setReceiverId(receiverId);
        message.setMessageType(dto.getMessageType());
        message.setContent(dto.getContent());
        message.setImages(JSON.toJSONString(dto.getImages()));
        message.setIsRead(0);
        message.setCreateTime(new Date());
        messageMapper.insert(message);
        
        // 6. 更新会话的最后消息时间
        session.setLastMessageTime(new Date());
        session.setLastMessageContent(dto.getContent());
        sessionMapper.updateById(session);
        
        // 7. WebSocket 实时推送给接收者
        webSocketService.sendMessageToUser(receiverId, message);
    }
}
```

#### 2.5.2 WebSocket 实时推送（详细实现）

**后端 WebSocket 配置：**
```java
@Configuration
@EnableWebSocket
public class WebSocketConfig implements WebSocketConfigurer {
    
    @Autowired
    private WebSocketHandler webSocketHandler;
    
    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(webSocketHandler, "/ws")
                .setAllowedOrigins("*")
                .addInterceptors(new WebSocketInterceptor());
    }
}

@Component
public class WebSocketHandler extends TextWebSocketHandler {
    
    // 存储用户ID和WebSocket会话的映射
    private static final Map<Long, WebSocketSession> USER_SESSIONS = new ConcurrentHashMap<>();
    
    @Override
    public void afterConnectionEstablished(WebSocketSession session) throws Exception {
        // 从URL参数获取token
        String token = getTokenFromSession(session);
        Long userId = jwtUtil.getUserIdFromToken(token);
        
        // 保存会话
        USER_SESSIONS.put(userId, session);
        
        System.out.println("用户 " + userId + " WebSocket连接成功");
    }
    
    @Override
    public void afterConnectionClosed(WebSocketSession session, CloseStatus status) throws Exception {
        // 移除会话
        Long userId = getUserIdFromSession(session);
        USER_SESSIONS.remove(userId);
        
        System.out.println("用户 " + userId + " WebSocket连接关闭");
    }
    
    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) throws Exception {
        // 处理客户端发送的消息（心跳等）
        String payload = message.getPayload();
        if ("ping".equals(payload)) {
            session.sendMessage(new TextMessage("pong"));
        }
    }
    
    /**
     * 发送消息给指定用户
     */
    public void sendMessageToUser(Long userId, Object message) {
        WebSocketSession session = USER_SESSIONS.get(userId);
        if (session != null && session.isOpen()) {
            try {
                String json = JSON.toJSONString(message);
                session.sendMessage(new TextMessage(json));
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }
}
```

**前端 WebSocket 连接：**
```javascript
// WebSocket 连接管理
class WebSocketManager {
  constructor() {
    this.ws = null
    this.reconnectTimer = null
    this.heartbeatTimer = null
    this.messageHandlers = []
  }
  
  // 连接
  connect(token) {
    const wsUrl = `ws://localhost:8080/ws?token=${token}`
    this.ws = new WebSocket(wsUrl)
    
    this.ws.onopen = () => {
      console.log('WebSocket 连接成功')
      this.startHeartbeat()
    }
    
    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data)
      this.handleMessage(message)
    }
    
    this.ws.onerror = (error) => {
      console.error('WebSocket 错误:', error)
    }
    
    this.ws.onclose = () => {
      console.log('WebSocket 连接关闭')
      this.stopHeartbeat()
      this.reconnect(token)
    }
  }
  
  // 断线重连
  reconnect(token) {
    if (this.reconnectTimer) return
    
    this.reconnectTimer = setTimeout(() => {
      console.log('尝试重新连接 WebSocket...')
      this.connect(token)
      this.reconnectTimer = null
    }, 3000)
  }
  
  // 心跳
  startHeartbeat() {
    this.heartbeatTimer = setInterval(() => {
      if (this.ws && this.ws.readyState === WebSocket.OPEN) {
        this.ws.send('ping')
      }
    }, 30000) // 30秒一次心跳
  }
  
  stopHeartbeat() {
    if (this.heartbeatTimer) {
      clearInterval(this.heartbeatTimer)
      this.heartbeatTimer = null
    }
  }
  
  // 处理消息
  handleMessage(message) {
    this.messageHandlers.forEach(handler => {
      handler(message)
    })
  }
  
  // 添加消息处理器
  addMessageHandler(handler) {
    this.messageHandlers.push(handler)
  }
  
  // 关闭连接
  close() {
    this.stopHeartbeat()
    if (this.reconnectTimer) {
      clearTimeout(this.reconnectTimer)
      this.reconnectTimer = null
    }
    if (this.ws) {
      this.ws.close()
      this.ws = null
    }
  }
}

// 使用
const wsManager = new WebSocketManager()

// 登录成功后连接
wsManager.connect(token)

// 添加消息处理器
wsManager.addMessageHandler((message) => {
  if (message.type === 'CHAT') {
    // 处理私信消息
    handleChatMessage(message)
  } else if (message.type === 'SYSTEM') {
    // 处理系统消息
    handleSystemMessage(message)
  }
})
```

---

### 2.7 智能审核机制（详细实现）

#### 2.7.1 敏感词检测（AC自动机算法）

**AC自动机实现：**
```java
/**
 * AC自动机节点
 */
class TrieNode {
    Map<Character, TrieNode> children = new HashMap<>();
    TrieNode fail; // 失败指针
    boolean isEnd; // 是否是敏感词结尾
    String word; // 敏感词
}

/**
 * AC自动机
 */
public class AhoCorasick {
    private TrieNode root = new TrieNode();
    
    /**
     * 构建AC自动机
     */
    public void build(List<String> words) {
        // 1. 构建Trie树
        for (String word : words) {
            TrieNode node = root;
            for (char c : word.toCharArray()) {
                node = node.children.computeIfAbsent(c, k -> new TrieNode());
            }
            node.isEnd = true;
            node.word = word;
        }
        
        // 2. 构建失败指针（BFS）
        Queue<TrieNode> queue = new LinkedList<>();
        root.fail = root;
        
        for (TrieNode child : root.children.values()) {
            child.fail = root;
            queue.offer(child);
        }
        
        while (!queue.isEmpty()) {
            TrieNode node = queue.poll();
            
            for (Map.Entry<Character, TrieNode> entry : node.children.entrySet()) {
                char c = entry.getKey();
                TrieNode child = entry.getValue();
                queue.offer(child);
                
                TrieNode failNode = node.fail;
                while (failNode != root && !failNode.children.containsKey(c)) {
                    failNode = failNode.fail;
                }
                
                child.fail = failNode.children.getOrDefault(c, root);
            }
        }
    }
    
    /**
     * 检测文本中的敏感词
     */
    public List<String> search(String text) {
        List<String> result = new ArrayList<>();
        TrieNode node = root;
        
        for (int i = 0; i < text.length(); i++) {
            char c = text.charAt(i);
            
            // 转小写
            c = Character.toLowerCase(c);
            
            // 跳过空格等特殊字符
            if (!Character.isLetterOrDigit(c) && !isChinese(c)) {
                continue;
            }
            
            while (node != root && !node.children.containsKey(c)) {
                node = node.fail;
            }
            
            node = node.children.getOrDefault(c, root);
            
            // 检查所有匹配的敏感词
            TrieNode temp = node;
            while (temp != root) {
                if (temp.isEnd) {
                    result.add(temp.word);
                }
                temp = temp.fail;
            }
        }
        
        return result;
    }
    
    private boolean isChinese(char c) {
        return c >= 0x4E00 && c <= 0x9FA5;
    }
}

/**
 * 敏感词服务
 */
@Service
public class SensitiveWordService {
    
    private AhoCorasick ahoCorasick = new AhoCorasick();
    
    @Autowired
    private SensitiveWordMapper sensitiveWordMapper;
    
    @PostConstruct
    public void init() {
        // 从数据库加载敏感词
        List<SensitiveWord> words = sensitiveWordMapper.selectList(null);
        List<String> wordList = words.stream()
            .map(SensitiveWord::getWord)
            .collect(Collectors.toList());
        
        // 构建AC自动机
        ahoCorasick.build(wordList);
        
        System.out.println("敏感词库加载完成，共 " + words.size() + " 个敏感词");
    }
    
    /**
     * 检测文本
     */
    public SensitiveWordCheckResult check(String text) {
        List<String> foundWords = ahoCorasick.search(text);
        
        if (foundWords.isEmpty()) {
            return SensitiveWordCheckResult.pass();
        }
        
        // 检查敏感词严重程度
        for (String word : foundWords) {
            SensitiveWord sw = sensitiveWordMapper.selectByWord(word);
            if (sw != null && "SEVERE".equals(sw.getLevel())) {
                return SensitiveWordCheckResult.reject("包含严重敏感词：" + word);
            }
        }
        
        return SensitiveWordCheckResult.needManualReview("包含敏感词：" + String.join("、", foundWords));
    }
}
```

---

### 2.8 管理员功能（详细实现）

#### 2.8.1 内容管理

**内容列表页：** `/admin/content/list`

**功能：**
- 查看所有已发布的内容（失物、商品、任务）
- 按类型筛选、按状态筛选、搜索
- 下架违规内容
- 删除不当内容

**下架功能实现：**
```java
@Service
public class ContentManageService {
    
    /**
     * 下架内容
     */
    @Transactional
    public void offshelfContent(String contentType, Long contentId, OffshelfDTO dto, Long adminId) {
        if ("LOST_FOUND".equals(contentType)) {
            // 下架失物
            LostFound lostFound = lostFoundMapper.selectById(contentId);
            if (lostFound == null) {
                throw new BusinessException("内容不存在");
            }
            
            lostFound.setStatus("ADMIN_OFFSHELF");
            lostFound.setOffshelfType("ADMIN");
            lostFound.setOffshelfReason(dto.getReason());
            lostFound.setOffshelfTime(new Date());
            lostFound.setOffshelfAdminId(adminId);
            lostFoundMapper.updateById(lostFound);
            
            // 发送通知给发布者
            notifyUser(lostFound.getUserId(), "内容被下架", 
                "您发布的失物《" + lostFound.getTitle() + "》因违规被下架。原因：" + dto.getReason() + "。您可以修改后重新提交。");
                
        } else if ("GOODS".equals(contentType)) {
            // 下架商品（类似）
            // ...
        } else if ("TASK".equals(contentType)) {
            // 下架任务（类似）
            // ...
        }
    }
}
```

#### 2.8.2 用户管理

**封禁功能实现：**
```java
@Service
public class UserManageService {
    
    /**
     * 封禁用户
     */
    @Transactional
    public void banUser(Long userId, BanDTO dto, Long adminId) {
        // 1. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 不能封禁管理员
        if ("ADMIN".equals(user.getRole())) {
            throw new BusinessException("不能封禁管理员");
        }
        
        // 3. 更新用户状态
        user.setStatus(2); // 已封禁
        user.setBanType(dto.getBanType());
        user.setBanReason(dto.getReason());
        user.setBanTime(new Date());
        user.setBanAdminId(adminId);
        
        if ("TEMPORARY".equals(dto.getBanType())) {
            user.setBanDays(dto.getBanDays());
            // 计算解封时间
            Calendar calendar = Calendar.getInstance();
            calendar.add(Calendar.DAY_OF_MONTH, dto.getBanDays());
            user.setUnbanTime(calendar.getTime());
        }
        
        userMapper.updateById(user);
        
        // 4. 保存封禁记录
        UserBanRecord record = new UserBanRecord();
        record.setUserId(userId);
        record.setBanType(dto.getBanType());
        record.setBanReason(dto.getReason());
        record.setBanDays(dto.getBanDays());
        record.setBanTime(new Date());
        record.setUnbanTime(user.getUnbanTime());
        record.setBanAdminId(adminId);
        banRecordMapper.insert(record);
        
        // 5. 踢出当前登录
        String key = "token:" + userId;
        redisTemplate.delete(key);
        
        // 6. 发送封禁通知（邮件）
        sendEmailNotification(user.getEmail(), "账号封禁通知", 
            "您的账号因违规被封禁。\n" +
            "封禁类型：" + (dto.getBanType().equals("PERMANENT") ? "永久封禁" : "临时封禁" + dto.getBanDays() + "天") + "\n" +
            "封禁原因：" + dto.getReason() + "\n" +
            "如有疑问，请联系管理员。");
    }
}
```

---

## 三、数据库设计

### 3.1 数据库设计说明

**设计原则：**
1. 遵循第三范式（3NF），减少数据冗余
2. 合理使用索引，提升查询性能
3. 使用乐观锁（version字段）处理并发问题
4. 所有表使用逻辑删除（delete_flag字段）
5. 统一时间字段（create_time、update_time）

**关键字段说明：**
- **view_count**：浏览次数，用于热度排序和统计
- **version**：乐观锁版本号，用于并发控制
- **audit_trigger_reason**：触发人工审核的原因，用于审核优化

**注意：** 失物招领模块不包含收藏和评论功能，因此不包含 `favorite_count` 和 `comment_count` 字段。其他模块（闲置交易、跑腿服务）可能包含这些字段。

### 3.2 用户表（user）

```sql
CREATE TABLE `user` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `email` VARCHAR(100) NOT NULL COMMENT '邮箱（用于登录）',
  `password` VARCHAR(255) NOT NULL COMMENT '密码（加密存储）',
  `nickname` VARCHAR(50) NOT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `gender` TINYINT(1) DEFAULT NULL COMMENT '性别：0-女，1-男',
  `grade` VARCHAR(20) DEFAULT NULL COMMENT '年级',
  `major` VARCHAR(100) DEFAULT NULL COMMENT '专业',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  
  -- 实名认证字段
  `real_name` VARCHAR(50) DEFAULT NULL COMMENT '真实姓名（实名认证）',
  `id_card` VARCHAR(18) DEFAULT NULL COMMENT '身份证号（实名认证）',
  `student_id` VARCHAR(50) DEFAULT NULL COMMENT '学号（实名认证）',
  `user_type` VARCHAR(20) DEFAULT '学生' COMMENT '用户类型：学生、教师',
  `is_verified` TINYINT(1) DEFAULT 0 COMMENT '是否实名认证：0-未认证，1-已认证',
  `verification_status` VARCHAR(20) DEFAULT 'NOT_VERIFIED' COMMENT '认证状态：NOT_VERIFIED、PENDING、VERIFIED、REJECTED',
  `verification_proof` TEXT DEFAULT NULL COMMENT '认证证明文件（JSON数组）',
  `verification_submit_time` DATETIME DEFAULT NULL COMMENT '认证提交时间',
  `verification_audit_time` DATETIME DEFAULT NULL COMMENT '认证审核时间',
  `verification_audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '认证拒绝原因',
  `verification_audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  
  -- 用户角色和状态
  `role` VARCHAR(20) DEFAULT 'USER' COMMENT '用户角色：USER-普通用户，ADMIN-管理员',
  `status` TINYINT(1) DEFAULT 1 COMMENT '账号状态：1-正常，2-已封禁',
  `ban_type` VARCHAR(20) DEFAULT NULL COMMENT '封禁类型：TEMPORARY-临时，PERMANENT-永久',
  `ban_reason` VARCHAR(500) DEFAULT NULL COMMENT '封禁原因',
  `ban_days` INT(11) DEFAULT NULL COMMENT '封禁天数',
  `ban_time` DATETIME DEFAULT NULL COMMENT '封禁时间',
  `unban_time` DATETIME DEFAULT NULL COMMENT '解封时间',
  `ban_admin_id` BIGINT(20) DEFAULT NULL COMMENT '封禁操作管理员ID',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_email` (`email`),
  KEY `idx_is_verified` (`is_verified`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 3.3 失物招领表（lost_found）

```sql
CREATE TABLE `lost_found` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '失物ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '发布者ID',
  `title` VARCHAR(200) NOT NULL COMMENT '物品名称',
  `category` VARCHAR(50) NOT NULL COMMENT '物品分类：证件类、电子产品、生活用品、书籍、其他',
  `description` TEXT NOT NULL COMMENT '物品描述',
  `type` VARCHAR(20) NOT NULL COMMENT '类型：LOST-失物，FOUND-招领',
  `lost_time` DATETIME NOT NULL COMMENT '丢失/拾取时间',
  `lost_location` VARCHAR(200) NOT NULL COMMENT '丢失/拾取地点',
  `contact_info` VARCHAR(200) DEFAULT NULL COMMENT '联系方式',
  `images` TEXT DEFAULT NULL COMMENT '物品图片（JSON数组）',
  `reward` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '悬赏金额',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，PENDING_CLAIM-待认领，CLAIMING-认领中，CLAIMED-已认领，CLOSED-已关闭，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态：PENDING-待审核，APPROVED-已通过，REJECTED-已拒绝',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因（敏感词、发布频繁等）',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型：USER-用户自行下架，ADMIN-管理员下架',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  
  -- 并发控制
  `version` INT(11) DEFAULT 0 COMMENT '乐观锁版本号',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_type` (`type`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_status_type_create_time` (`status`, `type`, `create_time` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_lost_found_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='失物招领表';
```

### 3.4 商品表（goods）

```sql
CREATE TABLE `goods` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '商品ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '卖家ID',
  `title` VARCHAR(200) NOT NULL COMMENT '商品标题',
  `category` VARCHAR(50) NOT NULL COMMENT '商品分类：数码产品、图书教材、服装鞋包、生活用品、运动健身、乐器、文创用品、其他',
  `description` TEXT NOT NULL COMMENT '商品描述',
  `original_price` DECIMAL(10, 2) NOT NULL COMMENT '原价',
  `current_price` DECIMAL(10, 2) NOT NULL COMMENT '现价',
  `condition` VARCHAR(50) DEFAULT NULL COMMENT '商品成色：全新、几乎全新、轻微使用痕迹、明显使用痕迹',
  `images` TEXT DEFAULT NULL COMMENT '商品图片（JSON数组，1-9张）',
  `trade_method` VARCHAR(50) DEFAULT NULL COMMENT '交易方式：FACE_TO_FACE-面交，MAIL-邮寄，BOTH-面交+邮寄',
  `trade_location` VARCHAR(200) DEFAULT NULL COMMENT '交易地点（面交时必填）',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，ON_SALE-在售，SOLD_OUT-已售出，CLOSED-已关闭，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  `favorite_count` INT(11) DEFAULT 0 COMMENT '收藏次数',
  `comment_count` INT(11) DEFAULT 0 COMMENT '评论次数',
  
  -- 并发控制
  `version` INT(11) DEFAULT 0 COMMENT '乐观锁版本号',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_current_price` (`current_price`),
  KEY `idx_view_count` (`view_count`),
  KEY `idx_status_create_time` (`status`, `create_time` DESC),
  KEY `idx_status_view_count` (`status`, `view_count` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_goods_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

### 3.5 跑腿任务表（errand_task）

```sql
CREATE TABLE `errand_task` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '发布者ID',
  `title` VARCHAR(200) NOT NULL COMMENT '任务标题',
  `type` VARCHAR(50) NOT NULL COMMENT '任务类型：代取快递、代买物品、代送物品、代排队、其他',
  `description` TEXT NOT NULL COMMENT '任务描述',
  `start_location` VARCHAR(200) NOT NULL COMMENT '起始地点',
  `end_location` VARCHAR(200) NOT NULL COMMENT '目的地',
  `expected_time` DATETIME DEFAULT NULL COMMENT '期望完成时间',
  `reward` DECIMAL(10, 2) NOT NULL COMMENT '报酬',
  `images` TEXT DEFAULT NULL COMMENT '任务图片（JSON数组，最多3张）',
  
  -- 状态字段
  `status` VARCHAR(20) DEFAULT 'PENDING_REVIEW' COMMENT '状态：PENDING_REVIEW-待审核，PENDING_ACCEPT-待接单，ACCEPTED-已接单，IN_PROGRESS-进行中，PENDING_CONFIRM-待确认，COMPLETED-已完成，CANCELLED-已取消，REJECTED-已拒绝，ADMIN_OFFSHELF-已下架',
  
  -- 审核字段
  `audit_status` VARCHAR(20) DEFAULT 'PENDING' COMMENT '审核状态',
  `audit_reason` VARCHAR(500) DEFAULT NULL COMMENT '审核拒绝原因',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_admin_id` BIGINT(20) DEFAULT NULL COMMENT '审核管理员ID',
  `audit_trigger_reason` VARCHAR(500) DEFAULT NULL COMMENT '触发人工审核原因',
  
  -- 下架字段
  `offshelf_type` VARCHAR(20) DEFAULT NULL COMMENT '下架类型',
  `offshelf_reason` VARCHAR(500) DEFAULT NULL COMMENT '下架原因',
  `offshelf_time` DATETIME DEFAULT NULL COMMENT '下架时间',
  `offshelf_admin_id` BIGINT(20) DEFAULT NULL COMMENT '下架管理员ID',
  
  -- 接单信息
  `accepter_id` BIGINT(20) DEFAULT NULL COMMENT '接单用户ID',
  `accept_time` DATETIME DEFAULT NULL COMMENT '接单时间',
  `complete_time` DATETIME DEFAULT NULL COMMENT '完成时间',
  `completion_proof` TEXT DEFAULT NULL COMMENT '完成凭证（图片JSON数组）',
  
  -- 统计字段
  `view_count` INT(11) DEFAULT 0 COMMENT '浏览次数',
  `favorite_count` INT(11) DEFAULT 0 COMMENT '收藏次数',
  `comment_count` INT(11) DEFAULT 0 COMMENT '评论次数',
  
  -- 并发控制
  `version` INT(11) DEFAULT 0 COMMENT '乐观锁版本号',
  
  -- 系统字段
  `delete_flag` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_type` (`type`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_accepter_id` (`accepter_id`),
  KEY `idx_reward` (`reward`),
  KEY `idx_view_count` (`view_count`),
  KEY `idx_status_reward` (`status`, `reward` DESC),
  KEY `idx_status_create_time` (`status`, `create_time` DESC),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `fk_errand_task_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
  CONSTRAINT `fk_errand_task_accepter` FOREIGN KEY (`accepter_id`) REFERENCES `user` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='跑腿任务表';
```

### 3.6 其他核心表

**（完整的数据库设计包含以下表，具体SQL见数据库操作文档）**

- **订单表（order）**：管理闲置交易订单
- **认领记录表（claim_record）**：管理失物认领申请
- **评论表（comment）**：支持多级评论
- **收藏表（favorite）**：用户收藏功能
- **私信会话表（chat_session）**：私信会话管理
- **私信消息表（chat_message）**：私信消息记录
- **系统消息表（system_message）**：系统通知
- **敏感词表（sensitive_word）**：敏感词库
- **举报记录表（report_record）**：举报管理
- **操作日志表（operation_log）**：操作审计

### 3.7 数据库设计亮点

1. **统计字段**：view_count支持数据展示和排序（失物招领模块不包含收藏和评论统计）
2. **完善的审核机制**：audit_status、audit_trigger_reason支持智能审核
3. **灵活的下架管理**：offshelf_type区分用户自行下架和管理员下架
4. **并发控制**：version字段支持乐观锁，避免并发问题
5. **性能优化**：合理的索引设计，支持多维度查询

### 3.8 MainLayout.vue 数据支持分析

**数据完整性检查：**

| 模块 | 所需字段 | 数据库支持 | 状态 |
|------|---------|-----------|------|
| **失物招领** | id, title, description, image, type, location, time, userAvatar, userName | ✅ 全部支持 | ✅ |
| **闲置交易** | id, title, price, views, image, userAvatar, userName, time, badge | ✅ 全部支持（views=view_count） | ✅ |
| **跑腿服务** | id, title, description, route, deadline, reward, userAvatar, userName | ✅ 全部支持 | ✅ |
| **数据统计** | 总数、活跃用户、趋势图 | ✅ 可实时查询 | ✅ |

**结论：** ✅ 数据库设计完全支持 MainLayout.vue 的所有数据展示需求！

---

## 四、开发计划

**第一阶段：基础框架搭建（1周）**
- 后端：Spring Boot 项目初始化，配置 MyBatis-Plus、Redis、MySQL
- 前端：Vue 项目初始化，配置 Element Plus、Pinia、Vue Router
- 数据库设计和创建

**第二阶段：用户管理模块（1周）**
- 用户注册登录（邮箱验证码）
- 用户信息管理
- 实名认证功能（表单、审核）
- 权限验证

**第三阶段：失物招领模块（1周）**
- 失物发布（智能审核）
- 失物浏览搜索
- 认领功能（私信沟通）

**第四阶段：闲置交易模块（1.5周）**
- 商品发布（需实名认证）
- 商品浏览搜索
- 订单管理（权限验证）
- 交易流程

**第五阶段：跑腿服务模块（1.5周）**
- 任务发布（需实名认证）
- 任务浏览搜索
- 接单功能（权限验证）
- 任务管理

**第六阶段：消息通知模块（1周）**
- 私信功能（自动创建会话）
- 系统消息
- WebSocket 实时推送

**第七阶段：审核和管理模块（1周）**
- 智能审核机制（AC自动机）
- 管理员后台
- 内容管理（下架功能）
- 用户管理（封禁功能）

**第八阶段：搜索功能优化（0.5周）**
- 关键词搜索功能
- 搜索历史功能

**第九阶段：数据统计（0.5周）**
- 数据统计页面
- ECharts 图表

**第十阶段：测试和优化（1周）**
- 功能测试
- 性能优化
- Bug 修复

**总计：约10周**

---

## 五、总结

本文档定义了校园互助系统的核心功能和详细实现方案：

**核心特点：**
1. **功能聚焦**：失物招领、闲置交易、跑腿服务三大核心功能
2. **权限清晰**：
   - 失物招领：所有用户可用
   - 闲置交易和跑腿：需要实名认证
3. **智能审核**：自动审核 + 人工审核，减轻管理负担
4. **简洁易用**：界面简洁，操作便捷
5. **技术成熟**：Spring Boot + Vue + MySQL，开发难度可控

**实名认证权限控制：**
- ⚠️ 未认证用户：只能使用失物招领
- ✅ 已认证用户：可使用所有功能（闲置交易、跑腿服务）

**下一步工作：**
1. 创建数据库和表
2. 搭建后端项目
3. 搭建前端项目
4. 按阶段逐步开发

---

**文档版本：** v2.1  
**创建日期：** 2024-01-01  
**最近更新：** 2024-01-02  
**文档维护：** 开发团队